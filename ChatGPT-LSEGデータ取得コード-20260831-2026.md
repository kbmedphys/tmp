# LSEGデータ取得コード

**User:** Anonymous  
**Created:** 8/31/2026 15:14:43  
**Updated:** 8/31/2026 15:50:05  
**Exported:** 8/31/2026 20:26:52  
**Link:** [https://chatgpt.com/c/6a951bd3-1b8c-83e8-98c2-1dcaf404e359](https://chatgpt.com/c/6a951bd3-1b8c-83e8-98c2-1dcaf404e359)  

## Response:
2026/8/31 15:14:56

LSEG Data Libraryの現行仕様に沿って、経済指標イベントの取得方法と日本CPIの識別子・フィールドを確認し、再利用可能なPythonコードに落とし込みます。

直近分は`FCAST_PRD`、`ECI_ACT_DT`＋`ACT_VAL_NS`、`RTR_POLL`、`ECON_ACT`で取得できます。一方、過去の公表日は`get_history()`ではなく別のイベントAPI等を検討します。

> **調査方針を整理中**
>
> 日本のCPIについて、基準日・発表日・予想値・実績値をLSEG Data Library for Pythonで取得する方法を確認しています。現在のAPI仕様と公式ドキュメントを優先し、実行可能なコード例にまとめます。
>
> **整理した調査方針**
>
> 
>
> **ウェブを検索中**
>
> [LSEG Data Library for Python](https://developers.lseg.com/en/api-catalog/lseg-data-platform/lseg-data-library-for-python)
>
> **ウェブを検索中**
>
> [Retrieve Cross Economic Data using Data Library for Python](https://developers.lseg.com/en/article-catalog/article/retrieve-cross-economic-data-using-data-library)
> [Is There a Way to Automatically Update Economic Events ...](https://community.developers.lseg.com/discussion/134450/is-there-a-way-to-automatically-update-economic-events-for-each-new-week-using-python)
>
> ****
>
> [How do I get the countries CPI values from Thomson ...](https://community.developers.lseg.com/discussion/50774/how-do-i-get-the-countries-cpi-values-from-thomson-reuters-eikon-api-via-python-spyder-or-excel)
> [Economic Indicators with Datastream](https://developers.lseg.com/en/article-catalog/article/economic-indicators-with-datastream)
>
> **仕様を追加確認中**
>
> 公式情報だけでは指標コードや取得項目の対応が一度で確認できないため、まず日本CPIの候補コードを検索し、基準日・発表日・予想・実績を取得する手順へ整理しています。
>
> **仕様を追加確認した**
>
> 
>
> **ウェブを検索中**
>
> [Historical and scheduled dates and time of an economic ...](https://community.developers.lseg.com/discussion/98352/historical-and-scheduled-dates-and-time-of-an-economic-event-per-eikon-api-via-python)
>
> **取得方法を整理中**
>
> 最新値の取得と、過去の発表ごとの予想・実績履歴では使うデータ構造が異なりそうです。まず候補指標を検索で特定し、発表日・対象期間・予想値・実績値の対応を分けて示します。
>
> **取得方法を整理した**
>
> 
>
> **ウェブを検索中**
>
> [Economic indicators from ek.get_data ek.get_timeseries](https://community.developers.lseg.com/)
> [Using Economic Indicators with Eikon Data API](https://developers.lseg.com/en/article-catalog/article/using-economic-indicators-with-eikon-data-api-a-machine-learning-example)
>
> **項目対応を確認中**
>
> LSEG経済指標では、対象期間、発表日時、予想値、実績値が別項目です。現在、各項目の正式な対応関係と、最新値・過去履歴を安全に区別できる形へ整えています。
>
> **項目対応を確認しました**
>
> 
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **ウェブを検索中**
>
> [Building Search into your Application Workflow](https://developers.lseg.com/en/article-catalog/article/building-search-into-your-application-workflow)
>
> **コード構成を固め中**
>
> まず検索で日本CPIの指標コードを見つけ、取得結果の列名を確認します。その後、対象期間・発表日時・市場予想・実績値を整形し、履歴取得の制約も明記します。
>
> **コード構成を固めた**
>
> 
>
> **ウェブを検索中**
>
> [Retrieve Cross Economic Data using Data Library for Python](https://developers.lseg.com/en/article-catalog/article/retrieve-cross-economic-data-using-data-library)
> [Historical and scheduled dates and time of an economic ...](https://community.developers.lseg.com/discussion/98352/historical-and-scheduled-dates-and-time-of-an-economic-event-per-eikon-api-via-python)
>
> **まとめを準備中**
>
> 日本CPIは指標コードを決め打ちせず、まず検索結果から確認する構成にします。最新値取得と発表履歴取得を分け、対象期間・発表日時・予想・実績の対応を明確に示します。
>
> **まとめを準備した**
>
> 
>
> **ウェブを検索中**
>
> [Exact endpoints and documentation for building an ...](https://community.developers.lseg.com/discussion/134051/exact-endpoints-and-documentation-for-building-an-economic-calendar-real-time-historical)
> [Product and Content Support | MyAccount](https://myaccount.lseg.com/en/productsupport.ht)
>
> **詰め方を整理中**
>
> 最新値は取得しやすい一方、過去の発表日・予想・実績を同じ表で再現するには別の履歴データが必要です。私は両者を混同しないコード構成にしています。
>
> **取得方法を整理中**
>
> 日本CPIでは、対象期間・公表日時・市場予想・実績値を別項目として扱います。最新値の取得と、過去発表の完全な履歴取得は分けて考え、まず指標コードを確認する流れにします。
>
> **整理して提示中**
>
> 日本CPIの「対象期間・公表日時・予測値・実績値」を、LSEG Data Library for Pythonで確認する手順にまとめています。最新値と過去発表履歴では取得方法が異なるため、両者を分けて説明します。
>
> **詰め方と取得方法を整理して提示**
>
> 
>
> **ウェブを検索中**
>
> [Getting Benchmark Revisions for Economic Indicator](https://community.developers.lseg.com/discussion/132985/getting-benchmark-revisions-for-economic-indicator)
>
> **履歴取得を区別中**
>
> 通常の履歴系列では対象期間や値は取れても、各発表日の予想・実績を完全には再現できない場合があります。発表履歴が必要なら、経済イベント用データ製品や保存済みスナップショットを別途検討します。
>
> **使い方をまとめる**
>
> 日本CPIは、まず経済指標の候補コードを検索し、選んだコードの項目を確認します。対象期間・公表日時・予測値・実績値を分けて取得し、過去の発表履歴は別扱いにします。
>
> **対応項目を確定中**
>
> 「参照日」は対象期間と観測日を分けて表示します。公表日は発表日時、予測値は市場コンセンサス、実績値は公表結果として整理し、過去履歴の制約も併記します。
>
> **履歴取得と使い方を整理し対応項目を確定しました**
>
> 
>
> **ウェブを検索中**
>
> [get_history versus get_data (pros and cons)](https://community.developers.lseg.com/discussion/112791/get-history-versus-get-data-pros-and-cons)
> [LSEG Data Library for Python v2.1.0 (March 27, 2025)](https://developers.lseg.com/en/api-catalog/lseg-data-platform/lseg-data-library-for-python/documentation/what-is-new/release-notes)
>
> **対応項目を整理中**
>
> LSEGの経済指標では、対象期間・観測日・公表日時・市場予想・実績値が別項目です。私はまず指標コードを特定し、最新値と過去発表履歴を混同しない形でコード例へまとめています。
>
> **実装手順を確定**
>
> 日本CPIの対象期間・観測日・公表日時・予測値・実績値を分離して取得するコードにします。指標コードは検索で確認し、最新スナップショットと過去発表履歴の違いも明記します。
>
> **対応項目を整理し実装手順を確定**
>
> 
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **取得した日本CPIスナップショットコード**
>
> 
>
> 17m 58s考えました

## 結論

取得可能です。ただし、LSEGでは次のようにデータ取得方法を分ける必要があります。

| 取得対象 | 最新リリース | 過去の履歴 |
|---|---|---|
| 参照日・参照期間 | `FCAST_PRD` | `ld.get_history()` のインデックス |
| 公表日・公表時刻 | `ECI_ACT_DT`、`ACT_VAL_NS` | Datastreamの `DREL1`、`TREL1` |
| 予測値 | `RTR_POLL` | Reuters Poll用RIC |
| 実績値 | `ECON_ACT` | 現行系列は `ld.get_history()`、初回公表値は `REL1` |

`ld.get_data()` は最新イベントのスナップショットを取得できます。一方、`ld.get_history()` は過去の実績時系列を取得できますが、過去各回の公表日や `ECON_ACT`、`RTR_POLL` を列として返す仕組みではありません。過去の初回公表日と初回公表値まで必要な場合は、Datastream DSWSの利用が適しています。([LSEG Developers](https://developers.lseg.com/en/article-catalog/article/retrieve-cross-economic-data-using-data-library))

なお、以下で使用する `JPCPI=ECI` は、一般に市場で注目される**全国コアCPI前年比、すなわち生鮮食品を除く総合CPI**です。総合CPIや「生鮮食品およびエネルギーを除く総合」を取得したい場合は、別のRICを指定する必要があります。([Reuters](https://www.reuters.com/article/japan-economy-cpi-idINT28145520080228/))

---

## 1. パッケージ

ユーザー環境に合わせて、`uv` では次のように追加します。

```bash
uv add lseg-data pandas
```

過去の初回公表日まで取得する場合のみ、Datastream用ライブラリも追加します。

```bash
uv add DatastreamPy
```

Workspace Desktop Sessionを使用する場合は、LSEG Workspaceを起動した状態で `ld.open_session()` を実行します。([LSEG Developers](https://developers.lseg.com/en/article-catalog/article/retrieve-cross-economic-data-using-data-library))

---

# 2. 最新のCPI：参照日・公表日・予測値・実績値

最新公表分については、`lseg.data` だけで4項目を取得できます。

```python
import re

import pandas as pd
import lseg.data as ld
from IPython.display import display

# 全国コアCPI前年比：生鮮食品を除く総合
CPI_RIC = "JPCPI=ECI"

MONTH_NUMBER = {
    "Jan": 1,
    "Feb": 2,
    "Mar": 3,
    "Apr": 4,
    "May": 5,
    "Jun": 6,
    "Jul": 7,
    "Aug": 8,
    "Sep": 9,
    "Oct": 10,
    "Nov": 11,
    "Dec": 12,
}

def forecast_period_to_month_end(value) -> pd.Timestamp:
    """
    FCAST_PRDが 'Jul 26' または 'Jul 2026' のような形式の場合に、
    参照月末日へ変換する。
    """
    if pd.isna(value):
        return pd.NaT

    text = str(value).strip()
    match = re.fullmatch(
        r"([A-Za-z]{3})\s+(\d{2}|\d{4})",
        text,
    )

    if match is None:
        return pd.NaT

    month = MONTH_NUMBER.get(match.group(1).title())
    if month is None:
        return pd.NaT

    year_text = match.group(2)
    year = int(year_text)

    if len(year_text) == 2:
        year = 2000 + year if year <= 69 else 1900 + year

    return (
        pd.Timestamp(year=year, month=month, day=1)
        + pd.offsets.MonthEnd(0)
    )

def combine_gmt_date_time(
    date_series: pd.Series,
    time_series: pd.Series,
) -> pd.Series:
    """
    ECI_ACT_DTとACT_VAL_NSをUTC日時にまとめる。
    ナノ秒部分は切り捨てて秒まで使用する。
    """
    date_text = (
        pd.to_datetime(date_series, errors="coerce")
        .dt.strftime("%Y-%m-%d")
        .astype("string")
    )

    time_text = (
        time_series.astype("string")
        .str.extract(r"(\d{1,2}:\d{2}:\d{2})", expand=False)
    )

    return pd.to_datetime(
        date_text + " " + time_text,
        errors="coerce",
        utc=True,
    )

fields = [
    "TR.IndicatorName",
    "TR.IndicatorType",
    "TR.IndicatorSource",

    # 参照期間
    "FCAST_PRD",

    # 公表日・公表時刻
    "ECI_ACT_DT",
    "ACT_VAL_NS",

    # 予測値・実績値
    "RTR_POLL",
    "ECON_ACT",

    # 参考
    "ECON_PRIOR",
    "ECON_REV",
    "FCAST_LOW",
    "FCAST_HIGH",
    "FCAST_NUM",
    "NDOR_1",
]

ld.open_session()

try:
    latest_raw = ld.get_data(
        universe=[CPI_RIC],
        fields=fields,
    )
finally:
    ld.close_session()

latest = latest_raw.rename(
    columns={
        "FCAST_PRD": "参照期間",
        "ECI_ACT_DT": "公表日_GMT",
        "ACT_VAL_NS": "公表時刻_GMT",
        "RTR_POLL": "予測値",
        "ECON_ACT": "実績値",
        "ECON_PRIOR": "前回値",
        "ECON_REV": "改定値",
        "FCAST_LOW": "予測下限",
        "FCAST_HIGH": "予測上限",
        "FCAST_NUM": "予測回答数",
        "NDOR_1": "次回公表日",
    }
).copy()

# FCAST_PRDを参照月末日に変換
if "参照期間" in latest.columns:
    latest["参照日"] = latest["参照期間"].map(
        forecast_period_to_month_end
    )

# GMTの公表日・時刻を結合し、日本時間にも変換
required = {"公表日_GMT", "公表時刻_GMT"}

if required.issubset(latest.columns):
    latest["公表日時_UTC"] = combine_gmt_date_time(
        latest["公表日_GMT"],
        latest["公表時刻_GMT"],
    )

    latest["公表日時_JST"] = (
        latest["公表日時_UTC"]
        .dt.tz_convert("Asia/Tokyo")
    )

output_columns = [
    "Instrument",
    "参照日",
    "参照期間",
    "公表日時_UTC",
    "公表日時_JST",
    "予測値",
    "実績値",
    "前回値",
    "改定値",
    "予測下限",
    "予測上限",
    "予測回答数",
    "次回公表日",
]

display(
    latest[
        [column for column in output_columns if column in latest.columns]
    ]
)
```

`FCAST_PRD` は対象となる予測・実績の期間、`ECI_ACT_DT` は公表日、`ACT_VAL_NS` は公表時刻、`RTR_POLL` はReuters Pollのコンセンサス、`ECON_ACT` は最新の実績値です。LSEGの公式サンプルでも同じフィールド構成が使用されています。([LSEG Developers](https://developers.lseg.com/en/article-catalog/article/retrieve-cross-economic-data-using-data-library))

日本のCPIは通常、日本時間の朝に公表されるため、GMT日付では前日になることがあります。そのため、`ECI_ACT_DT` をそのまま「日本の公表日」とせず、`ACT_VAL_NS` と結合してJSTに変換する処理を入れています。

---

# 3. 過去の参照日・予測値・実績値

## 3.1 `lseg.data` のみで取得する場合

過去の実績値は `JPCPI=ECI`、Reuters Pollの中央値は、一般的なRIC命名規則を当てはめると `pJPCPI=M` が第一候補です。

過去のReuters Pollでは、一般に次のRIC形式が使われます。

- `p<ECIのベースRIC>=M`：中央値
- `p<ECIのベースRIC>=L`：最低値
- `p<ECIのベースRIC>=H`：最高値

LSEGの例では、GDPについて `pEUGDQY=M/L/H`、米国CPIについて `pUSCPI=M` が使用されています。したがって `pJPCPI=M` はこの規則から推定した候補であり、実際の契約・コンテンツ環境で解決するかをWorkspaceのQuoteまたはEconomic Monitorで確認してください。([LSEG Developer Community](https://community.developers.lseg.com/discussion/62764/time-series-of-economic-events-using-python-api))

```python
import pandas as pd
import lseg.data as ld
from IPython.display import display

CPI_RIC = "JPCPI=ECI"

# Reuters Poll中央値の候補RIC
POLL_RIC = "pJPCPI=M"

START = "2010-01-01"
END = pd.Timestamp.now(
    tz="Asia/Tokyo"
).date().isoformat()

def get_single_value_history(
    ric: str,
    column_name: str,
    start: str,
    end: str,
) -> pd.Series:
    """
    1つのECIまたはPoll RICについて、最初の値列をSeriesとして返す。
    LSEGの返却列がMultiIndexでも動作する。
    """
    frame = ld.get_history(
        universe=[ric],
        start=start,
        end=end,
        interval="monthly",
    )

    if frame is None or frame.empty:
        raise RuntimeError(
            f"{ric} の履歴を取得できませんでした。"
            "RICまたは契約権限を確認してください。"
        )

    series = frame.iloc[:, 0].copy()
    series.index = pd.to_datetime(
        series.index,
        errors="coerce",
    )

    series = pd.to_numeric(
        series,
        errors="coerce",
    )

    series = series[
        ~series.index.isna()
    ]

    # 同一日付が重複している場合は最後の値
    series = series.groupby(level=0).last()
    series.name = column_name

    return series

ld.open_session()

try:
    actual = get_single_value_history(
        ric=CPI_RIC,
        column_name="実績値_現行系列",
        start=START,
        end=END,
    )

    forecast = get_single_value_history(
        ric=POLL_RIC,
        column_name="予測値_ReutersPoll中央値",
        start=START,
        end=END,
    )
finally:
    ld.close_session()

history = (
    pd.concat(
        [actual, forecast],
        axis=1,
        join="outer",
    )
    .dropna(how="all")
    .sort_index()
    .rename_axis("参照日")
    .reset_index()
)

history["サプライズ"] = (
    history["実績値_現行系列"]
    - history["予測値_ReutersPoll中央値"]
)

display(history.tail(24))
```

出力イメージは次のようになります。

| 参照日 | 実績値_現行系列 | 予測値_ReutersPoll中央値 | サプライズ |
|---|---:|---:|---:|
| 2026-05-31 | … | … | … |
| 2026-06-30 | … | … | … |
| 2026-07-31 | … | … | … |

### この方法の制約

ここで `参照日` として返る `ld.get_history()` のインデックスは、CPIが対象とする月、すなわち観測期間です。**実際にその値が公表された日ではありません**。

また、`JPCPI=ECI` の過去の実績系列は、後日の訂正や改定を反映した現行系列である可能性があります。標準の `get_history()` では、`ECON_ACT`、`RTR_POLL`、公表日を過去の各行に付けて取得することはできません。([LSEG Developer Community](https://community.developers.lseg.com/discussion/62764/time-series-of-economic-events-using-python-api))

---

# 4. 過去の公表日と初回公表値まで取得する

バックテストで以下を正確に揃える場合は、Datastream DSWSを使います。

- 参照日
- 初回公表日
- 初回公表時刻
- 公表時点の実績値
- 予測値
- 現在の改定後実績値

Datastreamでは次のフィールドを使用します。

| Datastreamフィールド | 内容 |
|---|---|
| `X` | 現行の時系列値 |
| `REL1` | 初回公表値 |
| `DREL1` | 初回公表日 |
| `TREL1` | 初回公表時刻 |

`REL1` と `DREL1` は、経済指標のpoint-in-timeデータを作るためにLSEG Developer Communityで案内されているフィールドです。LSEGの公式サンプルでも `X`、`DREL1`、`TREL1` を用いて参照日と実際のリリース日時を結び付けています。([LSEG Developer Community](https://community.developers.lseg.com/discussion/90694/retrieve-historical-economic-data-with-release-date-python-api))

## 4.1 Datastreamの取得コード

```python
import os

import pandas as pd
import DatastreamPy as dsweb
from IPython.display import display

START = "2010-01-01"
END = pd.Timestamp.now(
    tz="Asia/Tokyo"
).date().isoformat()

# Reuters RICからDatastreamへのクロスリファレンス。
# 取得できない場合はDatastream Navigatorで確認した
# 日本全国コアCPIのDatastream Mnemonicに置き換える。
DS_CPI_TICKER = "<JPCPI=ECI>"

# 認証情報は環境変数から読み込む
ds = dsweb.DataClient(
    None,
    username=os.environ["DATASTREAM_USERNAME"],
    password=os.environ["DATASTREAM_PASSWORD"],
)

pit_raw = ds.get_data(
    tickers=DS_CPI_TICKER,
    fields=[
        "X",
        "REL1",
        "DREL1",
        "TREL1",
    ],
    kind=1,
    start=START,
    end=END,
    freq="M",
)

def flatten_dsws_columns(frame: pd.DataFrame) -> pd.DataFrame:
    """
    DatastreamPyのMultiIndex列からField部分を取り出す。
    """
    result = frame.copy()

    if isinstance(result.columns, pd.MultiIndex):
        result.columns = [
            column[-1]
            for column in result.columns
        ]
    else:
        result.columns = [
            str(column)
            for column in result.columns
        ]

    result.index = pd.to_datetime(
        result.index,
        errors="coerce",
    )

    result.index.name = "参照日"

    return result.reset_index()

pit = flatten_dsws_columns(
    pit_raw
).rename(
    columns={
        "X": "実績値_現行系列_DS",
        "REL1": "実績値_初回公表",
        "DREL1": "公表日",
        "TREL1": "公表時刻_Datastream",
    }
)

if "公表日" in pit.columns:
    pit["公表日"] = pd.to_datetime(
        pit["公表日"],
        errors="coerce",
        dayfirst=True,
    )

if "公表時刻_Datastream" in pit.columns:
    pit["公表時刻_Datastream"] = (
        pit["公表時刻_Datastream"]
        .astype("string")
        .str.extract(
            r"(\d{1,2}:\d{2}:\d{2})",
            expand=False,
        )
    )

# 実績値が全くない将来行などを除く
actual_columns = [
    column
    for column in [
        "実績値_初回公表",
        "実績値_現行系列_DS",
    ]
    if column in pit.columns
]

if actual_columns:
    pit = pit.dropna(
        subset=actual_columns,
        how="all",
    )

display(pit.tail(24))
```

現在の `DatastreamPy` では `DataClient` が時系列取得用の標準クライアントであり、`get_data()` に `start`、`end`、`freq`、`fields` を指定できます。Datastreamの利用には、Workspaceの契約とは別にDSWSへアクセス可能なDatastreamアカウントまたは権限が必要です。([LSEG Developer Community](https://community.developers.lseg.com/discussion/133860/are-datastreamucs-and-datastreamnavigator-officially-supported-lseg-packages))

---

## 4.2 Reuters Pollと結合する

前節で作成した `history` から予測値を取り出し、Datastreamの参照日と月単位で結合します。

Datastreamの参照日は月の15日など、LSEG Data Library側の月末日と一致しない場合があるため、日付そのものではなく `Period[M]` で結合します。

```python
poll_history = history[
    [
        "参照日",
        "予測値_ReutersPoll中央値",
    ]
].copy()

pit["_reference_month"] = (
    pd.to_datetime(
        pit["参照日"],
        errors="coerce",
    )
    .dt.to_period("M")
)

poll_history["_reference_month"] = (
    pd.to_datetime(
        poll_history["参照日"],
        errors="coerce",
    )
    .dt.to_period("M")
)

# 同じ月に複数観測がある場合は最後のものを使用
poll_history = (
    poll_history
    .sort_values("参照日")
    .drop_duplicates(
        subset="_reference_month",
        keep="last",
    )
)

cpi_release_history = pit.merge(
    poll_history[
        [
            "_reference_month",
            "予測値_ReutersPoll中央値",
        ]
    ],
    on="_reference_month",
    how="left",
)

if {
    "実績値_初回公表",
    "予測値_ReutersPoll中央値",
}.issubset(cpi_release_history.columns):
    cpi_release_history["サプライズ_初回公表ベース"] = (
        cpi_release_history["実績値_初回公表"]
        - cpi_release_history["予測値_ReutersPoll中央値"]
    )

cpi_release_history = (
    cpi_release_history
    .drop(columns="_reference_month")
    .sort_values("参照日")
)

final_columns = [
    "参照日",
    "公表日",
    "公表時刻_Datastream",
    "予測値_ReutersPoll中央値",
    "実績値_初回公表",
    "実績値_現行系列_DS",
    "サプライズ_初回公表ベース",
]

display(
    cpi_release_history[
        [
            column
            for column in final_columns
            if column in cpi_release_history.columns
        ]
    ].tail(24)
)
```

最終的には次のような表になります。

| 参照日 | 公表日 | 公表時刻 | 予測値 | 初回実績値 | 現行実績値 | サプライズ |
|---|---|---|---:|---:|---:|---:|
| 2026-05-15 | 2026-06-19 | 23:30:00 | … | … | … | … |
| 2026-06-15 | 2026-07-24 | 23:30:00 | … | … | … | … |

公表時刻のタイムゾーンは、Datastreamのコンテンツ定義・環境を確認してからJST変換してください。`ECI_ACT_DT` のように公式にGMTと示されたフィールドとは異なり、`TREL1` を機械的にUTCと仮定しない方が安全です。

---

# 5. 総合CPI・コアCPIなどの正確なRICを検索する

`JPCPI=ECI` は全国コアCPIです。総合CPI、コアコアCPI、指数水準、前月比などのRICを確認するには、Search APIで日本の経済指標を抽出できます。

```python
import lseg.data as ld

ld.open_session()

try:
    japan_indicators = ld.discovery.search(
        view=ld.discovery.Views.INDICATOR_QUOTES,
        top=200,
        filter=(
            "(SearchAllCategoryv2 eq 'Economic Indicators') "
            "and (RCSCountryOfIndicatorLeaf in ('Japan')) "
            "and (DataType eq 'Realtime')"
        ),
        select=(
            "RIC,"
            "DTSubjectName,"
            "IDNDescription,"
            "SeriesDescription,"
            "SourceName,"
            "NextReleaseDate,"
            "ObservationDate,"
            "Frequency"
        ),
        order_by="NextReleaseDate Asc",
    )
finally:
    ld.close_session()

search_text = (
    japan_indicators
    .astype("string")
    .fillna("")
    .agg(" ".join, axis=1)
)

cpi_candidates = japan_indicators[
    search_text.str.contains(
        r"\bCPI\b|consumer price|消費者物価",
        case=False,
        regex=True,
        na=False,
    )
].copy()

display(cpi_candidates)
```

Search APIで日本の経済指標RICを列挙することはできますが、Economic Monitorの内部フィルターと完全に同じ結果になるとは限らないため、最終的な系列定義はWorkspaceのEconomic MonitorまたはQuote画面で確認するのが安全です。([LSEG Developer Community](https://community.developers.lseg.com/discussion/134450/is-there-a-way-to-automatically-update-economic-events-for-each-new-week-using-python))

---

## バックテスト用途で推奨する列

最終データセットは、次の構造にするのが適切です。

```text
reference_date
release_date
release_time
forecast_consensus
actual_first_release
actual_latest_revised
surprise_first_release
```

特に予測サプライズ戦略やCPI公表後のイベントスタディでは、

```python
surprise = actual_first_release - forecast_consensus
```

とし、`actual_latest_revised` を使わないことが重要です。後日改定された現行系列を過去時点の実績値として用いると、先読みバイアスが発生します。LSEGのDSWSにおいて `REL1` と `DREL1` が案内されているのも、このpoint-in-time分析を可能にするためです。([LSEG Developer Community](https://community.developers.lseg.com/discussion/90694/retrieve-historical-economic-data-with-release-date-python-api))

DSWS権限がない環境では、日々または各公表直後に `ld.get_data()` の結果をParquetへ追記保存し、独自に初回公表履歴を蓄積する方法が現実的です。過去分を一括バックフィルする場合は、Datastream DSWSまたは別契約のEvents Calendar delivered via APIが候補になります。([LSEG Developer Community](https://community.developers.lseg.com/discussion/134051/exact-endpoints-and-documentation-for-building-an-economic-calendar-real-time-historical))

---

**Sources:**

- [Retrieve Cross Economic Data using Data Library for Python | Developer Community](https://developers.lseg.com/en/article-catalog/article/retrieve-cross-economic-data-using-data-library)
- [Japan Jan nationwide core CPI +0.8 pct yr/yr | Reuters](https://www.reuters.com/article/japan-economy-cpi-idINT28145520080228/)
- [Time series of economic events using Python API - LSEG Developer Community](https://community.developers.lseg.com/discussion/62764/time-series-of-economic-events-using-python-api)



---
Powered by [ChatGPT Exporter](https://www.chatgptexporter.com)