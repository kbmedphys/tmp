# 解説要求 URL実装

**User:** Anonymous  
**Created:** 7/27/2026 21:06:58  
**Updated:** 8/5/2026 19:11:07  
**Exported:** 8/5/2026 20:00:41  
**Link:** [https://chatgpt.com/c/6a6749e1-049c-83ee-8937-a30a66c224d6](https://chatgpt.com/c/6a6749e1-049c-83ee-8937-a30a66c224d6)  

## Response:
2026/7/27 21:06:58

> **ウェブを検索中**
>
>
> **github.com を検索**
>
> 
>
> **GitHubリポジトリのクローンと検索**
>
>
> **クローンしGitHubリポジトリを検索した**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **github.com を検索**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **Searching for raw GitHub file links**
>
> [GitHub - Ashwin-kumar-0309/AI-Storytelling-Engine-with-Multi-Character-Narratives: Interactive AI-powered storytelling platform with dual protagonists, contextual choice generation, and persistent character management using Llama 3.1 and Streamlit · GitHub](https://github.com/Ashwin-kumar-0309/AI-Storytelling-Engine-with-Multi-Character-Narratives)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **ウェブを検索中**
>
>
> **eliteai.tools を検索**
>
> 
>
> **PDFスキルの読み取り**
>
>
> **読み取ったPDFスキル**
>
> 
>
> **ウェブを検索中**
>
>
> **nvlabs.github.io を検索**
>
> 
>
> **GitHub APIへの接続確認**
>
>
> **確認したGitHub APIへの接続**
>
> 
>
> **ウェブを検索中**
>
> [GitHub - briankeithn/narrative-maps: Narrative Maps Visualization Tool · GitHub](https://github.com/briankeithn/narrative-maps)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **ウェブを検索中**
>
>
> **ウェブを検索中**
>
> 
>
> **GitHubリポジトリの情報検索**
>
> [Gitingest](https://gitingest.com/)
> [GitHub - TrelisResearch/trelis-gitingest-mcp: An MCP Server for analysing Github Repo Content with Gitingest · GitHub](https://github.com/TrelisResearch/trelis-gitingest-mcp)
> [Traffico del sito web gitingest.com, ranking, analisi [aprile 2026]](https://it.semrush.com/website/gitingest.com/overview/)
> [GitIngest - Prompt-friendly Codebase](https://gitingestzip.com/)
> [gitingest-mcp](https://www.codelibrary.ai/mcp/gitingest-mcp)
> [gitinjest - AI Agent skill](https://eliteai.tools/agent-skills/gitinjest)
> [gitingest · PyPI](https://pypi.org/project/gitingest/)
> [gitingest.com Website Traffic, Ranking, Analytics [May 2026]](https://www.semrush.com/website/gitingest.com/overview/)
> [Gitingest-MCP](https://himcp.ai/server/gitingest-mcp)
> [gitingest.com Tráfico Web, Ranking, Analítica [abril de 2026]](https://es.semrush.com/website/gitingest.com/overview/)
> [narumiruna/gitingest-mcp — MCP Servers · Directories by Enterprise DNA](https://enterprisedna.co/directories/mcp/narumiruna-gitingest-mcp/)
> [Jupyterhub source code compiled by https://gitingest.com/ · GitHub](https://gist.github.com/ptdtan/359cc88c3c73e5143aefa8d530dda3b8)
> [GitIngest](https://www.aitoolnet.com/gitingest)
>
> **13件のウェブサイトを検索しました**
>
> 
>
> **ネットワーク環境のプロキシ設定確認**
>
>
> **確認したネットワークのプロキシ設定とポート経由**
>
> 
>
> **GitHubでのファイルダウンロード方法を検索**
>
> [RepoMapr - Visualize GitHub Repositories](https://www.repomapr.com/)
> [Agenda-based Narrative Extraction: Steering Pathfinding Algorithms with Large Language Models](https://arxiv.org/abs/2603.29661)
> [GitGPS — Navigate Any GitHub Codebase Instantly](https://gitgps.com/)
> [narrativegraphs · PyPI](https://pypi.org/project/narrativegraphs/)
> [GitHub - knaidoo29/mistree: A python package for constructing and analysing minimum spanning trees. · GitHub](https://github.com/knaidoo29/mistree)
> [Navigator | Narrative](https://docs.narrativetools.io/pro/navigator/)
>
> **6件のウェブサイトを検索しました**
>
> 
>
> **DNS接続のテスト**
>
>
> **テストしたDNS接続**
>
> 
>
> **Searching for SoftwareX code archives and repositories**
>
> [Supplementary Materials: Semantic Interaction for Narrative Map Sensemaking | Zenodo](https://zenodo.org/records/18930805)
> [(PDF) Semantic Interaction for Narrative Map Sensemaking: An Insight-based Evaluation](https://www.researchgate.net/publication/403379832_Semantic_Interaction_for_Narrative_Map_Sensemaking_An_Insight-based_Evaluation)
> [ナラティブマップ可視化ツール(NMVT):ナラティブマップフレームワークに基づく対話型ナラティブ分析システム〖JST機械翻訳〗 | 文献情報 | J-GLOBAL 科学技術総合リンクセンター](https://jglobal.jst.go.jp/public/202602227091061478)
> [Design guidelines for narrative maps in sensemaking tasks](https://ouci.dntb.gov.ua/en/works/9JpaGwD4/)
> [components.mesh.eco — the Mutual Component Library](https://components.mesh.eco/)
> [Quantum Kernels for Narrative Coherence: An Application to Path Optimization in Document Graphs for Storyline Extraction](https://www.mdpi.com/2227-7390/14/10/1734)
> [dblp: Brian Keith](https://dblp.org/pid/214/2392)
> [RecallNest MCP server — install in Claude, Cursor & VS Code — Unyly](https://unyly.org/mcp/recallnest)
> [Qyvaria OS Download — All Release Files + Repository Files + Wikipedia](https://qyvaria1ai.neocities.org/)
> [Hu Xiuhan](https://huxiuhan.com/)
> [itilme.com — Portfolio + Knowledgebase](https://www.itilme.com/)
> [SendToSelf Archive](https://sendtoself.dev/)
> [jnthnlstr -](https://jnthnlstr.tiddlyspot.com/)
>
> **13件のウェブサイトを検索しました**
>
> 
>
> **Testing GitHub IP connectivity via curl**
>
>
> **Tested GitHub IP connectivity via curl**
>
> 
>
> **GitHubコードリポジトリ検索とアクセス**
>
> [GitHub - FlashML-org/flashlib: Fast and memory-efficient classical machine learning operators · GitHub](https://github.com/FlashML-org/flashlib)
> [Linear Programming Solving problem using constraints · GitHub](https://gist.github.com/97harsh/e49f3d3415644bd645d561aa845463a8)
>
> **2件のウェブサイトを検索しました**
>
> 
>
> **Reading PDF skill**
>
>
> **読んでPDFをレンダリングしページを分析した**
>
> 
>
> **画像サイズの確認とクロップ**
>
>
> **確認し画像をクロップした**
>
> 
>
> **Pythonツールで画像処理**
>
>
> 35m 28s考えました

# 全体像

添付論文 **“Narrative Maps: An Algorithmic Approach to Represent and Extract Information Narratives”** は、時系列文書群から「複数の相互接続したストーリーライン」を抽出し、重み付き有向非巡回グラフとして表現する方法を提案しています。中心的な貢献は、ナラティブ理論とグラフ理論の対応付け、coherence 最大化と topic coverage を組み合わせた線形計画、そして main route と representative landmarks の抽出です。`2009.04508v2.pdf`

一方、指定された GitHub の `main` ブランチは、2020年論文のコードをそのまま保存したものではなく、**Narrative Maps Visualization Tool 2.1.0** という対話型システムへ発展した現行版です。2020年論文が基本表現と抽出アルゴリズムを扱うのに対し、現行版は Dash/Cytoscape によるUI、semantic interaction、entity-based connection、SHAPによる説明などを追加しています。([github.com](https://github.com/briankeithn/narrative-maps?utm_source=chatgpt.com), [researchgate.net](https://www.researchgate.net/publication/397596644_Narrative_Maps_Visualization_Tool_NMVT_An_interactive_narrative_analytics_system_based_on_the_narrative_maps_framework?utm_source=chatgpt.com))

確認した範囲は、GitHub `main` のディレクトリ構成、README、TUTORIAL、依存関係、SoftwareX掲載時の固定リポジトリ、および2025年のSoftwareX論文です。GitHub側のキャッシュ制約により巨大な `NMVT.py` 本体を行単位で取得できなかったため、以下では、

- **2020年論文部分**：論文の図2・数式から再構成した clean-room 実装仕様
- **現行GitHub部分**：著者のREADME、TUTORIAL、SoftwareX論文に明記された実装仕様

として区別します。内部関数名や未公開の細かな定数は推測していません。

---

# 1. Narrative Map の計算表現

## 1.1 ナラティブ理論とグラフ理論の対応

論文では、ナラティブを単なる時系列リストではなく、**interrelated stories の system** と定義します。対応関係は次のようになります。

| ナラティブ理論 | グラフ表現 | Route map metaphor | 実装上の意味 |
|---|---|---|---|
| Event | node | landmark | ニュース記事などの時点付きイベント |
| Discourse element | node attribute | landmark attribute | 媒体、URL、信頼性、バイアス等 |
| Starting event | source | starting place | 入次数0の開始ノード |
| Ending event | sink | ending place | 出次数0の終了ノード |
| Story | path / chain | route | 時系列に沿うイベント列 |
| Coherence | edge weight | route evaluation metric | 2イベントを接続する自然さ |
| Constituent events | maximum-likelihood chain | main route | 中心ストーリーのイベント |
| Supplementary events | maximum antichain | representative landmarks | 並行ストーリーを横断的に代表するイベント |
| Narrative | weighted st-DAG | route map | 単一source・単一sinkを持つDAG |

この対応は論文の表1で明示されています。特に、イベントはノード、ストーリーはパス、ナラティブ全体は複数パスを含む重み付き st-graph です。`2009.04508v2.pdf`

形式的には、

$$
G=(D,E)
$$

を、単一source $s$ と単一sink $t$ を持つ重み付きDAGとします。各 $s$-$t$ path が一つの storyline であり、edge weight は coherence に基づく遷移確率です。`2009.04508v2.pdf`

---

# 2. 論文のエンド・ツー・エンド処理

論文の図2は、入力から可視化までを次の6段階に分けています。

1. 文書取得・前処理  
2. coherence 計算  
3. 線形計画による st-graph 抽出  
4. main route 抽出  
5. representative landmarks 抽出  
6. GraphVizによる可視化  

`2009.04508v2.pdf`

実装上は、以下の中間生成物を明示的に保存すると再現性を確保できます。

```text
events
  ↓
embedding_matrix
  ↓
angular_similarity_matrix
topic_membership_matrix
topic_similarity_matrix
  ↓
coherence_matrix
  ↓
LP node flows / edge flows / cluster coverage
  ↓
normalized weighted DAG
  ↓
main route
representative landmarks
  ↓
SVG / HTML / JSON
```

---

# 3. Phase 1：データと前処理

## 3.1 論文忠実版の入力

2020年論文ではニュース記事の**見出し**を一つのイベントとして扱います。理由は、ニュース記事の inverted pyramid structure により、見出しに主要イベント情報が集約されているという前提です。入力には少なくとも次の列が必要です。`2009.04508v2.pdf`

```text
event_id
headline
publication_date
url
publication / source
```

実装では次を強制すべきです。

```python
df = (
    df.assign(publication_date=pd.to_datetime(df["publication_date"], utc=True))
      .sort_values(["publication_date", "event_id"], kind="stable")
      .reset_index(drop=True)
)

df["time_index"] = np.arange(len(df))
```

重要なのは、グラフの時間順序が日付そのものではなく、最終的には **row index $i<j$** で決まることです。同一時刻の記事がある場合、元IDなどによる決定論的な tie-break が必要です。

## 3.2 開始・終了イベント

論文の一般定義ではユーザーが $s,t$ を選択します。ただし、事例研究では期間内の最初の記事と最後の記事を使用しています。

再現実装では、最も安全なのは次の処理です。

```python
df = df.loc[
    (df["publication_date"] >= start_date)
    & (df["publication_date"] <= end_date)
].copy()

s = 0
t = len(df) - 1
```

任意の中間ノードを $s,t$ として使用する場合は、論文図2の制約だけでは source に流入したり sink から流出したりする可能性があるため、

$$
\sum_i x_{is}=0,\qquad
\sum_j x_{tj}=0
$$

を明示するか、$s,t$ の時刻区間だけにデータを切る必要があります。

---

# 4. Phase 2：Coherence の計算

論文では、2イベント間の coherence を次の2要素で構成します。

1. **event similarity**：文書埋め込みの意味的近さ  
2. **topic similarity**：クラスタ所属確率分布の近さ  

片方だけが高い接続を避けるため、最終値は算術平均ではなく幾何平均です。`2009.04508v2.pdf` `2009.04508v2.pdf`

---

## 4.1 Event similarity：angular similarity

論文は Universal Sentence Encoder v4.0 により、各見出しを512次元ベクトルに変換しています。

$$
e_i\in\mathbb{R}^{512}
$$

angular similarity は、

$$
A_{ij}
=
1-
\frac{
\arccos
\left(
\frac{e_i^\top e_j}
{\|e_i\|\|e_j\|}
\right)
}{\pi}
$$

です。

$$
A_{ij}\in[0,1]
$$

となり、

- 同方向：1
- 直交：0.5
- 反対方向：0

です。

2020年論文本文では式が省略されていますが、著者の後続研究では上式が明示されています。([MDPI](https://www.mdpi.com/1099-4300/28/7/732?utm_source=chatgpt.com))

実装は次のようになります。

```python
from __future__ import annotations

import numpy as np

def angular_similarity_matrix(embeddings: np.ndarray) -> np.ndarray:
    """埋め込み行列から pairwise angular similarity を計算する。"""
    x = np.asarray(embeddings, dtype=np.float64)

    if x.ndim != 2:
        raise ValueError("embeddings must be a 2D array")

    norms = np.linalg.norm(x, axis=1, keepdims=True)
    if np.any(norms <= 0):
        raise ValueError("zero-norm embedding was found")

    unit = x / norms
    cosine = np.clip(unit @ unit.T, -1.0, 1.0)

    angular = 1.0 - np.arccos(cosine) / np.pi
    np.fill_diagonal(angular, 1.0)

    return np.clip(angular, 0.0, 1.0)
```

---

## 4.2 Topic extraction：UMAP + HDBSCAN

埋め込みをそのままHDBSCANへ渡すと、高次元空間で距離が不安定になり、計算コストも大きくなります。そのため論文では、

$$
e_i
\overset{\mathrm{UMAP}}{\longrightarrow}
z_i
\overset{\mathrm{HDBSCAN}}{\longrightarrow}
p_i
$$

という処理を採用しています。

ここで、

$$
p_i=(p_{i1},\ldots,p_{ic})
$$

はイベント $i$ の各クラスタへの soft membership vector です。HDBSCANを使うため、クラスタ数 $c$ は事前指定されません。`2009.04508v2.pdf`

再現用の基本形は次です。

```python
import hdbscan
import umap

reducer = umap.UMAP(
    n_neighbors=15,
    n_components=5,
    min_dist=0.0,
    metric="cosine",
    random_state=42,
)

projected = reducer.fit_transform(embeddings)

clusterer = hdbscan.HDBSCAN(
    min_cluster_size=5,
    min_samples=None,
    metric="euclidean",
    cluster_selection_method="eom",
    prediction_data=True,
)

labels = clusterer.fit_predict(projected)

topic_prob = hdbscan.all_points_membership_vectors(clusterer)
```

ただし、上のUMAP/HDBSCANパラメータは**決定論的な初期実装例**であり、論文には完全な設定値が記載されていません。したがって、論文図5と同一のグラフを得るには、元コードのパラメータと元データスナップショットが必要です。

### Noise point の処理

HDBSCANの soft membership は、外れ値について行和が1未満になることがあります。論文図2は membership vector の和を1として描いているため、論文忠実な実装では少なくとも正の行について再正規化するのが自然です。

```python
row_sum = topic_prob.sum(axis=1, keepdims=True)

positive = row_sum[:, 0] > 0
topic_prob[positive] /= row_sum[positive]
```

全要素0のイベントは、次のいずれかを明示的に選ぶ必要があります。

- noise cluster を追加する
- coverage計算から除外する
- event similarity のみで接続する

これは論文だけでは一意に決まらない実装上の未特定部分です。

---

## 4.3 Topic similarity：Jensen-Shannon similarity

イベント $i,j$ の topic membership distribution を $p_i,p_j$ とすると、

$$
T_{ij}
=
1-d_{\mathrm{JS}}(p_i,p_j)
$$

とします。

ここで $d_{\mathrm{JS}}$ は Jensen-Shannon **distance**、すなわちJensen-Shannon divergenceの平方根です。著者の2026年の定式化では、この distance を使うことが明示されています。([MDPI](https://www.mdpi.com/1099-4300/28/7/732?utm_source=chatgpt.com))

SciPy の `jensenshannon` は divergence ではなく、その平方根である distance を返します。したがって**二乗してはいけません**。([SciPyドキュメント](https://docs.scipy.org/doc/scipy-1.5.1/reference/generated/scipy.spatial.distance.jensenshannon.html?utm_source=chatgpt.com))

```python
from scipy.spatial.distance import jensenshannon

def topic_similarity_matrix(topic_prob: np.ndarray) -> np.ndarray:
    """HDBSCAN soft-membership 分布の JS similarity を計算する。"""
    p = np.asarray(topic_prob, dtype=np.float64)

    if p.ndim != 2:
        raise ValueError("topic_prob must be a 2D array")
    if np.any(p < 0):
        raise ValueError("topic probabilities must be non-negative")

    row_sum = p.sum(axis=1, keepdims=True)
    if np.any(row_sum <= 0):
        raise ValueError("zero-sum probability vector was found")

    p = p / row_sum

    n = len(p)
    similarity = np.eye(n, dtype=np.float64)

    for i in range(n):
        for j in range(i + 1, n):
            js_distance = float(
                jensenshannon(p[i], p[j], base=2.0)
            )
            value = 1.0 - js_distance
            similarity[i, j] = value
            similarity[j, i] = value

    return np.clip(similarity, 0.0, 1.0)
```

---

## 4.4 Composite coherence

最終的な coherence は、

$$
C_{ij}
=
\sqrt{A_{ij}T_{ij}}
$$

です。

```python
angular = angular_similarity_matrix(embeddings)
topical = topic_similarity_matrix(topic_prob)

coherence = np.sqrt(
    np.clip(angular * topical, 0.0, 1.0)
)
```

幾何平均を使うことで、一方がゼロなら coherence もゼロになります。また、一方だけ高い組合せに対して算術平均より強いペナルティがかかります。論文はこれを、event similarity と topic similarity の両方を満たす厳格な接続条件として説明しています。`2009.04508v2.pdf`

---

# 5. Phase 3：線形計画による Narrative Map 抽出

## 5.1 変数

イベントを時刻順に $i=0,\ldots,n-1$ とします。

| 記号 | 意味 |
|---|---|
| $y_i$ | `node-active_i`：ノード $i$ を通るフロー |
| $x_{ij}$ | `next-node_{i,j}`：$i\to j$ を通るフロー |
| $\gamma$ | `minedge`：目的関数で最大化する bottleneck |
| $a_k$ | `cluster-active_k`：クラスタ $k$ の coverage |
| $K$ | 期待ストーリー長 |
| `mincover` | 平均topic coverageの下限 |

論文図2には変数domainが明示されていません。しかし、

$$
0\le y_i\le1,\qquad
0\le x_{ij}\le1
$$

の**連続変数**と解釈する必要があります。

理由は、これらをbinaryにすると各active nodeの入辺・出辺が一本に限定され、解が一本のpathにしかならず、論文が示す分岐・合流を含む st-graph と矛盾するためです。現行システムが $K$ を “expected main storyline length” と説明していることも、このcontinuous-flow解釈と整合します。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

---

## 5.2 目的関数

論文図2の目的関数は、

$$
\max \gamma
$$

です。

coherenceとの関係は、

$$
\gamma
\le
1-x_{ij}+C_{ij}
\qquad \forall i<j
$$

です。

- $x_{ij}=1$ なら  
  $$
  \gamma\le C_{ij}
  $$
- $x_{ij}=0$ なら  
  $$
  \gamma\le1+C_{ij}
  $$
  となり、通常は拘束しません。
- $0<x_{ij}<1$ のside flowは、低coherenceでもある程度許容されます。

したがって厳密には、これは「全ての正の辺の最低coherence」を直接最大化するのではなく、**高いフローを持つ辺ほど強く coherence を要求する flow-relaxed maximin formulation** です。

著者の説明上は「ストーリーは最も弱いlinkによって評価される」という weakest-link objective です。`2009.04508v2.pdf`

---

## 5.3 時間・構造制約

論文図2の構造制約は次のとおりです。

### 開始・終了ノード

$$
y_s=1,\qquad y_t=1
$$

### 時間逆行の禁止

$$
x_{ij}=0
\qquad
\forall i\ge j
$$

実装では、最初から $i<j$ の辺だけ生成すれば同値です。

### Active node を飛び越す辺の制限

$$
x_{ij}
\le
1-y_k
\qquad
\forall i<k<j
$$

中間ノード $k$ に1単位のフローが通っている場合、

$$
y_k=1
\Rightarrow
x_{ij}=0
$$

となり、$k$ を飛び越す辺は作れません。

### Flow conservation

$$
\sum_i x_{ij}=y_j
\qquad
\forall j\ne s
$$

$$
\sum_j x_{ij}=y_i
\qquad
\forall i\ne t
$$

各中間ノードについて、

$$
\text{incoming flow}
=
\text{outgoing flow}
=
y_i
$$

となります。

### 期待長

$$
\sum_i y_i=K
$$

$$
\sum_{i,j}x_{ij}=K-1
$$

図2の線形計画は、論文13ページにまとめて示されています。`2009.04508v2.pdf`

---

## 5.4 $K$ は「選択ノード数」ではなく期待path長

この点は実装上非常に重要です。

一単位の $s$-$t$ flow は、DAG上のpath flowへ、

$$
x
=
\sum_{p\in\mathcal P}
\lambda_p x^{(p)},
\qquad
\sum_p\lambda_p=1
$$

と分解できます。

すると、

$$
y_i
=
\sum_{p:i\in p}\lambda_p
$$

であり、

$$
\sum_i y_i
=
\sum_p\lambda_p |p|
=
K
$$

です。

したがって $K$ は、path mixtureからランダムにpathを選んだときの**期待ノード数**です。グラフ全体に現れるノード数が $K$ に限定されるわけではありません。

これは、事例研究で $K$ が比較的小さくても、最終グラフに20以上のイベントが現れ得る理由です。現行UIも $K$ を期待main-story lengthとして説明しています。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

---

# 6. Topic coverage 制約

## 6.1 Edge membership

イベント $i,j$ のクラスタ $k$ へのmembershipを、

$$
p_{ik},\quad p_{jk}
$$

とすると、辺のmembershipは、

$$
m_{ijk}
=
\sqrt{p_{ik}p_{jk}}
$$

です。

例えば、

$$
p_{ik}=0.95,\quad p_{jk}=0.90
$$

なら、

$$
m_{ijk}
=
\sqrt{0.95\times0.90}
\approx0.925
$$

です。

片方だけがクラスタ $k$ に属する場合は小さくなり、どちらかがゼロなら辺membershipもゼロになります。論文の図3はこの処理を具体例で示しています。`2009.04508v2.pdf`

## 6.2 Cluster coverage

クラスタ $k$ のcoverageを、

$$
a_k
=
\sum_{i,j}
x_{ij}m_{ijk}
$$

とします。

制約は、

$$
\sum_{k=1}^{c}a_k
\ge
c\cdot \mathrm{mincover}
$$

すなわち、

$$
\frac{1}{c}
\sum_{k=1}^{c}a_k
\ge
\mathrm{mincover}
$$

です。

ここで注意すべきなのは、

$$
a_k\ge\mathrm{mincover}
\quad\forall k
$$

ではない点です。したがって、各クラスタに個別の最低coverageを強制するのではなく、**クラスタ全体に対する平均coverage制約**です。

また、ノード単体ではなく辺を使うため、あるtopicの記事が一つ孤立しているだけでは、そのtopicを十分にcoverしたとは扱われません。論文は「coherent sequence内にtopicが現れること」をcoverageと解釈しています。`2009.04508v2.pdf` `2009.04508v2.pdf`

論文では予備的な定性評価に基づき、80%程度のminimum coverageがうまく機能したとしています。一方、現行チュートリアルのCOVID例は `mincover=20`、すなわち20%を出発点にしています。モデル、データ、後処理が更新されているため、両者を同じ推奨値として扱うべきではありません。`2009.04508v2.pdf` ([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

---

# 7. PuLPによるコア線形計画の再現例

以下は論文図2をほぼ直接実装したものです。開始・終了イベントはデータの先頭・末尾に切り出してある前提です。

```python
from __future__ import annotations

from typing import Any

import numpy as np
import pulp

def solve_narrative_map_lp(
    coherence: np.ndarray,
    topic_prob: np.ndarray,
    K: float,
    mincover: float,
    *,
    solver: Any | None = None,
) -> dict[str, Any]:
    """
    論文 Figure 2 の continuous-flow LP。

    Parameters
    ----------
    coherence:
        shape=(n, n), [0, 1] の pairwise coherence.
    topic_prob:
        shape=(n, c), 各イベントの soft cluster membership.
    K:
        expected path length. 通常 2 <= K <= n.
    mincover:
        fraction表記。20%なら0.20。
    solver:
        PuLP solver. NoneならCBC。

    Notes
    -----
    events は時刻順に並び、source=0, sink=n-1 とする。
    """
    C = np.asarray(coherence, dtype=np.float64)
    P = np.asarray(topic_prob, dtype=np.float64)

    if C.ndim != 2 or C.shape[0] != C.shape[1]:
        raise ValueError("coherence must be square")

    n = C.shape[0]
    if P.ndim != 2 or P.shape[0] != n:
        raise ValueError("topic_prob has incompatible shape")

    if not 2 <= K <= n:
        raise ValueError("K must satisfy 2 <= K <= n")

    if not 0.0 <= mincover <= 1.0:
        raise ValueError("mincover must be expressed as a fraction")

    s = 0
    t = n - 1
    n_clusters = P.shape[1]

    # i < j の時系列順辺のみを候補にする
    edges = [
        (i, j)
        for i in range(n)
        for j in range(i + 1, n)
    ]

    model = pulp.LpProblem(
        name="narrative_map",
        sense=pulp.LpMaximize,
    )

    node_active = {
        i: pulp.LpVariable(
            f"node_active_{i}",
            lowBound=0.0,
            upBound=1.0,
            cat="Continuous",
        )
        for i in range(n)
    }

    next_node = {
        (i, j): pulp.LpVariable(
            f"next_node_{i}_{j}",
            lowBound=0.0,
            upBound=1.0,
            cat="Continuous",
        )
        for i, j in edges
    }

    minedge = pulp.LpVariable(
        "minedge",
        lowBound=0.0,
        upBound=1.0,
        cat="Continuous",
    )

    # Objective
    model += minedge

    # Coherence / weakest-link relaxation
    for i, j in edges:
        model += (
            minedge
            <= 1.0 - next_node[i, j] + float(C[i, j])
        )

    # Source and sink
    model += node_active[s] == 1.0
    model += node_active[t] == 1.0

    # An edge may not skip a fully active intermediate node
    for i, j in edges:
        for k in range(i + 1, j):
            model += (
                next_node[i, j]
                <= 1.0 - node_active[k]
            )

    # Expected number of nodes and edges
    model += pulp.lpSum(node_active.values()) == float(K)
    model += pulp.lpSum(next_node.values()) == float(K - 1.0)

    # Incoming flow equals node activity
    for j in range(n):
        if j == s:
            continue

        incoming = pulp.lpSum(
            next_node[i, j]
            for i in range(j)
        )
        model += incoming == node_active[j]

    # Outgoing flow equals node activity
    for i in range(n):
        if i == t:
            continue

        outgoing = pulp.lpSum(
            next_node[i, j]
            for j in range(i + 1, n)
        )
        model += outgoing == node_active[i]

    # Topic coverage
    cluster_coverage_expr = []

    for k in range(n_clusters):
        coverage_k = pulp.lpSum(
            next_node[i, j]
            * float(np.sqrt(P[i, k] * P[j, k]))
            for i, j in edges
        )
        cluster_coverage_expr.append(coverage_k)

    model += (
        pulp.lpSum(cluster_coverage_expr)
        >= float(n_clusters * mincover)
    )

    if solver is None:
        solver = pulp.PULP_CBC_CMD(msg=False)

    model.solve(solver)

    status = pulp.LpStatus[model.status]
    if status != "Optimal":
        raise RuntimeError(f"LP status: {status}")

    node_flow = {
        i: float(pulp.value(node_active[i]))
        for i in range(n)
    }

    edge_flow = {
        (i, j): float(pulp.value(next_node[i, j]))
        for i, j in edges
    }

    cluster_coverage = np.array(
        [
            float(pulp.value(expr))
            for expr in cluster_coverage_expr
        ],
        dtype=np.float64,
    )

    return {
        "status": status,
        "objective_minedge": float(pulp.value(minedge)),
        "node_flow": node_flow,
        "edge_flow": edge_flow,
        "cluster_coverage": cluster_coverage,
        "average_cluster_coverage": float(
            cluster_coverage.mean()
        ),
    }
```

## 実装時の数値上の注意

`edge_flow` には solver tolerance による微小値が残る可能性があります。グラフ生成では、

```python
EDGE_EPS = 1e-7
```

などの閾値を明示し、閾値を変えたときのノード数・辺数の感応度も確認すべきです。

---

# 8. Phase 4：Main route の抽出

LPで得られる $x_{ij}$ はflowであり、まだ条件付き遷移確率ではありません。論文では、各ノードの出辺を正規化します。

$$
p_{ij}
=
\frac{x_{ij}}
{\sum_\ell x_{i\ell}}
$$

その上で、path $c$ の確率を、

$$
P(c)
=
\prod_{(i,j)\in c}p_{ij}
$$

と定義します。

main route は、

$$
c^*
=
\arg\max_{c:s\rightsquigarrow t}
\prod_{(i,j)\in c}p_{ij}
$$

です。

数値計算では、

$$
-\log P(c)
=
\sum_{(i,j)\in c}
-\log p_{ij}
$$

を最小化すればよいため、shortest-path problemになります。`2009.04508v2.pdf`

```python
import math
import networkx as nx

def build_normalized_graph(
    edge_flow: dict[tuple[int, int], float],
    *,
    eps: float = 1e-7,
) -> nx.DiGraph:
    graph = nx.DiGraph()

    positive = {
        edge: value
        for edge, value in edge_flow.items()
        if value > eps
    }

    outgoing_total: dict[int, float] = {}
    for (i, _), value in positive.items():
        outgoing_total[i] = outgoing_total.get(i, 0.0) + value

    for (i, j), flow in positive.items():
        probability = flow / outgoing_total[i]

        graph.add_edge(
            i,
            j,
            flow=flow,
            probability=probability,
            neg_log_probability=-math.log(
                max(probability, 1e-15)
            ),
        )

    return graph

def extract_main_route(
    graph: nx.DiGraph,
    source: int,
    sink: int,
) -> list[int]:
    return nx.shortest_path(
        graph,
        source=source,
        target=sink,
        weight="neg_log_probability",
    )
```

論文上の constituent events は、このmain routeに含まれるイベントです。

---

# 9. Phase 5：Representative landmarks と最大反鎖

## 9.1 Antichain

DAGのantichainとは、互いにpathで到達できないノードの集合です。

$$
A\subseteq V
$$

について、任意の異なる $u,v\in A$ が、

$$
u\not\rightsquigarrow v,
\qquad
v\not\rightsquigarrow u
$$

を満たします。

maximum antichain は要素数が最大のantichainです。

Dilworthの定理により、

$$
\text{maximum antichain size}
=
\text{minimum chain decomposition size}
$$

です。したがって、最大反鎖の各ノードを、並行する各storylineの代表点と解釈できます。`2009.04508v2.pdf`

論文は NetworkX により全antichainを列挙し、

1. 最大要素数のものだけを残す  
2. 複数ある場合はtimestampが最も早いものを選ぶ  

という処理を行っています。`2009.04508v2.pdf`

```python
def first_maximum_antichain(
    graph: nx.DiGraph,
    timestamps: dict[int, pd.Timestamp],
) -> list[int]:
    antichains = list(nx.antichains(graph))

    if not antichains:
        return []

    maximum_size = max(len(a) for a in antichains)

    candidates = [
        list(a)
        for a in antichains
        if len(a) == maximum_size
    ]

    # 「最も早い」の実装を曖昧にしないため、
    # 時刻ベクトルの辞書式順序で決める
    return min(
        candidates,
        key=lambda nodes: tuple(
            sorted(timestamps[node] for node in nodes)
        ),
    )
```

ただし、`nx.antichains` による全列挙は最悪の場合指数時間です。論文再現ではこれを使うべきですが、大規模化する場合は maximum matching に基づく maximum-antichain algorithm に置き換える方が合理的です。

---

# 10. Phase 6：可視化

2020年論文では GraphViz と DOT を使ってSVGを生成し、HTML/CSSに埋め込んでいます。可視化ルールは概ね次です。

- 時間は上から下
- main route は青い破線
- representative landmarks は緑
- source / sink は専用アイコン
- edge weight は数値ではなく線幅
- ノードには日付、見出し、媒体ロゴ
- interactionは基本的なHTMLレベル

数値weightを明示しなかった理由は、予備評価で利用者を混乱させたためです。`2009.04508v2.pdf`

17ページの図5は、main route、side routes、representative landmarks、edge widthを一枚のmap上で表した完成形です。`2009.04508v2.pdf`

---

# 11. GitHub `main` の著者実装

## 11.1 リポジトリ構成

現行リポジトリの主要構成は次のとおりです。

```text
NMVT.py                  # アプリケーションのエントリポイント
packages/                # 内部ロジック
file_system_backend/     # キャッシュ・永続化関連
preprocessing/           # 独自データとembeddingの準備
data/                    # サンプルデータ
static/                  # 静的UI素材
assets/                  # Dash用assets
tutorial/                # 操作例
requirements.txt
Dockerfile
docker-compose.yml
```

READMEでは、`NMVT.py` が Narrative Maps Visualization Tool 本体で、`preprocessing` にカスタムデータ準備、`tutorial` に操作例があると説明されています。現行版は古い依存関係を更新し、過去版に含まれていた実験コードを整理した2.1.0リリースです。([GitHub](https://github.com/briankeithn/narrative-maps))

## 11.2 現行システムの5コンポーネント

SoftwareX論文では、現行NMVTを次の5部分に分けています。

1. Data Processing Module  
2. Narrative Extraction Engine  
3. Interactive Visualization Interface  
4. Semantic Interaction Layer  
5. Explainable AI Components  

データ処理はpandasとSentence Transformers、抽出はUMAP/HDBSCANとPuLP、表示はDash/Cytoscape、semantic interactionは3MSI、説明はSHAPに基づいています。([ResearchGate](https://www.researchgate.net/publication/397596644_Narrative_Maps_Visualization_Tool_NMVT_An_interactive_narrative_analytics_system_based_on_the_narrative_maps_framework?utm_source=chatgpt.com))

---

## 11.3 現行版の入力CSV

GitHub版のカスタムデータには次の列が必要です。

```text
id
title
url
date
publication
full_text
embedding
```

`id` は0始まりで時間順、行順も時間昇順である必要があります。embeddingには `all-MiniLM-L6-v2` が推奨されています。([GitHub](https://github.com/briankeithn/narrative-maps))

したがって2020年版との違いは、

- 2020：headlineをUSE v4で内部変換
- 現行：full textとprecomputed embeddingを受け取るdocument-level design

です。

CSV内のembeddingが文字列の場合は、`eval` ではなく `ast.literal_eval` で読み取るべきです。

```python
import ast

df["embedding"] = df["embedding"].map(
    lambda value: np.asarray(
        ast.literal_eval(value),
        dtype=np.float32,
    )
)
```

---

# 12. 論文版と現行GitHub版の重要な差

| 項目 | 2020年論文 | GitHub main 2.1.0 |
|---|---|---|
| 基本単位 | ニュース見出し | full textを含む文書 |
| 埋め込み | USE v4、512次元 | precomputed embedding、MiniLM推奨 |
| start/end | 単一source・単一sinkが必須 | なし、startのみ、start+endを選択可能 |
| 基本coherence | angular × topic の幾何平均 | semantic、topical、entity connectionを扱う |
| 時間距離 | DAG順序だけ | optional temporal penalty |
| representative | earliest maximum antichain | 各storylineのembedding centroid |
| structural importance | 明示なし | node degree によるhub |
| UI | GraphViz SVG + HTML | Dash + Cytoscape |
| 人間の修正 | なし | event/edge/clusterの追加・削除 |
| XAI | なし | SHAPによるkeyword contribution |
| 出力 | 静的map | PNG、JSON、対話型map |
| $K$ | LP上のexpected length | UI上もexpected main story length |
| coverage例 | 80% | COVID tutorialでは20% |

現行チュートリアルとSoftwareX論文に基づく差分です。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

---

# 13. Representative landmark の意味は現行版で変わっている

これは最も注意すべき変更です。

## 2020年論文

代表イベントは、

$$
\text{earliest maximum antichain}
$$

です。

これはグラフ構造に基づく**横断的な代表点**です。複数のstorylineを横切る「breadth」を表します。

## 現行GitHub

赤枠の representative event は、各storylineに含まれるイベントのembedding centroidに最も近いイベントです。

$$
r_\ell
=
\arg\min_{i\in S_\ell}
\|e_i-\bar e_\ell\|
$$

ここで、

$$
\bar e_\ell
=
\frac{1}{|S_\ell|}
\sum_{i\in S_\ell}e_i
$$

です。

青枠の structurally important event は node degree に基づくhubです。現行チュートリアルはこの二種類を明確に区別しています。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

したがって、**論文を再現する場合はcentroidを使わずmaximum antichainを使う必要があります**。反対に現行UIを再現するなら、antichainではなくstoryline centroidとdegree centralityを実装します。

---

# 14. 現行版のSemantic Interaction

現行NMVTは、ユーザー操作を単なる表示変更ではなく、再最適化時の制約へ変換します。

## Remove Event

対象イベントを採用しないように、coherenceまたはnode activityをゼロに固定する制約を追加します。

$$
y_i=0
$$

あるいは、対象イベントに接続する候補edgeのcoherenceを0にします。

チュートリアルでは「zero coherenceを強制するstructure-space constraint」と説明されています。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

## Add / Remove Edge

選択辺について、

$$
x_{ij}\ge\varepsilon
$$

または、

$$
x_{ij}=0
$$

といった構造制約を追加します。

## Add to Cluster

イベントを指定topicへ割り当てる操作は、

1. semi-supervised UMAPでprojection spaceを変更  
2. cluster membershipを変更  
3. 対象イベントが弱く接続されるようstructure-space制約を追加  

という二層の修正です。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

## 正則化とstrict mode

現行UIには、

- edge weightに対するL1 regularization
- startから到達不能なcomponentを除くstrict mode
- common entity term
- temporal-distance penalty

があります。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

これらは2020年論文の基本LPにはありません。

---

# 15. 現行版のExplainable AI

現行版では、edge typeを次のように表示します。

- Similarity
- Topical
- Entity

edgeを選択すると、normalized coherence と、接続に寄与したkeywordをSHAPで表示します。また、未接続の2イベントについても比較できます。([GitHub](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md))

これは「なぜ接続されたか」を説明するものであり、因果関係を証明するものではありません。semantic similarity、topic membership、entity overlapの寄与を説明する機能です。

---

# 16. 論文の事例研究

論文はCOVID-19 archiveから、

- 1,576記事
- 373媒体
- 2020年1月から3月

を取得しています。

1月分は607記事で、探索空間を減らすため、記事数上位5媒体に限定し102記事としました。開始・終了イベントは時系列の最初と最後です。最適化後、固定した開始・終了に加えて22イベントが残りました。`2009.04508v2.pdf`

main routeからは主に、

- virus spread
- economic impacts

が抽出されました。

representative landmarksはこれに加えて、

- government containment measures
- scientific information
- criticism of official responses

を表しました。`2009.04508v2.pdf`

ただし、Wuhanでの生活のような一部のthemeは、main routeとrepresentative landmarksだけでは抽象化されて失われています。したがって、この二つはmap全体の完全な要約ではなく、

- main route：longitudinal / depth
- antichain landmarks：transverse / breadth

を提供するものです。`2009.04508v2.pdf` `2009.04508v2.pdf`

---

# 17. ユーザー評価の解釈

評価は、

- Representation
- Metaphor
- Visualization

の3次元、合計22項目の5段階Likert scaleです。Cronbach’s alphaは0.9560でした。`2009.04508v2.pdf`

1月、2月、3月の各mapについて20名の有効回答を集め、全般にcoherence、relevance、metaphorの自然さ、main routeとlandmarkの有用性は肯定的に評価されています。`2009.04508v2.pdf`

ただし、この評価が示すのは主に、

- 表現が理解しやすいか
- map metaphorが自然か
- visual interfaceが役立つか

であり、抽出されたgraphが「唯一正しいナラティブ」であることを検証したものではありません。

また、

- COVIDのみ
- 各map 20名
- headlinesのみ
- 既知テーマで事前知識が多い

という制約があります。著者自身もheadlineに加えてleadやfull textを使う余地、異なるtopicとより多人数での評価が必要だと述べています。`2009.04508v2.pdf`

---

# 18. 再現性と計算量

論文は、主要な計算量を概ね、

$$
O(n^2c)
$$

としています。ここで $n$ は文書数、$c$ はクラスタ数です。主要なボトルネックはpairwise event combinationです。`2009.04508v2.pdf`

より細かく見ると、

- embedding similarity：$O(n^2d)$
- topic similarity：$O(n^2c)$
- edge-cluster membership storage：$O(n^2c)$
- `i<k<j` の素朴なskip constraint生成：最悪 $O(n^3)$
- 全antichain列挙：最悪指数時間

です。

現行SoftwareX版では、著者のテスト環境で500文書程度までreasonable interactivityを維持し、新規データの初回処理は類似度行列・entity情報の計算に時間がかかる一方、キャッシュ後は大幅に短縮されると報告されています。1,000文書も理論上処理可能ですが、500を超えると対話性能が低下するとされています。これは研究用prototypeの実測範囲であり、本番性能保証ではありません。([ResearchGate](https://www.researchgate.net/publication/397596644_Narrative_Maps_Visualization_Tool_NMVT_An_interactive_narrative_analytics_system_based_on_the_narrative_maps_framework?utm_source=chatgpt.com))

---

# 19. 再現実装で必須となる診断

各段階で次を出力すべきです。

## Embedding / clustering

```text
embedding shape
embedding norm distribution
UMAP projection
HDBSCAN cluster count
noise ratio
cluster size
membership row sums
membership entropy
```

## Coherence

```text
angular similarity distribution
topic similarity distribution
composite coherence distribution
top / bottom coherent pairs
coherence vs time-distance scatter
```

## LP

```text
solver status
objective minedge
sum(node_active) - K
sum(edge_flow) - (K - 1)
flow conservation residual
average cluster coverage
coverage by cluster
number of positive-flow nodes and edges
```

## Graph

```text
nx.is_directed_acyclic_graph(G)
source in-degree == 0
sink out-degree == 0
nx.has_path(source, sink)
all edge timestamps increase
outgoing probabilities sum to 1
```

## Narrative summaries

```text
main route node count
main route probability
main route minimum coherence
main route average coherence
maximum antichain width
representative landmark timestamps
```

## Parameter sensitivity

少なくとも、

$$
K\in\{4,6,8,10,12\}
$$

と、

$$
\mathrm{mincover}
\in\{0.1,0.2,0.4,0.6,0.8\}
$$

のgridを評価し、

- feasibility
- main-route coherence
- graph size
- number of storylines
- cluster coverage
- solution stability

を比較すべきです。

---

# 20. 再現対象ごとの推奨仕様

## A. 2020年論文忠実版

次の仕様に固定します。

```text
event text:
    headline

encoder:
    Universal Sentence Encoder v4

topic model:
    UMAP + HDBSCAN soft membership

coherence:
    sqrt(angular_similarity * JS_similarity)

optimization:
    Figure 2 continuous-flow LP

endpoints:
    one fixed source and one fixed sink

main route:
    maximum-likelihood path after outgoing normalization

representative landmarks:
    earliest maximum antichain

visualization:
    GraphViz / DOT / SVG
```

この仕様なら、論文で提案されたアルゴリズムを再現できます。ただし、UMAP/HDBSCANの全設定、noise処理、antichain tie-break、元データの厳密なスナップショットが論文だけでは完全に特定できないため、**図5とbitwiseに同じgraphが得られる保証はありません**。

## B. 現行GitHub 2.1.0互換版

次を追加または変更します。

```text
event text:
    full_text + display title

encoder:
    precomputed all-MiniLM-L6-v2 embeddings

endpoints:
    none / start only / start and end

connection types:
    similarity / topical / entity

optional terms:
    common-entity emphasis
    temporal-distance penalty

representative events:
    storyline embedding centroid

structural events:
    degree-based hubs

semantic interaction:
    add/remove node
    add/remove edge
    add to cluster with semi-supervised UMAP

regularization:
    optional L1 edge regularization

post-processing:
    optional strict mode

XAI:
    SHAP keyword contributions

interface:
    Dash + Cytoscape + GraphViz layout
```

GitHub版を動作させるだけであれば、著者READMEではPython 3.12とGraphVizを前提とし、`NMVT.py`をエントリポイントとしています。主要ライブラリはPuLP、NetworkX、UMAP、HDBSCAN、Dash/Cytoscape、SHAPです。([GitHub](https://github.com/briankeithn/narrative-maps))

---

# 21. 実装上の最終的な判断

論文の本質は、単なる「記事クラスタリング」ではありません。

$$
\boxed{
\text{semantic coherence}
+
\text{topic coverage}
+
\text{chronological unit flow}
}
$$

を同時に満たす fractional-flow DAG を構築し、その後、

$$
\boxed{
\text{main route}
=
\text{maximum-likelihood longitudinal view}
}
$$

と、

$$
\boxed{
\text{representative landmarks}
=
\text{maximum-antichain transverse view}
}
$$

を抽出することにあります。

また、GitHub現行版は、この2020年の静的アルゴリズムをそのまま表示したものではなく、

$$
\text{algorithmic extraction}
\rightarrow
\text{interactive visual analytics}
\rightarrow
\text{human-guided re-optimization}
$$

へ拡張したシステムです。論文再現とGitHub再現を混在させると、特に representative event、endpoint constraints、coherence terms、`mincover` の解釈がずれるため、実装では両モードを明示的に分離する必要があります。

---

**Sources:**

- [An Information-Geometric Justification for Composite Coherence in Event-Based Narrative Extraction](https://www.mdpi.com/1099-4300/28/7/732?utm_source=chatgpt.com)
- [scipy.spatial.distance.jensenshannon — SciPy v1.5.1 Reference Guide](https://docs.scipy.org/doc/scipy-1.5.1/reference/generated/scipy.spatial.distance.jensenshannon.html?utm_source=chatgpt.com)
- [https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md](https://github.com/briankeithn/narrative-maps/blob/main/tutorial/TUTORIAL.md)
- [(PDF) Narrative Maps Visualization Tool (NMVT): An interactive narrative analytics system based on the narrative maps framework](https://www.researchgate.net/publication/397596644_Narrative_Maps_Visualization_Tool_NMVT_An_interactive_narrative_analytics_system_based_on_the_narrative_maps_framework?utm_source=chatgpt.com)

## Response:
2026/7/27 22:59:55

> **Web検索で確認中**
>
> ユーザーの要求に答えるために、GitHubの実装を最新の情報で確認し、関連する論文やリポジトリを調べる必要があります。
>
> **確認した**
>
> 
>
> **GitHub実装の検証と適用可能性調査**
>
> [GitHub - misbahsy/MindMapper: A NextJS based app that takes a user prompt, or a YouTube url, or a Website URL, and generates a beautiful Mindmap. · GitHub](https://github.com/misbahsy/MindMapper)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **GitHubリポジトリをクローン**
>
>
> **クローンしたGitHubリポジトリ**
>
> 
>
> **ウェブを検索中**
>
> [raw.githubusercontent.com](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/NMVT.py)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **適用性の検討**
>
> ユーザーの要求に基づき、紙の方法とGitHubの現在の実装を比較し、コーパスの範囲やナラティブの数による調整が必要であることを明確にする必要があります。
>
> **検討した適用性**
>
> 
>
> **ウェブを検索中**
>
> [raw.githubusercontent.com](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **適用性の確認**
>
> この手法は応用可能ですが、注意点があります。元々の論文は複数の独立したナラティブを抽出することを前提にしていないため、時間範囲やナラティブに応じて調整が必要です。
>
> **適用性と推奨手法**
>
> この方法は、スコープが明確な問題であれば適用可能です。ただし、広範なニュースや不特定のナラティブには、前処理やナラティブの家族のクラスター化が必要です。
>
> **確認した適用性と推奨手法**
>
> 
>
> **ウェブを検索中**
>
> [raw.githubusercontent.com](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> 9m 34s考えました

# 結論

**適用可能です。ただし、そのまま一度だけ全記事へ適用するのではなく、**

$$
\boxed{
\text{独立したNarrative候補の発見}
\rightarrow
\text{候補ごとのNarrative Map構築}
\rightarrow
\text{経路分解}
\rightarrow
\text{関連記事の紐付け}
}
$$

という二段階以上の構成にするのが妥当です。

重要なのは、次の二つを区別することです。

1. **一つのNarrative Map内に含まれる複数のstoryline（経路）の数が不明**
2. **記事集合全体に含まれる独立したNarrative Map自体の数が不明**

前者については論文・GitHubの方法を利用できます。後者については、論文・GitHubとも直接には解いていないため、Narrative Mapの前段に「Narrative familyの発見」を追加する必要があります。

論文の正式な問題設定は、「ある特定のissueに関する時刻付き文書集合」と開始イベント $s$、終了イベント $t$ が与えられたとき、両者を複数のpathで接続する一つのNarrative Mapを求めるものです。したがって、任意期間の一般ニュース全体から独立Narrative数を同時推定するモデルではありません。`2009.04508v2.pdf`

---

# 1. 「Narrative数が不明」の意味を分解する

論文・GitHubでは、以下の三つの数を混同しないことが重要です。

| 概念 | 意味 | 数の決まり方 |
|---|---|---|
| topic cluster | 経済、政策、企業業績などの話題 | HDBSCANがデータから推定 |
| storyline / route | 時系列に沿った記事経路 | Narrative Map上のpathとして出現 |
| narrative family / map | 相互に関連するstorylineのまとまり | 論文・GitHubの前提では原則として事前にスコープされる |

論文では、eventをnode、storylineをpath、Narrative全体を複数pathからなるst-DAGとして表現します。したがって、**一つのNarrativeは一つのpathではなく、相互に分岐・合流する複数のstorylineを含むgraph**です。`2009.04508v2.pdf`

HDBSCANはクラスタ数を事前指定せず、記事のtopic membershipを推定します。しかし、このクラスタ数はNarrative数でもstoryline数でもなく、LPのcoverage制約で利用される「topic数」です。`2009.04508v2.pdf`

したがって、ユーザーのいう「Narrative数」が以下のどちらかで結論が変わります。

- **同じニュースイベント内の経路数が不明**  
  → 論文・GitHubを適用可能
- **期間内に何個の独立したニュースイベントがあるか不明**  
  → 事前のNarrative family discoveryが必要

---

# 2. 適用可能性の判定

| 記事集合の状態 | 直接適用 | 判断 |
|---|---:|---|
| すでに一つのissueに絞られている | 可能 | 論文方式をほぼそのまま利用可能 |
| 企業・テーマ・政策など一つの検索条件で収集されている | 条件付きで可能 | 複数の独立episodeが混在していないか確認が必要 |
| 任意期間の一般ニュース全体 | 非推奨 | 前段でNarrative familyを分離する必要がある |
| 開始・終了イベントが既知 | 可能 | 論文方式に最も整合的 |
| 開始・終了イベントが不明 | 可能だが要拡張 | 仮想source/sinkまたは複数start/endを用いる |
| 全関連記事まで取得したい | 追加処理が必要 | Mapに採用されなかった記事を事後的に経路へ紐付ける |

原論文では単一source・単一sinkを持つst-DAGを採用し、すべてのstorylineをsourceからsinkへのpathとして定義します。`2009.04508v2.pdf`

一方、現行GitHub実装は、start nodeとend nodeを空にすることができ、その場合は入出力フローを等式ではなく不等式制約に変更します。このためendpointなしでも実行できますが、理論上の「一つのst-graphとしてのNarrative」よりも、複数成分を含む探索的graphに近くなります。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

---

# 3. 現行GitHubが返す「storyline数」はNarrative数ではない

現行GitHubの `graph_stories` は、概ね次の処理です。

1. graph上で最も尤もらしいpathを一つ抽出
2. そのpathに含まれるnodeをすべてgraphから削除
3. 残ったgraphから次のpathを抽出
4. edgeがなくなれば残りをsingleton storylineとして返す

実際の実装でも、maximum-likelihood chainを取得した後、そのchainの全nodeを削除して再帰しています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/graph_utils.py))

したがって、GitHubが表示するstoryline数は、

- 潜在的なNarrative数の統計推定値ではない
- node-disjointな貪欲path decompositionの結果
- graphの閾値やLPパラメータに依存
- singletonもstorylineとして数え得る
- 共通の開始部分や合流部分を持つ経路を別々に表しにくい

という性質があります。

UIで全体像を把握するには有用ですが、**研究・分析目的で「Narrativeが何本存在するか」を推定する方法としては、そのまま採用しない方がよい**です。

---

# 4. 推奨する全体アーキテクチャ

## Stage 0：記事をevent表現に変換する

ユーザーのDataFrameは、最低限次の形に正規化します。

```text
article_id
date
headline
body
event_text
embedding
```

### event_text

本文全体を単純連結するより、次のいずれかが適切です。

$$
\text{event\_text}_i
=
\text{headline}_i
+
\text{leadまたは本文要約}_i
$$

または、headlineと本文を別々に埋め込み、

$$
e_i
=
\operatorname{normalize}
\left(
w_h e_i^{\mathrm{headline}}
+
(1-w_h)e_i^{\mathrm{body}}
\right)
$$

とします。

原論文はheadlineを一つのeventとして扱っています。一方、著者自身が、headlineだけでは情報が不足することがあり、full text、適切なsummary、またはheadlineとleadの組み合わせを利用する余地があると述べています。`2009.04508v2.pdf` `2009.04508v2.pdf`

### 本文全体をそのまま使う際の問題

長い本文には、

- 主要イベント
- 過去経緯
- 複数人物の発言
- 一般的な背景説明
- 別の関連イベント

が混在します。そのため本文埋め込みは「同じ一般テーマの記事」を近づける一方、「どのイベントがどのイベントへ進展したか」という経路を弱める可能性があります。

実務的には、まず以下を推奨します。

```text
event_text = headline + 本文冒頭3～5文
```

その後、より高度な仕様では本文から5W1Hまたはevent summaryを抽出します。一記事に明確に複数イベントが含まれる場合は、一記事一nodeではなく、一記事から複数event nodeを生成する方がNarrative Mapの理論に整合します。

---

## Stage 1：独立したNarrative familyを発見する

これは原論文への追加処理です。

### 4.1 記事間の候補edge

記事 $i$ から、それより後の記事 $j$ への親和度を、例えば次で定義します。

$$
A_{ij}
=
\mathbf 1(t_i<t_j)
\left(S_{ij}^{\mathrm{sem}}\right)^{\alpha}
\left(S_{ij}^{\mathrm{entity}}+\varepsilon\right)^{\beta}
\exp\left(-\frac{t_j-t_i}{\tau}\right)
$$

ここで、

- $S^{\mathrm{sem}}$：embeddingの意味類似度
- $S^{\mathrm{entity}}$：人物、企業、国、商品などのentity overlap
- $\tau$：ニュースNarrativeの時間スケール

です。

全記事対全記事の完全graphを作るのではなく、各記事から、

- 時間的に後方
- 一定期間内
- semantic similarity上位 $k$ 件

だけを候補edgeとします。

### 4.2 Narrative familyの分離

候補graphを基に、

- weakly connected components
- community detection
- HDBSCANによるsoft clustering
- 時系列制約付きクラスタリング

のいずれかで、独立Narrative候補を作ります。

ここで得られるfamily数を、

$$
M=\text{number of candidate narrative families}
$$

とします。

ただし、単一のHDBSCAN結果をそのまま真のNarrative数と解釈するのではなく、以下の安定性を確認すべきです。

- embedding modelを変えてもfamilyが維持されるか
- HDBSCANの `min_cluster_size` を変えても維持されるか
- 対象期間を少し前後させても維持されるか
- random seedを変えても構成記事が維持されるか
- family内のentityと時間的連続性が十分か

記事は複数のNarrativeに関係することがあるため、hard assignmentではなく、

$$
P(m\mid i)
$$

を保持し、一定以上のmembershipを持つ複数familyへ記事を重複所属させる仕様が自然です。

---

# 5. Stage 2：各Narrative familyにNarrative Mapを適用する

family $m$ に属する記事だけを取り出し、その中で論文の処理を行います。

## 5.1 Topic clustering

記事embeddingにUMAPとHDBSCANを適用し、

$$
p_i=(p_{i1},\dots,p_{ic})
$$

というtopic membership vectorを推定します。クラスタ数 $c$ はHDBSCANによって自動的に決まるため、事前にtopic数を設定する必要はありません。`2009.04508v2.pdf`

ただし、これはNarrative familyを分離するStage 1とは別です。

- Stage 1のcluster：独立Narrative候補
- Stage 2のcluster：一つのNarrative内のtopic coverage

という役割分担にします。

## 5.2 Coherence

論文準拠のcoherenceは、

$$
C_{ij}
=
\sqrt{
S_{ij}^{\mathrm{event}}
S_{ij}^{\mathrm{topic}}
}
$$

です。

- $S^{\mathrm{event}}$：angular similarity
- $S^{\mathrm{topic}}$：Jensen-Shannon similarity

幾何平均を使うため、一方が低い記事対には強いペナルティがかかります。`2009.04508v2.pdf`

実務拡張として、時間とentityを追加できます。

$$
\widetilde C_{ij}
=
C_{ij}
\exp\left(-\frac{t_j-t_i}{\tau_m}\right)
\left(1+\eta E_{ij}\right)
$$

ただし、最終値は $[0,1]$ に正規化します。

---

# 6. GitHub実装をそのまま利用する場合の注意点

## 6.1 Semantic similarityがUMAP後の表現で計算される

現行コードは、事前計算されたembedding $X$ にUMAPを適用して `clusterable_embedding` を作り、その低次元表現上でangular similarityを計算しています。HDBSCANも同じUMAP表現に適用されています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

これは可視化・クラスタリングには合理的ですが、Narrative edgeのsemantic similarityまでUMAP空間で計算すると、UMAPによる局所構造の強調や距離変形がそのままcoherenceへ入ります。

推奨仕様は、

- semantic similarity：元の高次元embedding
- topic clustering：UMAP後のembedding
- visualization：2次元UMAP

と分離することです。

## 6.2 Entity similarityはtitleだけから取得される

現行コードのentity tableは、`article["title"]` にNERを適用して作られています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

本文を保持している場合は、

```text
headline + lead
```

またはevent summaryからentityを抽出した方がよいでしょう。

日本語記事の場合は、GitHub実装が前提とする英語NERやembeddingをそのまま使わず、日本語または多言語対応のsentence embeddingとNERへ置き換える必要があります。

## 6.3 Temporal termの扱い

現行コードは時間距離から、

$$
\exp(-\Delta t/\sigma_t)
$$

を計算していますが、確認した `create_LP` のcoherence式自体はsemantic similarity、cluster similarity、entity multiplier、relevance multiplierから構成されています。時間情報は主に `3\sigma_t` の候補edge windowとして反映されています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

時間的に近い記事を単に候補へ残すだけでなく、時間差をcoherenceに直接反映したい場合は、上記の $\widetilde C_{ij}$ のように目的関数側へ明示的に入れるべきです。

---

# 7. 開始・終了イベントが不明な場合

原論文はsourceとsinkをユーザーが選ぶことを前提とします。`2009.04508v2.pdf`

任意期間の記事では、期間の最初の記事がNarrativeの真の開始イベントとは限らず、最後の記事が終端とも限りません。そのため、次の順で推奨します。

## 推奨1：仮想source・仮想sink

Narrative familyごとに仮想node $s^\ast,t^\ast$ を追加します。

$$
s^\ast\to
\{\text{初期期間の候補記事}\}
$$

$$
\{\text{終盤期間の候補記事}\}
\to t^\ast
$$

仮想sourceからの接続先は、例えば、

- 時系列上の初期20%の記事
- entity centralityが高い記事
- 後続記事へのsemantic connectivityが高い記事

から選びます。

仮想sinkも同様に終盤記事から選びます。これにより、一つのst-DAGとしての理論構造を維持しながら、実質的なstart/endをモデルに選択させられます。

## 推奨2：複数start・複数end

現行GitHubは複数のstart node、end nodeを受け取る構造を持ちます。各start/endに均等な初期・終端flowを割り当てます。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

## 探索用：start/endなし

現行GitHubのendpointなしモードも利用できます。ただし、

- 一つのNarrative Mapというより複数componentの探索
- globalな $K$ を複数componentで共有
- singletonが発生しやすい
- storylines数の解釈が不安定

となるため、最終的な経路推定には仮想source/sink方式の方が適切です。

---

# 8. Narrative経路の抽出方法

## 8.1 Main route

論文のmain routeは、正規化されたedge probabilityについて、

$$
r^\ast
=
\arg\max_{r:s\rightsquigarrow t}
\prod_{(i,j)\in r}p_{ij}
$$

を満たすpathです。

実装上は、

$$
-\log p_{ij}
$$

をedge costとしてshortest pathを求めます。`2009.04508v2.pdf`

これはそのまま採用できます。

## 8.2 Alternative routes

論文は、すべてのpathを列挙する代わりに、

- main route
- maximum antichainによるrepresentative landmarks

を抽出します。maximum antichainはNarrativeの横断的な代表点を与えますが、各storylineの記事列を明示的に返すものではありません。`2009.04508v2.pdf`

ユーザーの目的は「経路と関連記事の抽出」であるため、alternative routeも明示的に抽出する必要があります。

### 推奨：LP flowのpath decomposition

LPで得られたedge flowを $x_{ij}$ とします。

1. 正のflowを持つsource-sink pathを一本取得
2. そのpathのweightを  
   $$
   w_r=\min_{(i,j)\in r}x_{ij}
   $$
   とする
3. path上の各edgeから $w_r$ を減算
4. 正のsource-sink flowがなくなるまで繰り返す

すると、

$$
x
=
\sum_{r=1}^{R}w_r x^{(r)}
$$

というweighted path decompositionを得られます。

この方法には、

- LPが実際に配分したflowと整合
- 分岐前の共通pathや合流後のpathを保持可能
- routeごとのweightが得られる
- 貪欲node削除よりNarrative Mapの構造に忠実

という利点があります。

### route数の決定

route数 $R$ は事前指定せず、以下で絞ります。

$$
\frac{\sum_{r=1}^{R^\ast}w_r}
{\sum_{r=1}^{R}w_r}
\ge q
$$

となる最小の $R^\ast$ を採用します。

例えば、累積flow mass、minimum path probability、minimum coherence、既存routeとの重複率を組み合わせます。具体的には、

- 累積flow mass
- path内の最低coherence
- route内の記事数
- main routeとのJaccard overlap
- 他routeに対する追加topic coverage

を用いて選択します。

これは論文にはない拡張ですが、経路数を固定せずに主要routeを抽出するという目的に最も整合します。

---

# 9. 「関連記事」は別工程で紐付ける必要がある

Narrative MapのLPは、すべての記事をMapへ残すわけではありません。

論文の事例では102記事から、固定したstart/endに加えて22記事だけが最適解に採用されました。残りの記事は、追加するとcoherenceが低下するためMapから除外されています。`2009.04508v2.pdf`

したがって、次の二種類を分けます。

- **core article**：LPによってNarrative Mapに選択された記事
- **related article**：Mapには選択されなかったが、特定routeと関連する記事

route $r$ に対する未選択記事 $d$ の関連度は、例えば次で定義できます。

$$
R(d,r)
=
\max_{v\in r}
\left[
\alpha S_{\mathrm{sem}}(d,v)
+
\beta S_{\mathrm{topic}}(d,v)
+
\gamma S_{\mathrm{entity}}(d,v)
\right]
\exp\left(
-\frac{|t_d-t_v|}{\tau}
\right)
$$

または、最大値だけでなくroute内上位3記事との平均類似度を使います。

関連記事は排他的に一つのrouteへ割り当てる必要はありません。例えば、ある金融政策記事が、

- inflation route
- bond-market route
- currency route

のすべてに関係することがあります。そのため、multi-label assignmentにします。

$$
d\in r
\quad\Longleftrightarrow\quad
R(d,r)\ge\theta_r
$$

閾値 $\theta_r$ は、少数の人手ラベルまたは類似度分布の上位分位点から決めます。

---

# 10. 推奨する出力DataFrame

## `narratives_df`

```text
narrative_id
n_articles
start_date
end_date
dominant_entities
n_topic_clusters
family_stability
```

## `routes_df`

```text
narrative_id
route_id
route_rank
route_weight
route_probability
n_core_articles
min_edge_coherence
mean_edge_coherence
topic_coverage
```

## `route_articles_df`

```text
narrative_id
route_id
article_id
date
headline
event_order
role               # main / alternative / landmark / related
core_flag
association_score
topic_membership
path_weight
```

## `edges_df`

```text
narrative_id
source_article_id
target_article_id
semantic_similarity
topic_similarity
entity_similarity
temporal_weight
coherence
lp_flow
transition_probability
```

この形で保持すれば、Narrative Mapの可視化だけでなく、

- 各経路の記事一覧
- 経路別ニュース要約
- Narrativeの出現・消滅
- route間の分岐・合流
- topicやentityの変化
- 日次のNarrative intensity

などへ拡張できます。

---

# 11. パラメータの考え方

論文で重要なパラメータは、

- $K$：期待されるmain routeまたは選択node量
- `mincover`：平均topic coverageの下限

です。これらはNarrative数ではありません。著者も、$K$、`mincover`、embedding、クラスタリング設定が最終Mapへ大きく影響すると述べています。`2009.04508v2.pdf`

現行GitHubの既定値は概ね、

```text
K = 6
mincover = 0.20
min_samples = 2
min_cluster_size = 2
n_neighbors = 2
sigma_t = 30
```

です。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

一方、原論文では対象データに対する予備的評価から80% coverageが機能したと報告しています。`2009.04508v2.pdf`

20%と80%には大きな差があるため、固定値をそのまま採用せず、

$$
K\times\mathrm{mincover}\times\tau
$$

のグリッドで、次を比較する必要があります。

- graph feasibility
- route coherence
- topic coverage
- selected article数
- route数
- singleton数
- parameter変更に対するrouteのJaccard安定性
- 重複記事率

---

# 12. 計算量への対応

原論文のLPは、記事数を $n$、topic cluster数を $c$ とすると、pairwise組み合わせが主要なボトルネックとなり、概ね

$$
O(n^2c)
$$

の変数・計算を必要とします。`2009.04508v2.pdf`

したがって、

- 数百記事程度の一つのNarrative family  
  → 比較的直接適用しやすい
- 数千・数万記事の全期間データ  
  → family分割とcandidate edgeのsparsificationが必須

です。

各記事から全未来記事へedgeを作るのではなく、

```text
時間window内
かつ
semantic nearest neighbor上位k件
かつ／または
共通entityを持つ記事
```

だけを候補にするべきです。

長期間については、全期間を一つの静的Mapにするより、重複するrolling windowでMapを作り、前後window間でNarrative familyをmatchingする方が、Narrativeの発生・分岐・消滅を扱いやすくなります。

---

# 13. 最終的な推奨仕様

採用すべき構成は、**論文準拠の最適化をコアにして、GitHubの拡張を選択的に取り込む方式**です。

## 論文から採用

- eventをnodeとするDAG
- semantic similarityとtopic similarityの幾何平均
- HDBSCANによるtopic数の自動推定
- coherence最大化＋coverage制約のLP
- maximum-likelihood main route
- maximum antichainによる横断的landmark

## GitHubから採用

- endpointを任意または複数にする機能
- entity similarity
- temporal candidate window
- regularized LP
- full-text情報の表示
- interactive graph

## 修正すべき点

- 独立Narrative family発見を前段に追加
- semantic similarityはUMAP後ではなく元embeddingで計算
- 日本語・多言語対応embeddingとNERを使用
- 時間減衰をcoherenceへ明示的に反映
- 現行の貪欲node削除によるstoryline抽出をflow decompositionへ変更
- 未選択記事をrouteへ事後的にmulti-label assignment
- $K$ やHDBSCAN cluster数をNarrative数として解釈しない

したがって適用判定は次のとおりです。

$$
\boxed{
\begin{array}{l}
\text{一つのissueに絞られた記事集合}
\Rightarrow
\text{ほぼ直接適用可能}
\\[4pt]
\text{複数の独立issueを含む任意期間の記事集合}
\Rightarrow
\text{事前分割を追加すれば適用可能}
\\[4pt]
\text{Narrative数・経路数をGitHub出力だけで推定}
\Rightarrow
\text{非推奨}
\end{array}
}
$$

最も適切なのは、**Narrative family discovery → family別LP → LP flowのpath decomposition → 全記事のroute assignment**という構成です。これにより、Narrative数を事前に固定せず、主要経路、代替経路、各経路のcore記事と関連記事を一貫した形で抽出できます。

---

**Sources:**

- [](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py)

## Response:
2026/7/27 23:21:27

> **要件定義を検討中**
>
> ユーザーがステージ1の物語家族発見の要件について尋ねています。以前の議論と添付された資料を基に、GitHubの現在の実装も確認する必要がありそうです。
>
> **検討中の要件定義を作成**
>
> 
>
> **検索してGitHubの現在の実装を確認**
>
> [GitHub - everestmz/llmcat: Render LLM-friendly maps of repositories · GitHub](https://github.com/everestmz/llmcat)
>
> **1件のウェブサイトを検索しました**
>
> 
>
> **考慮中の要件定義**
>
> ユーザーは実装要件を1つのノートブックで求めているようです。各セルに説明と入力/出力、診断を含む形で、目的はナラティブファミリーを識別することだと思われます。
>
> **考慮した要件定義**
>
> 
>
> **設計とアルゴリズムの選定**
>
> ステージ1のナラティブファミリー発見のために、HDBSCAN、グラフコミュニティ検出、ハイブリッドアプローチのようなアルゴリズムを選ぶ必要があります。基準となる方法を決定するために、各手法を比較します。
>
> **設計とアルゴリズムを選定した**
>
> 
>
> **アルゴリズムの確認**
>
> 長い時系列に対応するため、時間の影響を加味した類似度計算が必要です。HDBSCANなどの手法でセマンティックとエンティティに基づくクラスタリングを提案中です。
>
> **確認したアルゴリズム**
>
> 
>
> 13m 27s考えました

# 実装要件定義：独立した Narrative family の発見

## 1. 位置づけ

作成するノートブック名は、以下を推奨します。

```text
01_discover_narrative_families.ipynb
```

このノートブックの目的は、任意期間のニュース記事集合から、事前に個数を指定せずに、相互に独立性の高い **Narrative family候補**を抽出することです。

ここでいう `Narrative family` は原論文の正式な用語ではありません。原論文は、ある特定のissueに関する文書集合と開始・終了イベントがすでに与えられた状態で、一つのNarrative Mapを構築する問題を扱っています。Narrativeは複数storylineから構成されますが、記事集合全体にいくつの独立Narrativeが含まれるかは推定していません。`2009.04508v2.pdf` `2009.04508v2.pdf`

また、原論文でUMAP＋HDBSCANにより自動推定されるclusterは、一つのNarrative内部のtopic clusterです。これは独立Narrativeの数ではありません。`2009.04508v2.pdf`

現行GitHub実装も、渡された記事集合に対してUMAP、HDBSCAN、LPを適用する構造であり、任意の記事集合を複数の独立Narrativeへ分割する前処理は実装していません。現行コードには時間窓、entity similarity、UMAP＋HDBSCANなどの構成要素はありますが、Narrative family discoveryは本要件で追加する拡張です。これはコード構成からの推論です。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

---

# 2. 基本設計方針

Narrative familyの抽出には、単純な文書embeddingのHDBSCANだけではなく、以下の情報を統合した**疎な記事間グラフ**を構築し、そのcommunityを抽出します。

$$
\boxed{
\text{semantic similarity}
+
\text{lexical continuity}
+
\text{entity continuity}
+
\text{temporal continuity}
}
$$

実装方式は次のとおりです。

```text
記事の正規化・重複排除
        ↓
文書embedding
        ↓
k近傍による候補記事ペアの限定
        ↓
semantic・lexical・entity・time特徴量
        ↓
時間制約付きweighted graph
        ↓
複数パラメータ・複数seedでcommunity detection
        ↓
consensus partition
        ↓
primary familyとsecondary familyへのsoft assignment
        ↓
family品質・安定性診断
```

最終family数 $M$ は入力パラメータにしません。weighted graphのcommunity構造から決定します。

---

# 3. Narrative familyの操作的定義

本ノートブックでは、Narrative familyを以下の条件を満たす記事集合として定義します。

1. 記事間の意味的類似性が高い  
2. 同一または関連する出来事・entity・語彙が継続している  
3. 時間的に大きく断絶していない  
4. family内部の記事間affinityがfamily外部より高い  
5. 後続のNarrative Mapで時系列経路を構成できる程度の内部接続性がある  

V1では、長期間に反復する抽象的なthemeではなく、**時間的に連続したevent episode**をfamilyとして抽出します。

例えば「金融政策」という数年間継続するtheme全体ではなく、

```text
日銀の政策修正観測
→ 政策決定会合
→ 利上げ決定
→ 市場反応
```

のようなepisode単位をfamilyとします。

設定値は以下で固定します。

```python
FAMILY_GRANULARITY = "episode"
```

---

# 4. スコープ

## 4.1 実装対象

ノートブックは以下を実装します。

- 入力DataFrameの検証
- 日付・本文・headlineの正規化
- exact duplicateとnear duplicateの除去
- headline＋本文冒頭によるevent text作成
- multilingual document embedding
- sparse k-nearest-neighbor graph
- semantic similarity
- lexical similarity
- optional entity similarity
- temporal attenuation
- weighted graph community detection
- parameter・seedを変えたconsensus clustering
- family数の自動決定
- primary／secondary family membership
- family品質診断
- representative article抽出
- Stage 2へ渡せるDataFrame生成
- Notebook上での表・図の表示
- 必要最小限のParquet／NPZ出力

## 4.2 実装対象外

以下は本ノートブックには含めません。

- Narrative MapのLP最適化
- source／sinkの最終決定
- main route抽出
- alternative route抽出
- maximum antichain
- LLM APIによるfamily要約
- リアルタイムまたはオンライン更新
- supervised classifierの学習
- 因果関係の推定

本ノートブックはStage 2の入力を生成するところまでとします。

---

# 5. 入力データ要件

## 5.1 必須列

入力は以下のDataFrameです。

```python
articles_df
```

必須列は次の3列です。

| 列 | 型 | 内容 |
|---|---|---|
| `date` | datetime変換可能 | 記事公開日時 |
| `headline` | string | 記事headline |
| `body` | string | 記事本文 |

## 5.2 任意列

以下は存在すれば利用します。

| 列 | 用途 |
|---|---|
| `article_id` | 記事固有ID |
| `source` | 媒体名 |
| `url` | 記事URL |
| `language` | 言語 |
| `entities` | 事前抽出済みentityのlist |
| `category` | 元データのカテゴリ |
| `author` | 著者 |
| `timestamp_updated` | 更新日時 |

`article_id` がない場合は、次の情報からSHA-256ベースのIDを生成します。

```text
date + normalized_headline + normalized_body_prefix
```

## 5.3 日付処理

日付は次の順序で処理します。

```python
pd.to_datetime(df["date"], errors="coerce", utc=True)
```

処理後に、

```text
date
article_id
original_row_number
```

でstable sortし、以下を生成します。

```text
event_order = 0, 1, ..., N-1
```

同一日時の記事についても、`article_id`による決定論的な順序を付与します。

## 5.4 不正行

以下に該当する行は、黙って削除せず `rejected_rows_df` に格納します。

- 日付をparseできない
- headlineが欠損
- headlineとbodyの双方が空
- article_idが重複し、内容も不整合
- event textが最低文字数未満

```text
original_row_number
article_id
rejection_reason
```

を保持します。

---

# 6. Event textの作成

原論文はheadlineをevent単位としています。一方、論文自身もheadlineだけでなくfull text、summary、またはheadline＋leadを利用する拡張を挙げています。`2009.04508v2.pdf` `2009.04508v2.pdf`

本実装では、以下を標準仕様とします。

```text
event_text
    = normalized headline
    + separator
    + bodyの冒頭5文
```

本文全体をそのままembeddingすると、背景説明、過去経緯、引用、複数イベントが混在しやすいため、デフォルトではlead相当部分だけを使用します。

```python
TEXT_MODE = "headline_plus_lead"
LEAD_SENTENCES = 5
MAX_BODY_CHARS = 4_000
MIN_EVENT_TEXT_CHARS = 20
```

処理内容は以下です。

- Unicode NFKC正規化
- HTMLタグ除去
- 制御文字除去
- 連続空白の圧縮
- headline末尾とbody先頭の重複除去
- 最大文字数で切り詰め
- 元headlineとbodyは別途保持

出力列は次のとおりです。

```text
normalized_headline
normalized_body
lead_text
event_text
event_text_hash
```

---

# 7. 重複記事の処理

重複記事をそのままclusterへ入れると、同一配信記事や転載記事だけで一つのfamilyが形成される可能性があります。そのため、community detectionの前に重複排除を行います。

## 7.1 Exact duplicate

以下のいずれかが一致する記事をexact duplicateとします。

- 正規化headlineと本文prefixが完全一致
- URLが完全一致
- `event_text_hash` が一致

## 7.2 Near duplicate

候補記事ペアについて、

$$
\cos(e_i,e_j)\ge\theta_{\mathrm{dup}}
$$

かつ、

$$
|t_i-t_j|\le d_{\mathrm{dup}}
$$

の場合、near duplicate候補とします。

推奨初期値は以下です。

```python
NEAR_DUPLICATE_COSINE = 0.985
NEAR_DUPLICATE_MAX_DAYS = 3
```

near duplicate graphのconnected componentごとに `duplicate_group_id` を付与します。

canonical articleは次の優先順位で決定します。

1. 公開日時が早い  
2. 本文が長い  
3. source、URLが取得できている  
4. article_idが辞書順で小さい  

community detectionにはcanonical articleだけを使用します。

重複記事は最終出力でcanonical articleのfamily assignmentを継承します。

---

# 8. 文書embedding

## 8.1 標準仕様

Sentence Transformersを使用し、CPU、CUDA、MPSを自動判定します。`SentenceTransformer.encode()` はbatch処理とembedding正規化をサポートするため、`normalize_embeddings=True` を使用します。モデル名、revision、cache directoryは設定可能にします。([SentenceTransformers](https://www.sbert.net/docs/package_reference/sentence_transformer/SentenceTransformer.html))

推奨初期設定は次のとおりです。

```python
EMBEDDING_MODEL = (
    "sentence-transformers/"
    "paraphrase-multilingual-mpnet-base-v2"
)

EMBEDDING_MODEL_REVISION = None
EMBEDDING_BATCH_SIZE = 64
NORMALIZE_EMBEDDINGS = True
EMBEDDING_DTYPE = "float32"
```

モデルは設定セルから差し替え可能にします。

## 8.2 UMAPの利用範囲

本実装では、元の高次元embeddingをsemantic similarityに使用します。

UMAPは以下に限定します。

- 2次元可視化
- optional HDBSCAN baseline
- cluster構造の補助診断

現行GitHubコードはUMAP後のembeddingをsemantic similarityにも利用していますが、本要件ではembedding空間と可視化空間を分離します。現行コードではUMAP projection後にangular similarityとHDBSCANが計算されています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

## 8.3 Embedding cache

再実行時にembeddingを再計算しないよう、以下を含むfingerprintを生成します。

```text
article_id
event_text_hash
embedding model
model revision
text preprocessing parameters
```

保存形式は以下です。

```text
data/interim/
    narrative_family_embeddings_<fingerprint>.npz
```

cacheのfingerprintが一致しない場合は自動的に再計算します。

---

# 9. Candidate pairの生成

すべての記事対を計算すると、

$$
O(N^2)
$$

となるため、候補記事ペアをk近傍に限定します。原論文もpairwise combinationが主要な計算ボトルネックであると述べています。`2009.04508v2.pdf`

## 9.1 k-nearest neighbors

L2正規化embeddingに対してcosine距離によるk近傍を取得します。

```python
KNN_K = 30
KNN_METRIC = "cosine"
```

データ量に応じてbackendを切り替えます。

```text
Nが小さい:
    sklearn.neighbors.NearestNeighbors

Nが大きい:
    pynndescent.NNDescent
```

`NearestNeighbors` は各点のk近傍indexとdistanceを返し、疎なkNN graphも生成できます。([scikit-learn](https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.NearestNeighbors.html))

## 9.2 時間窓

記事 $i,j$ の日付差を、

$$
\Delta t_{ij}=|t_j-t_i|
$$

とします。

候補ペアは、

$$
\Delta t_{ij}\le W
$$

を満たすものに限定します。

推奨初期値は、

```python
TAU_DAYS = 30
MAX_GAP_DAYS = 3 * TAU_DAYS
```

です。

現行GitHub実装も、時間機能を有効化した場合に概ね $3\sigma_t$ を候補時間窓として使用しています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

ただし、この30日は論文の普遍的な推奨値ではなく、データに合わせて感応度分析する初期値です。

## 9.3 Candidate rule

記事ペア $(i,j)$ は以下のいずれかを満たす場合に候補とします。

```text
jがiのkNN
または
iがjのkNN
または
強いentity overlapがある
```

そのうえで、

```text
event_order_i != event_order_j
delta_days <= MAX_GAP_DAYS
```

を要求します。

---

# 10. 記事間特徴量

候補記事ペアだけについて、以下を計算します。

## 10.1 Semantic similarity

原論文に合わせてangular similarityを使用します。

$$
S_{ij}^{\mathrm{sem}}
=
1-
\frac{
\arccos\left(
\operatorname{clip}(e_i^\top e_j,-1,1)
\right)
}{\pi}
$$

$$
S_{ij}^{\mathrm{sem}}\in[0,1]
$$

embeddingはL2正規化済みとします。

## 10.2 Lexical similarity

日本語・英語の混在に対して形態素解析を必須にしないため、文字n-gram TF-IDFを使用します。

```python
TfidfVectorizer(
    analyzer="char_wb",
    ngram_range=(3, 5),
    min_df=2,
    max_df=0.98,
    sublinear_tf=True,
    norm="l2",
)
```

記事 $i,j$ のTF-IDF vectorを $v_i,v_j$ として、

$$
S_{ij}^{\mathrm{lex}}
=
v_i^\top v_j
$$

とします。

## 10.3 Entity similarity

entity列が入力に存在する場合、またはNER backendが設定されている場合にだけ使用します。

標準V1ではNERモデルを必須依存にせず、以下の優先順位とします。

```text
1. 入力DataFrameのentities列
2. optional NER backend
3. entity similarityを無効化
```

entity similarityはIDF-weighted Jaccardとします。

$$
S_{ij}^{\mathrm{ent}}
=
\frac{
\sum_{u\in E_i\cap E_j}\operatorname{idf}(u)
}{
\sum_{u\in E_i\cup E_j}\operatorname{idf}(u)
}
$$

entityが利用できない場合は、entity weightをsemantic／lexicalへ再配分します。

現行GitHubコードはtitleからentityを抽出し、entity overlapをcoherence multiplierとして利用しています。([GitHub](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py))

## 10.4 Temporal attenuation

$$
S_{ij}^{\mathrm{time}}
=
\exp\left(
-\frac{\Delta t_{ij}}{\tau}
\right)
$$

時間は類似性を作る要素ではなく、古い記事間接続を弱める要素として使用します。

## 10.5 Content score

$$
C_{ij}
=
\frac{
w_s S_{ij}^{\mathrm{sem}}
+
w_l S_{ij}^{\mathrm{lex}}
+
w_e S_{ij}^{\mathrm{ent}}
}{
w_s+w_l+w_e
}
$$

推奨初期値は以下です。

```python
SEMANTIC_WEIGHT = 0.80
LEXICAL_WEIGHT = 0.20
ENTITY_WEIGHT = 0.00
```

`entities`が有効な場合は、例えば次の設定を使用します。

```python
SEMANTIC_WEIGHT = 0.70
LEXICAL_WEIGHT = 0.20
ENTITY_WEIGHT = 0.10
```

## 10.6 Final affinity

時間が記事接続を単独で作らないよう、次の乗算型attenuationを使用します。

$$
A_{ij}
=
C_{ij}
\left[
(1-\lambda_t)
+
\lambda_t S_{ij}^{\mathrm{time}}
\right]
$$

```python
TEMPORAL_STRENGTH = 0.35
```

したがって、

$$
A_{ij}\in[0,1]
$$

となります。

---

# 11. Edge sparsification

すべてのkNN edgeを残すと、一般的な表現を含む記事が複数familyをつなぐbridgeになりやすいため、edgeをさらに限定します。

## 11.1 Mutual／strong edge rule

候補ペア全体のaffinity分布から、

$$
q_{\mathrm{low}},
q_{\mathrm{high}}
$$

を計算します。

edgeは次のいずれかを満たす場合に採用します。

$$
\text{mutual-kNN}
\land
A_{ij}\ge q_{\mathrm{low}}
$$

または、

$$
A_{ij}\ge q_{\mathrm{high}}
$$

推奨初期値は次です。

```python
EDGE_LOW_QUANTILE = 0.55
EDGE_HIGH_QUANTILE = 0.80
MAX_EDGES_PER_NODE = 15
```

絶対閾値だけに依存せず、データ内のaffinity分布に基づく相対閾値を使用します。

## 11.2 二種類のgraph

以下の2種類を生成します。

### Temporal directed graph

```text
G_temporal: nx.DiGraph
```

日付順に、

$$
i\rightarrow j
\quad\Longleftrightarrow\quad
event\_order_i<event\_order_j
$$

とします。

これはStage 2の候補edgeおよび時間連続性診断に使用します。

### Community graph

```text
G_family: nx.Graph
```

family抽出は対称的な所属関係なので、undirected weighted graphを使用します。

edge weightは、

$$
w_{ij}=A_{ij}
$$

です。

---

# 12. Community detection

## 12.1 標準方式

NetworkXのweighted Louvain community detectionを使用します。

Louvainはcommunity数を引数として要求せず、weighted graphのmodularityを用いてpartitionを返します。`resolution` が1未満なら大きいcommunity、1より大きければ小さいcommunityを形成しやすくなります。また、node処理順序のランダム性が結果に影響し得るため、複数seedを使用します。([NetworkX](https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.community.louvain.louvain_communities.html))

```python
nx.community.louvain_communities(
    G_family,
    weight="weight",
    resolution=resolution,
    seed=seed,
)
```

## 12.2 単一partitionを最終結果にしない

Louvainの一回の出力をそのまま最終familyとしません。

次の組合せで複数回実行します。

```python
RESOLUTION_GRID = (0.70, 1.00, 1.30)
EDGE_QUANTILE_GRID = (0.55, 0.65, 0.75)
COMMUNITY_SEEDS = (17, 42, 97)
```

合計27 runを標準とします。

各runについて以下を保存します。

```text
run_id
resolution
edge_quantile
seed
n_families
n_noise
largest_family_share
modularity
mean_internal_affinity
mean_external_affinity
```

## 12.3 Communityの後処理

各runについて以下を実施します。

1. community内部が複数connected componentsなら分割  
2. `MIN_FAMILY_SIZE` 未満は一旦noise  
3. edgeを持たないisolated nodeはnoise  
4. duplicateはcommunity計算に含めない  
5. family IDはこの段階ではrun固有IDとする  

```python
MIN_FAMILY_SIZE = 5
```

---

# 13. Consensus clustering

複数runで一貫して同じcommunityに入る記事対を抽出します。

記事 $i,j$ のco-assignment scoreを、

$$
P_{ij}
=
\frac{1}{R}
\sum_{r=1}^{R}
\mathbf{1}
\left(
z_i^{(r)}
=
z_j^{(r)}
\ne -1
\right)
$$

とします。

ただし、計算量を抑えるため、全 $N^2$ ペアではなくcandidate edgeに対してのみ計算します。

最終consensus graphのweightは、

$$
W_{ij}^{*}
=
A_{ij}P_{ij}
$$

とします。

以下を満たすedgeのみ残します。

$$
P_{ij}\ge \theta_{\mathrm{consensus}}
$$

```python
CONSENSUS_EDGE_THRESHOLD = 0.60
```

その後、`W*` をweightとするgraphにLouvainを再適用し、最終familyを決定します。

この方式により、単一seedや単一resolutionにだけ現れる不安定なclusterを抑制します。

---

# 14. HDBSCAN baseline

原論文との対応確認のため、UMAP＋HDBSCANによるbaselineも実装します。ただし、最終family決定には使用せず、診断用とします。

```python
RUN_HDBSCAN_BASELINE = True
```

処理は次のとおりです。

```text
original embedding
→ UMAP 10次元
→ HDBSCAN
→ hard label
→ soft membership vector
```

HDBSCANはcluster数を事前指定せず、noiseを含むclusterを推定できます。`all_points_membership_vectors()` は各点についてcluster membership vectorを返します。`2009.04508v2.pdf` ([HDBSCAN](https://hdbscan.readthedocs.io/en/latest/soft_clustering.html))

baselineとの比較指標は以下です。

- family数
- noise率
- Adjusted Rand Index
- Adjusted Mutual Information
- cluster size分布
- graph familyとの対応表

ここでのHDBSCAN clusterはあくまでsemantic clusterであり、最終Narrative familyとは解釈しません。

---

# 15. Soft family membership

community detectionはhard partitionを返しますが、記事は複数Narrativeに関係する可能性があります。

そのため、最終結果では、

- primary family
- secondary family
- ambiguous
- noise

を区別します。

## 15.1 Family centroid

family $m$ のembedding centroidを、

$$
\mu_m
=
\operatorname{normalize}
\left(
\frac{1}{|F_m|}
\sum_{j\in F_m}e_j
\right)
$$

とします。

## 15.2 Membership raw score

記事 $i$ のfamily $m$ に対するscoreは、

$$
u_{im}
=
\alpha
S^{\mathrm{sem}}(e_i,\mu_m)
+
(1-\alpha)
\frac{
\sum_{j\in F_m} A_{ij}
}{
\sum_j A_{ij}+\varepsilon
}
$$

とします。

```python
CENTROID_MEMBERSHIP_WEIGHT = 0.60
GRAPH_MEMBERSHIP_WEIGHT = 0.40
```

softmaxを適用します。

$$
p_{im}
=
\frac{
\exp(u_{im}/T)
}{
\sum_{\ell}\exp(u_{i\ell}/T)
}
$$

ただし、`p_im` は統計的にcalibrateされた確率ではないため、出力名は `membership_score` とします。

## 15.3 Assignment rule

推奨初期値は以下です。

```python
CORE_MEMBERSHIP_MIN = 0.60
PRIMARY_MEMBERSHIP_MIN = 0.45
SECONDARY_MEMBERSHIP_MIN = 0.25
AMBIGUITY_MARGIN_MIN = 0.10
MAX_SECONDARY_FAMILIES = 2
```

分類は次のとおりです。

| status | 条件 |
|---|---|
| `core` | primary score ≥ 0.60、marginも十分 |
| `peripheral` | primary score ≥ 0.45 |
| `ambiguous` | top1とtop2の差が0.10未満 |
| `noise` | どのfamilyにも十分所属しない |
| `duplicate` | canonical articleの所属を継承 |

hardな独立partitionにはprimary familyを使い、Stage 2で関連記事を補う場合はsecondary familyも使用します。

---

# 16. Family品質指標

各familyについて以下を計算します。

## 16.1 Internal affinity

$$
I_m
=
\frac{1}{|E_m|}
\sum_{(i,j)\in E_m}A_{ij}
$$

## 16.2 External affinity

$$
X_m
=
\frac{1}{|E_m^{\mathrm{cross}}|}
\sum_{\substack{i\in F_m\\j\notin F_m}}
A_{ij}
$$

## 16.3 Separation

$$
\operatorname{separation}_m
=
I_m-X_m
$$

## 16.4 Temporal continuity

family内の記事について、

- 同じfamilyの過去記事へのedge
- 同じfamilyの将来記事へのedge

の少なくとも一方を持つ記事比率を計算します。

## 16.5 Stability

family内部candidate edgeの平均consensus scoreを、

$$
\operatorname{stability}_m
=
\frac{1}{|E_m|}
\sum_{(i,j)\in E_m}P_{ij}
$$

とします。

## 16.6 その他

- canonical article数
- duplicateを含む総記事数
- 開始日・終了日
- duration
- weighted density
- conductance
- source HHI
- duplicate share
- ambiguous share
- centroid similarity to nearest family
- family内最大time gap
- connected component数

## 16.7 Quality flag

以下を満たすfamilyを `accepted` とします。

```text
n_canonical >= MIN_FAMILY_SIZE
stability >= 0.60
separation > 0
temporal_continuity >= 0.60
connected_components == 1
```

満たさない場合も削除せず、以下を付与します。

```text
low_stability
weak_separation
poor_temporal_continuity
too_small
merge_candidate
split_candidate
```

---

# 17. Representative article

各familyについて次の2種類の代表性を計算します。

1. family embedding centroidに近い  
2. family graph内でcentralityが高い  

各記事について、

```text
centroid_distance_rank
graph_centrality_rank
```

を求め、その平均rankが最小の記事をrepresentative articleとします。

出力は以下です。

```text
representative_article_id
representative_headline
representative_date
top_3_representative_article_ids
```

family labelは、外部LLMを使わず次の形式とします。

```text
F0001 | representative headline
```

補助情報として、

- 代表headline上位3件
- dominant entities
- family内TF-IDF上位語
- start／end date

を表示します。

---

# 18. 出力オブジェクト

ノートブック内に以下のdataclassを定義します。

```python
@dataclass
class NarrativeFamilyResult:
    articles: pd.DataFrame
    families: pd.DataFrame
    memberships: pd.DataFrame
    edges: pd.DataFrame
    duplicates: pd.DataFrame
    rejected_rows: pd.DataFrame
    run_diagnostics: pd.DataFrame
    embeddings: np.ndarray
    family_graph: nx.Graph
    temporal_graph: nx.DiGraph
    config: dict
    fingerprint: str
```

エントリポイントは次の形とします。

```python
result = discover_narrative_families(
    articles_df=articles_df,
    config=CONFIG,
)
```

---

# 19. 出力DataFrame要件

## 19.1 `result.articles`

| 列 | 内容 |
|---|---|
| `article_id` | 記事ID |
| `canonical_article_id` | canonical ID |
| `date` | 公開日時 |
| `event_order` | 時系列順 |
| `headline` | 元headline |
| `body` | 元本文 |
| `event_text` | embedding用テキスト |
| `is_duplicate` | duplicate flag |
| `duplicate_group_id` | duplicate group |
| `primary_family_id` | primary family |
| `primary_membership_score` | primary score |
| `secondary_family_ids` | secondary family list |
| `secondary_membership_scores` | secondary scores |
| `assignment_status` | core/peripheral等 |
| `family_stability_score` | 所属安定度 |
| `baseline_hdbscan_label` | 診断用label |

## 19.2 `result.families`

| 列 | 内容 |
|---|---|
| `family_id` | `F0001`形式 |
| `n_canonical_articles` | canonical記事数 |
| `n_all_articles` | duplicateを含む記事数 |
| `start_date` | 開始日 |
| `end_date` | 終了日 |
| `duration_days` | 期間 |
| `representative_article_id` | 代表記事 |
| `representative_headline` | 代表headline |
| `top_article_ids` | 上位記事 |
| `dominant_entities` | 主要entity |
| `mean_internal_affinity` | 内部affinity |
| `mean_external_affinity` | 外部affinity |
| `separation` | 内外差 |
| `stability` | consensus安定性 |
| `temporal_continuity` | 時間連続性 |
| `weighted_density` | weighted density |
| `ambiguity_ratio` | 曖昧記事比率 |
| `quality_flag` | accepted／review |
| `suggested_start_article_ids` | Stage 2用候補 |
| `suggested_end_article_ids` | Stage 2用候補 |

## 19.3 `result.edges`

```text
source_article_id
target_article_id
source_event_order
target_event_order
delta_days
semantic_similarity
lexical_similarity
entity_similarity
temporal_similarity
content_score
affinity
is_mutual_knn
candidate_rule
retained_in_base_graph
consensus_score
final_edge_weight
same_final_family
```

## 19.4 `result.memberships`

long形式とします。

```text
article_id
family_id
membership_raw_score
membership_score
membership_rank
is_primary
is_secondary
assignment_status
```

## 19.5 `result.run_diagnostics`

```text
run_id
seed
resolution
edge_quantile
n_families
noise_rate
largest_family_share
modularity
mean_internal_affinity
mean_separation
median_family_stability
```

---

# 20. Stage 2への引き渡し

各familyについて、以下のhelper関数を実装します。

```python
stage2_df = make_stage2_input(
    result=result,
    family_id="F0001",
    include_secondary=True,
    secondary_threshold=0.30,
    include_duplicates=False,
)
```

出力列は最低限以下とします。

```text
article_id
date
event_order
headline
body
event_text
embedding
family_id
family_membership_score
assignment_status
```

Stage 2に渡すDataFrameは必ず、

```python
sort_values(["date", "event_order"])
```

とします。

duplicate記事は関連記事情報として保持しますが、Narrative Mapのnodeには原則としてcanonical articleだけを使用します。

---

# 21. Notebookのセル構成

すべての関数、設定、実行処理を一つのNotebook内に実装します。.pyファイルは作成しません。

| セクション | 内容 | Notebook上の出力 |
|---|---|---|
| 0 | タイトル・目的 | Markdown |
| 1 | 論文との関係・Stage 1の定義 | 数式・処理図 |
| 2 | Configuration | 全パラメータ表 |
| 3 | Imports・環境確認 | Python、device、version |
| 4 | 入力DataFrame取得 | shape、列、期間 |
| 5 | 入力QA | 欠損・重複・不正日付 |
| 6 | テキスト正規化 | 処理前後サンプル |
| 7 | Exact duplicate | duplicate件数 |
| 8 | Embedding生成／cache | shape、norm分布 |
| 9 | Near duplicate | duplicate group表 |
| 10 | kNN candidate生成 | edge数、degree分布 |
| 11 | Pair feature計算 | similarity分布 |
| 12 | Base graph構築 | node数、edge数、density |
| 13 | 複数community run | run diagnostics |
| 14 | Consensus graph | consensus分布 |
| 15 | Final family抽出 | family size表 |
| 16 | Soft membership | ambiguous／noise表 |
| 17 | Family summary | representative記事 |
| 18 | HDBSCAN baseline | 比較表 |
| 19 | 可視化 | UMAP、timeline等 |
| 20 | Stage 2入力生成 | family別サンプル |
| 21 | QA・assertions | PASS／WARN／FAIL |
| 22 | 出力保存 | 保存ファイル一覧 |
| 23 | 最終サマリー | family数・品質概要 |

各コードセルの直前に日本語Markdownで以下を記載します。

```text
処理目的
入力
主要パラメータ
出力
注意点
```

---

# 22. 可視化要件

Notebook上に最低限、以下を表示します。

## 22.1 データ診断

- 記事数の時系列推移
- headline／body文字数分布
- duplicate率
- 言語別件数
- source別件数

## 22.2 Similarity診断

- semantic similarity分布
- lexical similarity分布
- affinity分布
- affinityとtime gapのscatter
- retained／rejected edge比較

## 22.3 Graph診断

- degree分布
- connected component size
- edge数とnode数
- giant component share

## 22.4 Family診断

- UMAP 2次元scatter
- family別記事timeline
- family size bar chart
- family stability
- internal vs external affinity
- family centroid similarity matrix
- ambiguous／noise記事一覧

UMAP 2次元座標は可視化専用とし、family affinity計算には使用しません。

---

# 23. QA・assertion要件

## 23.1 入力QA

```python
assert articles_df is not None
assert required_columns <= set(df.columns)
assert article_id is unique after generation
assert date has no NaT in accepted rows
assert event_order is monotonic increasing
```

## 23.2 Embedding QA

```python
assert embeddings.ndim == 2
assert embeddings.shape[0] == n_canonical
assert np.isfinite(embeddings).all()
assert np.allclose(
    np.linalg.norm(embeddings, axis=1),
    1.0,
    atol=1e-3,
)
```

## 23.3 Edge QA

```python
assert source_id != target_id
assert source_event_order < target_event_order
assert 0 <= affinity <= 1
assert 0 <= consensus_score <= 1
assert no duplicate edge
```

## 23.4 Graph QA

```python
assert nx.is_directed_acyclic_graph(G_temporal)
assert no self loops
assert every accepted family is connected in G_family
```

## 23.5 Assignment QA

canonical記事は必ず次のいずれかでなければなりません。

```text
core
peripheral
ambiguous
noise
```

duplicate記事は必ずcanonical articleを参照します。

## 23.6 Warning条件

以下はhard errorではなく警告として表示します。

```text
noise率 > 50%
最大familyが全記事の50%以上
family数がparameter run間で大きく変動
median stability < 0.60
internal affinity <= external affinity
temporal continuity < 0.60
duplicate率 > 30%
accepted familyが0件
```

---

# 24. 推奨初期パラメータ

以下は論文由来の固定値ではなく、実装開始時の基準値です。

```python
CONFIG = {
    # Reproducibility
    "random_seed": 42,

    # Input
    "date_col": "date",
    "headline_col": "headline",
    "body_col": "body",
    "article_id_col": None,

    # Text
    "text_mode": "headline_plus_lead",
    "lead_sentences": 5,
    "max_body_chars": 4_000,
    "min_event_text_chars": 20,

    # Embedding
    "embedding_model": (
        "sentence-transformers/"
        "paraphrase-multilingual-mpnet-base-v2"
    ),
    "embedding_batch_size": 64,
    "normalize_embeddings": True,

    # Duplicate
    "near_duplicate_cosine": 0.985,
    "near_duplicate_max_days": 3,

    # Candidate graph
    "knn_k": 30,
    "tau_days": 30,
    "max_gap_days": 90,
    "max_edges_per_node": 15,

    # Affinity
    "semantic_weight": 0.80,
    "lexical_weight": 0.20,
    "entity_weight": 0.00,
    "temporal_strength": 0.35,

    # Consensus community detection
    "resolution_grid": (0.70, 1.00, 1.30),
    "edge_quantile_grid": (0.55, 0.65, 0.75),
    "community_seeds": (17, 42, 97),
    "consensus_edge_threshold": 0.60,
    "min_family_size": 5,

    # Membership
    "core_membership_min": 0.60,
    "primary_membership_min": 0.45,
    "secondary_membership_min": 0.25,
    "ambiguity_margin_min": 0.10,
    "max_secondary_families": 2,

    # Baseline
    "run_hdbscan_baseline": True,

    # Cache / save
    "cache_embeddings": True,
    "save_outputs": True,
}
```

---

# 25. 保存ファイル

大量の中間ファイルは作成せず、以下に限定します。

```text
data/interim/
    narrative_family_embeddings_<fingerprint>.npz
    narrative_family_pair_features_<fingerprint>.parquet

outputs/narrative_family_discovery/
    family_articles.parquet
    family_summary.parquet
    family_memberships.parquet
    family_edges.parquet
    run_diagnostics.parquet
    duplicate_mapping.parquet
    rejected_rows.parquet
    run_metadata.json
```

図表は原則としてNotebook上に表示し、PNGの自動大量保存は行いません。

---

# 26. 依存ライブラリ

Python 3.12を前提に、依存関係は`uv`で管理します。

```bash
uv add \
  pandas \
  numpy \
  scipy \
  scikit-learn \
  sentence-transformers \
  torch \
  networkx \
  umap-learn \
  hdbscan \
  matplotlib \
  pyarrow \
  tqdm \
  pynndescent
```

Notebook内での`pip install`は行いません。

NERを使用する場合だけ、別途optional dependencyを追加します。NERなしでもsemantic＋character n-gram lexical similarityにより、Notebook全体が実行できる仕様にします。

---

# 27. 計算量要件

主要処理の計算量は次を目標とします。

| 処理 | 計算量 |
|---|---:|
| Embedding | $O(N)$ |
| kNN | backend依存 |
| Pair features | $O(Nk)$ |
| Graph storage | $O(Nk)$ |
| Community detection | 疎graphに依存 |
| Consensus | $O(RNk)$ |

通常処理で $N\times N$ のdense similarity matrixは作成しません。

例外として、以下の場合だけdense matrixを許可します。

```python
N <= DENSE_MATRIX_MAX_N
```

推奨値は、

```python
DENSE_MATRIX_MAX_N = 3_000
```

です。

---

# 28. Definition of Done

実装完了条件は以下です。

1. `articles_df`を設定するだけでNotebookが上から下まで実行できる  
2. family数をユーザーが指定しない  
3. 同じ入力・設定・seedで同じ結果を得られる  
4. exact／near duplicateがfamily数を不当に増やさない  
5. semantic、lexical、temporalの各寄与を確認できる  
6. familyごとに記事、代表記事、期間、品質指標を取得できる  
7. primaryとsecondary familyを区別できる  
8. noiseおよびambiguous記事を明示できる  
9. familyのparameter stabilityを確認できる  
10. HDBSCAN baselineとgraph familyを比較できる  
11. accepted familyが時間的・グラフ的に接続している  
12. Stage 2用DataFrameをfamily別に生成できる  
13. future-directed edgeが一件も存在しない  
14. 大規模なdense pairwise matrixを通常処理で生成しない  
15. 各処理セルで入力・出力を目視確認できる  
16. 最終セルにPASS／WARN／FAILのQA summaryを表示する  

この仕様では、**semantic topicの単純クラスタリングではなく、時間的に連続し、関連記事間の接続構造が安定しているevent episodeをNarrative familyとして抽出**します。そのうえで、primary familyによる独立partitionとsecondary membershipによる重複関係を両方保持し、次段階のNarrative Map最適化へ渡せる構造にします。

---

**Sources:**

- [https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py](https://raw.githubusercontent.com/briankeithn/narrative-maps/main/packages/solve_LP.py)
- [https://www.sbert.net/docs/package_reference/sentence_transformer/SentenceTransformer.html](https://www.sbert.net/docs/package_reference/sentence_transformer/SentenceTransformer.html)
- [https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.NearestNeighbors.html](https://scikit-learn.org/stable/modules/generated/sklearn.neighbors.NearestNeighbors.html)
- [https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.community.louvain.louvain_communities.html](https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.community.louvain.louvain_communities.html)
- [https://hdbscan.readthedocs.io/en/latest/soft_clustering.html](https://hdbscan.readthedocs.io/en/latest/soft_clustering.html)

## Response:
2026/7/27 23:59:54

> **パーソナライズ中**
>
>
> **パーソナライズ設定を確認し、実装構造を特定した**
>
> 
>
> 33m 1s考えました

## 実装済みNotebook

[01_discover_narrative_families.ipynb をダウンロード](sandbox:/mnt/data/01_discover_narrative_families.ipynb)

Notebookは単体完結で、全コードセルを実行済みです。構文・Notebook形式を検証し、実行エラーがないことも確認しています。

原論文は、あらかじめ「特定のissueに関する記事集合」と開始・終了イベントが与えられた状態で、一つのNarrative Mapを構築する問題を対象としています。したがって、本NotebookのNarrative family discoveryは、その前段に追加した拡張処理です。`2009.04508v2.pdf`  
一方、クラスタ数を事前指定せずにUMAP＋HDBSCANを利用する考え方は原論文に沿っており、Notebook内でも診断用baselineとして実装しています。`2009.04508v2.pdf`

## 実装内容

Notebookには、以下をすべて含めています。

- `date`、`headline`、`body`を持つDataFrameの入力検証
- headline＋本文冒頭からのevent text生成
- Unicode・HTML・空白等のテキスト正規化
- 不正行の隔離と理由記録
- exact duplicateおよびnear duplicateの統合
- Sentence Transformerによる多言語embedding
- 事前計算embeddingの利用
- 軽量なTF-IDF＋TruncatedSVD fallback
- embedding cacheとfingerprint管理
- cosineから変換したangular semantic similarity
- 文字n-gram TF-IDFによるlexical similarity
- 任意の`entities`列を使ったIDF-weighted Jaccard similarity
- 時間減衰を含む記事間affinity
- k-nearest-neighborによる疎なcandidate graph
- 複数resolution・edge threshold・random seedによるLouvain反復
- co-assignmentに基づくconsensus graph
- Narrative family数の自動決定
- primary／secondary soft membership
- `core`、`peripheral`、`ambiguous`、`noise`の分類
- familyごとの代表記事、主要entity、上位語、期間
- internal／external affinity、separation、stability、temporal continuity
- Stage 2向け開始・終了記事候補
- HDBSCAN baselineとのARI・AMI比較
- DataFrame、グラフ、埋め込み、設定をまとめた結果dataclass
- QAのPASS／WARN／FAIL判定
- ParquetまたはCSV、NPZ、JSONへの保存
- family別Stage 2入力DataFrameの生成
- family size、timeline、stability、embedding projectionなどの可視化

原論文でも、embedding空間、クラスタリング手法、期待経路長、coverageなどのパラメータが最終表現へ大きく影響すると指摘されています。そのためNotebookでは単一partitionを採用せず、複数設定によるconsensusと安定性診断を実装しています。`2009.04508v2.pdf`

## 実データへの切り替え

入力セルを次のように変更します。

```python
USE_DEMO_DATA = False
articles_df = your_dataframe.copy()
```

必須列は次の3列です。

```text
date
headline
body
```

任意列として以下を利用できます。

```text
article_id
source
url
language
entities
```

`entities`は、人物、企業、国、政策、商品などをlist形式で保持する想定です。存在しない場合も、semantic similarityとlexical similarityだけで実行できます。

実データでは、Configurationセルの以下が標準設定です。

```python
CONFIG["embedding_backend"] = "sentence_transformer"
CONFIG["embedding_model"] = (
    "sentence-transformers/"
    "paraphrase-multilingual-mpnet-base-v2"
)
```

依存関係はNotebook冒頭に`uv add`形式で記載しており、Notebook内で`pip install`は実行しません。

## 主要出力

実行後の中心オブジェクトは`result`です。

```python
result.articles
```

記事ごとのprimary family、secondary family、duplicate、assignment statusを保持します。

```python
result.families
```

familyごとの期間、代表記事、記事数、安定性、分離度、時間連続性、品質フラグを保持します。

```python
result.memberships
```

記事×familyのlong形式soft membershipです。

```python
result.edges
```

candidate edgeごとのsemantic、lexical、entity、temporal similarity、affinity、consensus scoreを保持します。

```python
result.run_diagnostics
```

resolution、edge threshold、seedを変えた各community detection runの診断結果です。

## Stage 2への受け渡し

任意のfamilyを次のように抽出できます。

```python
stage2_df = make_stage2_input(
    result=result,
    family_id="F0001",
    include_secondary=False,
    include_primary_ambiguous=False,
    include_duplicates=False,
)
```

出力には次が含まれます。

```text
article_id
date
event_order
headline
body
event_text
embedding
family_id
family_membership_score
assignment_status
```

`include_primary_ambiguous=False`が既定であるため、hard communityには属していてもsoft membershipが曖昧な記事は、Narrative Mapのcore node候補から除外されます。

## 実行確認結果

付属の合成データでは、以下を確認しています。

- 36記事を入力
- canonical article：34件
- duplicate：2件
- Narrative family：3個
- 既知の3episodeに対するAdjusted Rand Index：`1.000`
- 既知episode記事のhard-family coverage：`100%`
- 3familyすべての品質判定：`accepted`
- QA assertion：実行エラーなし

付属demoでは、無関係なone-off記事の一つがhard communityへ入るケースも意図的に残していますが、soft membershipでは`ambiguous`と判定され、Stage 2入力から既定で除外されます。これにより、hard partitionだけを過信せず、soft assignmentと品質診断を併用する挙動を確認できます。

## Response:
2026/8/2 22:12:51

> **方針を整理中**
>
> 添付資料の手法と、既存のナラティブ・ファミリー発見工程を接続するには、発見したクラスターを時系列シグナルへ変換し、将来情報の混入を防ぎながら検証する設計が軸になりそうです。
>
> **方針を整理した**
>
> 
>
> **測定設計を詰める**
>
> 記事の重複除去は発見工程で行いつつ、強度は原記事数を基本に集計し、重複群への上限や媒体別重みで頑健性を確認します。感情は日本語・英語対応を分け、全体・肯定・否定を別系列として保持します。
>
> **測定設計を具体化した**
>
> 
>
> **設計を具体化する**
>
> 親ファミリー強度は論文準拠のモメンタム指標に使い、経路・エピソード強度は説明や早期警戒に回します。登録簿は先行情報なしで更新し、時点別IDを固定します。内生的発見と既知分類を並行検証し、分類数変動も補正します。
>
> **具体的な設計を策定した**
>
> 
>
> **出力構造と関数定義の確認**
>
>
> **接続方針を比較する**
>
> 添付手法は固定ナラティブの時系列強度を前提とするため、まず発見したファミリーを時点付き登録簿へ変換します。その上で、論文準拠の強度・モメンタムと、経路別の早期警戒指標を分離して検証する設計が適切です。
>
> **比較して接続方針を決定した**
>
> 
>
> **出力構造と関数定義を確認した**
>
> 
>
> **パーソナライズ中**
>
>
> **パーソナライズとニュースデータ要件を整合化した**
>
> 
>
> 12m 45s考えました

# 結論

接続は可能です。ただし、現在の `01_discover_narrative_families.ipynb` が抽出する **短期的なニュース・エピソード**を、そのまま *Narrative Momentum* 論文の「Narrative」として扱うのは非推奨です。

両論文では「Narrative」の時間スケールが異なります。

- **Narrative Maps**：特定のissueに属する記事群から、開始イベントと終了イベントを結ぶ複数のstorylineを抽出する。対象はイベント・エピソード単位です。`2009.04508v2.pdf`
- **Narrative Momentum**：Inflation、Recession、Monetary Policy、Armed Conflictなど、長期間継続する347個の比較的安定したNarrativeを前提に、日次attention intensity、52週ローリングの株式beta、月次のmimicking portfolioを構築します。`Narrative Momentum.pdf`

したがって、推奨する接続構造は次です。

$$
\boxed{
\text{安定した親Narrative}
\supset
\text{動的なNarrative family／episode}
\supset
\text{Narrative Mapのroute}
}
$$

たとえば、

```text
Armed Conflict                    ← 長期・安定した親Narrative
├─ Russia-Ukraine escalation      ← Stage 1のNarrative family
│  ├─ military escalation route   ← Narrative Mapの経路
│  └─ sanctions route
└─ Middle East conflict
   ├─ ceasefire route
   └─ energy-market route
```

という階層にします。

---

# 1. 現在のStage 1をそのまま接続できない理由

## 1.1 `F0001`などのfamily IDはrun内でしか意味を持たない

現在のNotebookは、与えられた期間のcommunity detection結果に、

```text
F0001
F0002
...
```

というIDを付けています。

別期間で再実行すると、同じNarrativeでも別IDになる可能性があります。そのため、日次や月次のintensity panelを作る前に、期間をまたいで一貫した、

```text
canonical_narrative_id
episode_id
family_version
active_from
active_to
```

を管理する必要があります。

## 1.2 現在のfamilyはepisode粒度であり、52週betaを推定しにくい

*Narrative Momentum* は、各株式について直近52週を用いてNarrative betaを推定し、最低26週の観測を要求しています。`Narrative Momentum.pdf`

一方、Stage 1が発見するfamilyが数週間から数か月のepisodeである場合、

- 52週のintensity variationが存在しない
- family出現前はintensityがほぼゼロ
- betaが少数のattention spikeに依存する
- family誕生と価格変動が同時になり、内生性が強くなる

という問題が生じます。

したがって、長期betaは原則として安定した親Narrativeに対して推定し、動的episodeは親Narrativeの内訳や短期overlayとして使用するのが自然です。

## 1.3 全期間を使ったクラスタリングは将来情報を含む

*Narrative Momentum* は、記事をpoint-in-timeで収集している点を重視し、archiveのbackfillや事後修正によるbiasを避けています。`Narrative Momentum.pdf`

また、「attentionが上昇した後にNarrativeがライブラリへ追加されたのではないか」というselection biasに対し、事前定義されたevergreen narrativesや外部Narrative setを使った検証を行っています。`Narrative Momentum.pdf`

したがって、過去から未来までの全記事を使ってfamilyを発見し、そのfamilyに過去記事を遡及的に割り当て、そのintensityでbacktestすることはできません。

---

# 2. 考えられる三つの接続方針

| 方針 | 内容 | 論文整合性 | 新規Narrative | 推奨用途 |
|---|---|---:|---:|---|
| 固定Narrative方式 | 事前期間でNarrative集合を作り、その後は固定classifierで記事を割り当てる | 高い | 弱い | 論文再現・baseline |
| 完全動的方式 | 各時点でStage 1を再実行し、familyを随時生成する | 低い | 強い | exploratory analysis |
| 階層型方式 | 安定した親Narrativeと動的episodeを併用する | 高い | 強い | 推奨production仕様 |

## 推奨：階層型方式

最も整合的なのは、

1. 長期的に安定した親Narrativeを作る  
2. Stage 1で動的episodeを発見する  
3. episodeを親Narrativeへmulti-labelで紐付ける  
4. 親Narrativeで論文準拠のbetaとmimicking portfolioを推定する  
5. episodeおよびNarrative Map情報をsignalの補助変数として使う  

という方法です。

---

# 3. 推奨する全体処理フロー

```text
日次記事
  │
  ├─ technical duplicate除去
  │
  ├─ Stage 1：Narrative family discovery
  │      └─ 動的episodeの発見
  │
  ├─ Narrative registry
  │      ├─ stable parent narrative
  │      ├─ episode lineage
  │      └─ split / merge / retirement
  │
  ├─ 記事のmulti-label narrative assignment
  │
  ├─ 日次Narrative intensity
  │
  ├─ Stage 2：episode別Narrative Map
  │      ├─ main route
  │      ├─ alternative routes
  │      ├─ representative landmarks
  │      └─ coherence / coverage
  │
  ├─ 週次Narrative intensity change
  │
  ├─ 株式Narrative beta
  │
  ├─ Narrative-mimicking portfolio
  │
  └─ Narrative Momentum strategy
```

Narrative Mapは、Narrative intensityの代わりではありません。役割は、

- Narrative familyの品質確認
- episodeの代表記事・経路の抽出
- family IDの継続判定
- route別attentionの分解
- intensity signalを補強する特徴量の生成

です。

---

# 4. 安定したNarrative registryを追加する

現在のNotebookに直接追加するより、次段階としてNarrative registryを管理する処理を作るのが適切です。

## 4.1 管理する三種類のID

```text
canonical_narrative_id
    長期間継続する親Narrative
    例：INFLATION、MONETARY_POLICY

episode_id
    Stage 1で発見された一時的episode
    例：INFLATION_2026_OIL_SHOCK

family_run_id
    特定のStage 1実行で得られたlocal family
    例：2026M07_F0003
```

関係は、

```text
canonical_narrative_id 1 ─ N episode_id
episode_id             1 ─ N family_run_id
```

とします。

## 4.2 期間間のfamily matching

月次または週次でStage 1を実行した場合、新family $C_{m,t}$ と既存episode $E_{n,t-1}$ の一致度を、

$$
M_{mn,t}
=
\lambda_1
\cos(\mu_{m,t},\mu_{n,t-1})
+
\lambda_2
J(\mathcal E_{m,t},\mathcal E_{n,t-1})
+
\lambda_3
J(\mathcal K_{m,t},\mathcal K_{n,t-1})
+
\lambda_4
O_{mn,t}
$$

で計算します。

ここで、

- $\mu$：family embedding centroid
- $\mathcal E$：主要entity集合
- $\mathcal K$：主要keyword集合
- $O$：重複window内の記事重複率

です。

Hungarian matchingなどで一対一の継続候補を決めたうえで、

- one-to-one：継続
- one-to-many：split
- many-to-one：merge
- unmatched：新episodeまたは終了

としてlineageを記録します。

## 4.3 Point-in-time activation

新しいepisodeは、初めて検出された時点まで過去に遡って取引に使ってはいけません。

```text
detected_at
active_from
inactive_from
tradable_from
```

を分けます。

たとえば、

```text
2回連続のrolling windowで検出
family stability >= 0.60
n_articles >= 10
```

を満たした翌営業日を `tradable_from` とします。

---

# 5. 記事membershipはsoftmaxではなく独立multi-labelに変更する

現在のStage 1のsoft membershipは、family間でsoftmaxを取り、概ね合計が1になるpartition-orientedなscoreです。

これはNarrative Mapへ記事を分割する目的には適していますが、Narrative intensityには適していません。ニュース記事は同時に、

```text
Inflation
Monetary Policy
Oil
Recession
```

へ関連し得るからです。

intensity用には、Narrativeごとに独立したrelevance score、

$$
q_{a,n}\in[0,1]
$$

を作ります。各記事について、

$$
\sum_n q_{a,n}=1
$$

である必要はありません。

実装方法としては次の順が考えられます。

1. Stage 1のcore記事とrepresentative記事を教師データとする  
2. Narrativeごとにprototype embeddingを作る  
3. one-vs-rest classifierまたはprototype similarityをcalibrationする  
4. 新規記事を複数Narrativeへ割り当てる  

Stage 1の、

```text
primary_family_id
secondary_family_ids
```

はNarrative Map構築に使用し、intensity計算には別の、

```text
narrative_relevance_score
narrative_relevance_flag
```

を使用します。

---

# 6. Narrative intensityへの接続

## 6.1 論文準拠の基本形

*Narrative Momentum* では、日次のnegativeまたはpositive intensityを、そのNarrativeに関連するsentiment記事数の、日次全記事数に対する比率として構成し、月次intensityは日次intensityの月平均です。`Narrative Momentum.pdf`

hard classificationなら、

$$
I_{n,d}^{-}
=
\frac{
\sum_{a\in\mathcal A_d}
\mathbf 1(a\in n)
\mathbf 1(\operatorname{sentiment}_a=-)
}{
|\mathcal A_d|
}
$$

です。

## 6.2 Stage 1と接続するsoft intensity

記事のNarrative relevanceを $q_{a,n}$、negative sentiment確率を $s_a^{-}$ とすると、

$$
I_{n,d}^{-}
=
\frac{
\sum_{a\in\mathcal A_d}
q_{a,n}s_a^{-}
}{
N_d
}
$$

と拡張できます。

positive、overallも、

$$
I_{n,d}^{+}
=
\frac{
\sum_a q_{a,n}s_a^{+}
}{
N_d
}
$$

$$
I_{n,d}^{\mathrm{all}}
=
\frac{
\sum_a q_{a,n}
}{
N_d
}
$$

と計算します。

これは論文のhard countをsoft countへ拡張したものであり、論文そのものの定義ではありません。したがって、最初に必ず、

- hard assignment intensity
- soft assignment intensity

の両方を計算し、結果を比較すべきです。

## 6.3 sentimentの扱い

論文のbaselineはnegative intensityです。ただし、positive intensityおよびpositive＋negativeのoverall intensityでもNarrative momentumが確認され、negativeが相対的に強いという結果です。`Narrative Momentum.pdf`

日本語記事を含む場合、Loughran-McDonald辞書をそのまま適用するのは不適切です。次の三系列を並行して作るのが安全です。

```text
intensity_negative
intensity_positive
intensity_overall
```

sentiment modelの精度に不安がある段階では、`intensity_overall` をrobustness baselineとして保持します。

---

# 7. duplicate処理はMap用とintensity用で分ける

現在のNotebookではnear duplicateをcanonical articleへ統合しています。これはNarrative Mapには適切ですが、media attentionには注意が必要です。

同じ記事が複数媒体で掲載されたこと自体が、Narrativeの拡散やmedia attentionを表す可能性があるためです。論文も、幅広いsourceから大量の記事を収集することを重視し、WSJやNYTのような一般媒体だけに限定すると結果が弱くなると報告しています。`Narrative Momentum.pdf`

したがって、

### Narrative Map

```text
canonical articleのみ
```

### Narrative intensity

```text
全media itemを使用
ただし同一URLの二重取得などtechnical duplicateは除去
```

とします。

同一wire記事が複数sourceで掲載された場合は、

- volume-weighted baseline：各掲載を1件として数える
- source-balanced robustness：sourceごとに正規化する
- duplicate-group-capped：同一duplicate groupの寄与に上限を設ける

の三仕様を比較するのが適切です。

---

# 8. Media reservoirへの拡張

論文は記事を、

- General
- Corporate
- FX
- Country Equity

に分け、reservoirごとにNarrative intensityとNarrative betaを計算しています。reservoirによってstrategy performanceが異なり、特にFX reservoirが強いケースが報告されています。`Narrative Momentum.pdf` `Narrative Momentum.pdf`

ユーザーデータに `source` や `category` がある場合、

$$
I_{n,d,r}^{-}
=
\frac{
\sum_{a\in\mathcal A_{d,r}}
q_{a,n}s_a^{-}
}{
N_{d,r}
}
$$

を計算し、

```text
Aggregate
General
Corporate
FX
Country
```

などを別系列で保持します。

日本語ニュースの場合は、たとえば、

```text
一般報道
企業・決算
為替
金利・債券
商品
政策・マクロ
```

へ再設計しても構いません。ただし、baselineとして全記事aggregateも残します。

---

# 9. 週次Narrative intensityと株式beta

論文は、木曜日から翌水曜日までの日次intensityを平均して週次intensityとし、水曜終値から翌水曜終値までの株式returnと対応させています。`Narrative Momentum.pdf`

$$
I_{n,w}
=
\frac{1}{|D_w|}
\sum_{d\in D_w} I_{n,d}
$$

$$
\Delta I_{n,w}
=
I_{n,w}-I_{n,w-1}
$$

株式Narrative betaは、

$$
r_{i,w}^{e}
=
\alpha_i
+
\beta_{m,i}r_{m,w}^{e}
+
\beta_{n,i}\Delta I_{n,w}
+
\varepsilon_{i,w}
$$

です。

論文準拠仕様は、

```text
beta estimation frequency : weekly
rolling window             : 52 weeks
minimum observations       : 26 weeks
beta snapshot              : month-end
portfolio rebalance        : monthly
estimation/execution gap   : at least 1 day
```

です。`Narrative Momentum.pdf`

日本市場へ適用する場合も、曜日を機械的に合わせることより、

```text
記事集計cutoff
→ beta estimation cutoff
→ portfolio execution
```

の因果順序を厳守することが重要です。

---

# 10. Narrative-mimicking portfolio

論文では、各Narrativeについて、

- beta上位25銘柄をlong
- beta下位25銘柄をshort
- 上位3,000社
- 株価1ドル超
- 月次rebalance

としています。`Narrative Momentum.pdf`

$$
R_{n,t}^{NMP}
=
\sum_i w_{i,n,t}^{+}r_{i,t}
-
\sum_i w_{i,n,t}^{-}r_{i,t}
$$

現在のStage 1と接続する場合は、まず親Narrativeについてこのportfolioを作ります。

## 親Narrative

長いintensity historyがあるため、論文準拠betaを推定できます。

## episode

十分なhistoryがないことが多いため、次のいずれかとします。

1. 親Narrative portfolioを利用する  
2. episode betaを親betaへshrinkする  
3. 26週以上のhistoryを持つepisodeだけ個別推定する  
4. episodeはportfolio exposureではなくsignal強度だけに使う  

推奨は4です。

つまり、

```text
exposureを決める：親Narrative beta
timingを補強する：episode intensity / Narrative Map features
```

とします。

---

# 11. Narrative Momentum signal

論文のintensity momentum signalは、直近 $J$ か月の平均intensityと、その前の $J$ か月の平均intensityとの差です。

$$
S_{n,t}^{(J)}
=
\frac{1}{J}
\sum_{j=1}^{J}I_{n,t-j}
-
\frac{1}{J}
\sum_{j=J+1}^{2J}I_{n,t-j}
$$

論文は、

$$
J\in\{1,3,6,9,12\}
$$

を使用し、上位10Narrativeをrising、下位10をdecliningとしています。`Narrative Momentum.pdf`

そのNarrative-mimicking portfolioを、

$$
K\in\{1,3,6,9\}
$$

か月保有し、$K>1$ の場合はoverlapping portfolioをladder方式で平均します。`Narrative Momentum.pdf`

接続後のbaselineは、

```text
J = 6 months
K = 6 months
top narratives = 10
bottom narratives = 10
```

が最も論文と比較しやすい仕様です。

ただし動的Narrative universeでは、各月のeligible narrative数が変化します。したがって、

```python
n_select = min(
    10,
    floor(eligible_narratives * 0.20),
)
```

などのfallbackを用意します。

eligibilityには最低限、

```text
active_from <= formation date - 2J
intensity history >= 2J months
beta history >= 26 weeks
family quality accepted
minimum media coverage
```

を要求します。

---

# 12. Narrative Mapはどこで使うか

Narrative Map論文では、main routeは最大尤度path、representative landmarksはmaximum antichainから抽出されます。main routeはNarrativeの時間方向の深さ、landmarksはstoryline横断方向の広がりを表します。`2009.04508v2.pdf`

これをNarrative Momentumへ接続する方法は四つあります。

## 12.1 Family品質filter

以下を満たさないfamilyをNarrative universeから除外します。

```text
LP feasible
minimum coherence以上
coverage以上
main route length以上
十分な関連記事数
```

Stage 1のcommunityだけでなく、実際に時系列的なstorylineを形成できることを確認できます。

## 12.2 Narrative prototypeの更新

main route記事とrepresentative landmarksを使って、

```text
narrative centroid
representative headlines
dominant entities
keywords
```

を更新します。

すべての記事を単純平均するより、familyの中心的な意味を保持しやすくなります。

## 12.3 Route別intensity

episode $e$ のroute $r$ ごとに、

$$
I_{e,r,d}^{-}
=
\frac{
\sum_a q_{a,e,r}s_a^{-}
}{
N_d
}
$$

を計算します。

たとえばInflation Narrativeの中で、

```text
energy-price route
wage route
monetary-policy route
supply-chain route
```

を分けて追跡できます。

ただし、route IDはepisode間で安定しないため、これは短期分析または説明用とし、長期betaの主要説明変数にはしない方が安全です。

## 12.4 Map-weighted intensity

記事の役割に応じて、

$$
\widetilde I_{n,d}^{-}
=
\frac{
\sum_a
q_{a,n}
s_a^{-}
\omega_{a,n}
}{
N_d
}
$$

とします。

例として、

```text
main route constituent : 1.00
representative landmark: 0.75
alternative route      : 0.60
peripheral related     : 0.30
```

と重み付けできます。

ただしこれはNarrative Momentum論文の手法ではありません。baselineは必ず記事数ベースのunweighted intensityとし、Map-weighted intensityは追加signalとして検証すべきです。

---

# 13. 「slowly trending narratives」への接続

論文では、attentionが小刻みに連続して変化するNarrativeほどmomentumが強いという結果があります。

smoothnessは、

$$
\operatorname{Smoothness}_{n,t}
=
\frac{
\left|
\operatorname{mean}(\Delta I_{n,w})
\right|
}{
\operatorname{std}(\Delta I_{n,w})
}
$$

として計算されています。`Narrative Momentum.pdf`

また、月次intensityにAR(12)を適合し、単位shockへのimpulse responseが0.25未満になるまでの月数をquarter-lifeとしています。persistentなNarrativeの方が強いmomentumを示します。`Narrative Momentum.pdf`

Stage 1およびNarrative Mapから得られる、

```text
family duration
route length
branching
coherence
source diffusion
article arrival regularity
```

を、このsmoothnessやquarter-lifeと組み合わせられます。

ただし、

```text
Narrative Mapの経路が長い
=
attention intensityがpersistent
```

とは限りません。論文準拠のsmoothnessとAR quarter-lifeをまず計算し、Map特徴量は追加的な説明変数として検証します。

---

# 14. 現在のNotebookに必要な変更

[現在のStage 1 Notebook](sandbox:/mnt/data/01_discover_narrative_families.ipynb)は、Narrative Mapの入力生成にはそのまま利用できます。Narrative Momentumへ接続するには、少なくとも次の変更が必要です。

## 変更1：local family IDとpersistent IDを分離

追加列：

```text
family_run_id
episode_id
canonical_narrative_id
parent_narrative_id
family_version
detected_at
active_from
tradable_from
inactive_from
lineage_status
```

## 変更2：as-of実行モード

```python
result = discover_narrative_families_asof(
    articles_df=articles_df,
    asof_date=asof_date,
    lookback_days=365,
)
```

のように、`asof_date`より未来の記事を一切使わない仕様にします。

## 変更3：intensity用の独立multi-label relevance

現在のsoftmax membershipとは別に、

```text
article_id
canonical_narrative_id
relevance_score
relevance_flag
classification_model_version
classification_asof
```

を出力します。

## 変更4：canonical記事と全media itemを両方保持

```text
map_core_flag
technical_duplicate_flag
syndication_group_id
count_for_intensity
```

を分離します。

## 変更5：sentimentとreservoir

```text
sentiment_negative_probability
sentiment_positive_probability
reservoir
source_weight
```

を追加します。

## 変更6：Narrative Map特徴量を保存

```text
map_coherence
map_coverage
main_route_length
main_route_probability
antichain_width
n_routes
route_role
map_asof
```

をepisodeごとに保存します。

---

# 15. 推奨するNotebook構成

既存Notebookを過度に肥大化させず、以下のように分けるのが適切です。

```text
01_discover_narrative_families.ipynb
    動的episodeの発見
    現在実装済み

02_track_narrative_registry_point_in_time.ipynb
    family matching
    stable ID
    split / merge / activation

03_extract_narrative_maps_by_episode.ipynb
    LP
    main route
    landmarks
    map quality

04_compute_narrative_intensities.ipynb
    multi-label relevance
    sentiment
    daily / weekly / monthly intensity
    reservoir別系列

05_estimate_narrative_betas_and_mimicking_portfolios.ipynb
    rolling 52週beta
    top/bottom stock portfolios
    monthly rebalance

06_backtest_narrative_momentum.ipynb
    J-month signal
    K-month ladder
    top/bottom narratives
    performance・robustness
```

---

# 16. 推奨baseline仕様

最初の実証では、Narrative Map情報をsignalに混ぜず、論文に近いbaselineを作るべきです。

```python
BASELINE = {
    "narrative_level": "stable_parent",
    "taxonomy_mode": "pre_sample_frozen",
    "article_assignment": "independent_multilabel",
    "intensity_type": "negative",
    "intensity_weighting": "unweighted_media_count",
    "weekly_alignment": "thu_to_wed",
    "beta_window_weeks": 52,
    "beta_min_observations": 26,
    "beta_controls": ["market"],
    "stock_selection_per_leg": 25,
    "portfolio_rebalance": "monthly",
    "formation_months_J": 6,
    "holding_months_K": 6,
    "narratives_per_leg": 10,
    "laddered_holding": True,
}
```

その後、順番に、

1. soft intensity  
2. overall intensity  
3. reservoir別intensity  
4. Map quality filter  
5. Map-weighted intensity  
6. episode intensity overlay  
7. factor-neutralまたはBarra-neutral mimicking portfolio  

を追加します。

---

# 17. Barra・純化ポートフォリオへ接続する場合

論文準拠baselineでは、beta推定時のcontrolはmarket returnです。その後のalpha評価でFF6を使っています。

実務拡張としては、

$$
r_{i,w}
=
\alpha_i
+
B_{i,w}^{\top}f_w
+
\beta_{n,i}\Delta I_{n,w}
+
\varepsilon_{i,w}
$$

のように、Barraまたは既存factorを同時にcontrolできます。

あるいは、あらかじめ、

$$
r_{i,w}^{\mathrm{resid}}
=
r_{i,w}-B_{i,w}^{\top}f_w
$$

を計算し、

$$
r_{i,w}^{\mathrm{resid}}
=
\alpha_i+\beta_{n,i}^{\mathrm{pure}}\Delta I_{n,w}+\varepsilon_{i,w}
$$

とすれば、既存factorから純化されたNarrative betaを推定できます。

Narrative-mimicking portfolioも、

$$
\max_w
\quad
w^\top\beta_n
-
\lambda w^\top\Sigma w
$$

subject to

$$
B^\top w=0,\qquad
\mathbf 1^\top w=0,\qquad
\|w\|_1\le G
$$

とすることで、Barra-neutralなNarrative portfolioへ拡張できます。

ただし、論文再現結果との比較を可能にするため、

```text
単純top/bottom 25 portfolio
Barra-neutral optimized portfolio
```

は必ず別系列として保持すべきです。

---

# 最終推奨

最も学術的・実務的に整合する接続方針は、

$$
\boxed{
\begin{aligned}
&\text{Stage 1で動的episodeを発見}\\
&\rightarrow
\text{stable parent Narrativeへ紐付け}\\
&\rightarrow
\text{全記事からpoint-in-time intensityを構築}\\
&\rightarrow
\text{親Narrativeで52週betaを推定}\\
&\rightarrow
\text{Narrative-mimicking portfolioを構築}\\
&\rightarrow
\text{intensity momentumでNarrativeを選択}\\
&\rightarrow
\text{Narrative Map情報を品質filter・補助signalとして追加}
\end{aligned}
}
$$

です。

特に重要なのは、**Narrative Mapに選択された記事だけでintensityを計算しないこと**、**Stage 1のsoftmax membershipをそのままintensityへ使わないこと**、**全期間を見て発見したfamilyを過去へ遡及して取引しないこと**の三点です。

## Response:
2026/8/2 22:50:03

> **{"paths":["files"]}**
>
>
> **設計を整理する**
>
> 私は、動的な家族発見をやめ、固定ナラティブと強度別の展開へ移す設計として整理します。既存の類型分類・採点・ベンチマークは再利用し、入力契約と出力形式を固定します。
>
> **設計を整理した**
>
> 
>
> **設計方針を固める**
>
> 私は、固定ナラティブ集合を先に定義し、各記事を既存の前処理・重複除去・埋め込み・関連記事抽出で割り当て、日次強度と移動平均との差分を計算する構成にします。論文の347類型、割合ベース強度、点時点処理を踏襲します。
>
> **組み立て直す**
>
> 私は既存処理を再利用し、固定ナラティブ辞書、記事割当、日次割合強度、平滑化、前年比較、監査用証拠表までを一つのノートブックにまとめます。動的クラスタは探索補助へ限定し、最終系列は固定定義にします。
>
> **設計方針を固めて再構成した**
>
> 
>
> **設計を具体化する**
>
> 私は、固定ナラティブ辞書を先に凍結し、既存の前処理・重複除去・埋め込みを再利用して記事を割り当てます。日次割合強度、移動平均との差分、監査用証拠表を一つのノートブックで出力します。
>
> **具体的な設計を策定した**
>
> 
>
> **ファイル一覧を取得しました**
>
> 
>
> 14m 47s考えました

# 結論

現在の [01_discover_narrative_families.ipynb](sandbox:/mnt/data/01_discover_narrative_families.ipynb) は、**固定Narrativeを時系列で計測するための前処理基盤としてかなり再利用できます**。ただし、現在のLouvain／HDBSCANによる動的family discoveryとsoftmax membershipを、そのままNarrative Intensityへ使うべきではありません。

推奨構成は次です。

$$
\boxed{
\text{既存の前処理・重複管理・埋め込み}
\rightarrow
\text{固定Narrative Registry}
\rightarrow
\text{独立multi-label分類}
\rightarrow
\text{記事sentiment}
\rightarrow
\text{日次Narrative Intensity}
}
$$

既存Stage 1は廃止せず、以下の二つに使います。

1. 固定Narrative分類器を作るための正例候補・関連記事候補の抽出  
2. 固定Narrativeに分類されない新規episodeやtaxonomy driftの監視  

一方、実際の時系列推定は、期間ごとに変わるcommunity IDではなく、あらかじめ固定した `narrative_id` によって行います。

---

# 1. 文献から再現できる部分と、再現できない部分

## 1.1 再現できる部分

*Narrative Momentum* は、約350のNarrativeについてmedia coverage intensityを定量化し、株式Narrative betaとmimicking portfolioを構築しています。`Narrative Momentum.pdf`

Intensityについては明確に定義されています。

- 日次negative intensity：そのNarrativeに関連するnegative記事数を、その日の全media items数で割る
- 日次positive intensity：同様にpositive記事で計算
- 月次intensity：日次intensityの月内平均

`Narrative Momentum(1).pdf`

したがって、記事ごとの

```text
固定Narrative所属
sentiment
公開日
```

があれば、文献と整合的なNarrative Intensityを構築できます。

## 1.2 文献だけでは再現できない部分

文献には、347 NarrativeのライブラリがReuters、IPTC、Bloombergのニュースタグ、analyst interestなどから構築されたことは記載されていますが、記事をNarrativeへ分類する具体的なモデル、辞書、閾値、学習データは記載されていません。`Narrative Momentum(1).pdf`

したがって、

- **Intensityの集計式**は文献準拠で再現可能
- **MKT MediaStatsと完全に同一の記事分類**はPDFだけからは再現不可
- 記事分類器は、既存Notebookを利用して透明性のある代替手法を構築する

という整理になります。

なお、Narrative Mapsは、あるissueに関する記事集合から、coherenceとcoverageを考慮した時系列graphを抽出する手法です。固定Narrativeの定義・分類器ではありません。`2009.04508v2.pdf`

---

# 2. 固定Narrativeの定義

固定Narrativeでは、少なくとも次を満たす必要があります。

1. `narrative_id` が期間を通じて不変  
2. Narrativeの意味的定義が期間を通じて不変  
3. 各記事の分類は、その記事本文と固定定義だけで決まる  
4. 同一記事が複数Narrativeへ所属できる  
5. 分類器を変更する場合はmodel versionを分ける  
6. backtestでは、将来記事から得た情報を過去の分類へ使わない  

たとえば、

```text
N_MACRO_INFLATION
N_MACRO_RECESSION
N_POLICY_MONETARY
N_GEOPOL_ARMED_CONFLICT
N_TRADE_TENSION
```

のような恒久IDを付与します。

名称変更や説明文の改善があっても、意味が同じならIDは変更しません。

---

# 3. 固定Narrative Registry

固定Narrative一覧は、次のDataFrameとして保持するのが適切です。

```python
narrative_registry_df
```

## 推奨列

| 列 | 内容 |
|---|---|
| `narrative_id` | 永続的な一意ID |
| `narrative_name_ja` | 日本語名称 |
| `narrative_name_en` | 英語名称 |
| `description_ja` | 日本語での定義 |
| `description_en` | 英語での定義 |
| `parent_tag` | Macroeconomics、Technology等 |
| `parent_narrative_id` | 親Narrative |
| `evergreen_flag` | 長期継続Narrativeか |
| `aliases_ja` | 日本語表記揺れ |
| `aliases_en` | 英語表記揺れ |
| `include_phrases` | 高precisionの包含語句 |
| `exclude_phrases` | 除外語句・反例 |
| `include_entities` | 関連企業・機関・政策等 |
| `exclude_entities` | 除外entity |
| `positive_seed_article_ids` | 正例記事 |
| `negative_seed_article_ids` | 負例記事 |
| `valid_from` | 定義の有効開始日 |
| `valid_to` | 有効終了日 |
| `registry_version` | Registry版 |
| `classification_threshold` | Narrative固有の分類閾値 |

例としては次の形です。

```python
narrative_registry_df = pd.DataFrame([
    {
        "narrative_id": "N_MACRO_INFLATION",
        "narrative_name_ja": "インフレ",
        "narrative_name_en": "Inflation",
        "description_ja": (
            "財・サービス価格、賃金、期待インフレ、"
            "物価指数などを通じた一般物価水準の持続的上昇"
        ),
        "parent_tag": "Macroeconomics",
        "evergreen_flag": True,
        "aliases_ja": ["物価上昇", "インフレーション", "価格上昇"],
        "aliases_en": ["inflation", "price pressures", "rising prices"],
        "include_phrases": ["消費者物価", "CPI", "インフレ率"],
        "exclude_phrases": [],
        "positive_seed_article_ids": [],
        "negative_seed_article_ids": [],
        "valid_from": "2010-01-01",
        "valid_to": None,
        "registry_version": "v1.0",
        "classification_threshold": None,
    }
])
```

文献では53のevergreen NarrativeをJEL分類と対応させて定義し、Narrative選択の事後バイアスに対するrobustness testに利用しています。`Narrative Momentum.pdf`

したがって、最初の実装では、短期流行語よりも、

- Inflation
- Recession
- Monetary Policy
- Interest Rate
- Armed Conflict
- International Trade
- Climate Change
- Unemployment／Labor Market
- Housing Market
- Commodity Prices

のような長期継続型Narrativeから始めるのが合理的です。

---

# 4. 既存Notebookの再利用範囲

## 4.1 そのまま再利用できる処理

既存Notebookの以下は、固定Narrative版でも原則そのまま利用できます。

| 既存処理・関数 | 固定Narrative版での用途 |
|---|---|
| `normalize_text` | 日本語・英語テキスト正規化 |
| `extract_lead` | 本文冒頭抽出 |
| `parse_entities` | entity列の標準化 |
| `prepare_articles` | 入力QA、日付、ID、event text |
| `stable_hash` | article ID、cache fingerprint |
| `compute_embeddings` | multilingual embedding生成 |
| embedding cache | 再実行の高速化 |
| rejected rows管理 | 不正記事の隔離 |
| Parquet／NPZ保存 | 中間・最終結果保存 |
| `angular_similarity_from_cosine` | Narrative prototypeとのsemantic score |
| character n-gram TF-IDF | lexical relevance |
| weighted entity Jaccard | entity relevance |

既存の、

```text
event_text = headline + 本文冒頭5文
```

も固定Narrative分類のbaselineとしてそのまま使用できます。

## 4.2 修正して再利用する処理

### Duplicate処理

現在のNotebookは、exact duplicateとnear duplicateを一つのcanonical articleへ集約します。これはNarrative Mapには適切ですが、Intensityでは一部修正が必要です。

Intensityでは次を分けます。

| 種類 | 例 | Intensityでの処理 |
|---|---|---|
| technical duplicate | 同一URLの二重取得、同一sourceでの再取得 | 除外 |
| syndicated copy | Reuters記事が複数媒体へ転載 | 原則として各掲載を数える |
| semantic near duplicate | 同じ出来事を異なる媒体が独自報道 | 各記事を数える |

同じニュースが多数の媒体に掲載されること自体がmedia attentionだからです。

したがって、現在の、

```text
is_duplicate
canonical_article_id
duplicate_group_id
```

を、次に分けます。

```text
is_technical_duplicate
syndication_group_id
canonical_content_id
count_for_intensity
count_for_narrative_map
```

標準仕様は次です。

```text
Narrative Map:
    canonical contentのみ

Narrative Intensity:
    technical duplicateを除く全media items
```

embeddingはcanonical contentについて一度だけ計算し、そのscoreを同じsyndication groupの記事へ複製できます。

## 4.3 固定Narrative版では置き換える処理

以下は動的family discovery用なので、固定Narrative Intensityの主要処理には使用しません。

```text
build_sparsified_graph
partition_graph
consensus_community_detection
Louvain family IDs
HDBSCAN family baseline
compute_soft_memberships
```

特に現在の `compute_soft_memberships` は、全familyに対してsoftmaxを適用しています。

$$
\sum_n p_{a,n}=1
$$

となる相対的なmembershipです。しかし固定Narrativeでは、同一記事が、

```text
Inflation
Monetary Policy
Interest Rate
Recession
```

へ同時に関連し得るため、合計1の制約は不適切です。

固定Narrativeでは、Narrativeごとに独立した、

$$
q_{a,n}\in[0,1]
$$

を推定します。

$$
\sum_n q_{a,n}
$$

は1である必要はありません。

---

# 5. 固定Narrative分類器

## 5.1 推奨構成

分類器は次の三層とします。

```text
Layer 1：rule-based high-precision retrieval
Layer 2：embedding-based semantic classifier
Layer 3：optional reranker / verifier
```

最初の実装ではLayer 1とLayer 2で十分です。

## 5.2 Article-Narrative特徴量

記事 $a$、Narrative $n$ について次を計算します。

### Semantic relevance

固定Narrativeのdescription、aliases、正例記事からprototype embeddingを作ります。

$$
\mu_n
=
\operatorname{normalize}
\left(
\frac{1}{|P_n|}
\sum_{p\in P_n}e_p
\right)
$$

記事とのsemantic similarityは、

$$
S^{\mathrm{sem}}_{a,n}
=
1-\frac{\arccos(e_a^\top\mu_n)}{\pi}
$$

とします。

既存Notebookのangular similarityをそのまま再利用できます。

単一centroidだけでなく、正例記事上位 $k$ 件との類似度を使う方が多義的Narrativeに向きます。

$$
S^{+}_{a,n}
=
\operatorname{meanTopK}_{p\in P_n}
S(e_a,e_p)
$$

### Lexical relevance

Narrativeのaliases、include phrasesと記事のcharacter n-gramまたはBM25 similarityを計算します。

$$
S^{\mathrm{lex}}_{a,n}\in[0,1]
$$

既存Notebookのcharacter n-gram TF-IDF処理を転用できます。

### Entity relevance

$$
S^{\mathrm{ent}}_{a,n}
=
\operatorname{weighted\ Jaccard}
(E_a,E_n)
$$

既存のentity parserとweighted Jaccardを再利用します。

### Exclusion score

Narrativeに似ているが対象外の記事を除外するため、

$$
S^{\mathrm{excl}}_{a,n}
$$

を計算します。

例えばInflationであれば、

- 特定商品の一時的な値上げだけの記事
- 株価の「インフレ」という比喩表現
- 物価と無関係なinflationという用語

などをhard negativeとします。

## 5.3 分類score

初期版では、

$$
z_{a,n}
=
b_n
+
\beta_s S^{\mathrm{sem}}_{a,n}
+
\beta_l S^{\mathrm{lex}}_{a,n}
+
\beta_e S^{\mathrm{ent}}_{a,n}
-
\beta_x S^{\mathrm{excl}}_{a,n}
$$

$$
q_{a,n}=\sigma(z_{a,n})
$$

とします。

Narrative所属は、

$$
y_{a,n}
=
\mathbf 1(q_{a,n}\ge\theta_n)
$$

です。

$\theta_n$ は全Narrative共通ではなく、Narrativeごとに決めます。Inflationのように明確なNarrativeと、RiskやMarket Uncertaintyのように広いNarrativeでは、適切な閾値が異なるためです。

---

# 6. 学習データへの既存Stage 1の利用

既存のfamily discoveryは、固定Narrative分類器の**弱教師データ作成**に利用できます。

## 推奨フロー

### Step 1：過去の学習期間でStage 1を実行

```text
training_start
～
training_end
```

だけの記事で、既存Notebookを実行します。

### Step 2：familyを固定Narrativeへ対応付ける

次のmapping tableを作ります。

```text
family_run_id
narrative_id
mapping_type
mapping_confidence
reviewed_by
reviewed_at
```

一つのfamilyが複数Narrativeへ対応しても構いません。

### Step 3：正例候補を作る

既存Stage 1の、

- `core`
- representative article
- centroid score上位記事
- graph centrality上位記事

を固定Narrativeのpositive seed候補とします。

`peripheral`は追加学習候補、`ambiguous`は原則として人手確認対象とします。

### Step 4：hard negativeを作る

対象Narrativeとsemantic similarityは高いが、別Narrativeへ割り当てられた記事をhard negativeとします。

例えば、

```text
Inflation
vs
Interest Rate
vs
Monetary Policy
vs
Food Prices
```

のような近接Narrative間の判別に有効です。

### Step 5：固定分類器をfreeze

学習後は、

```text
registry_version
classifier_version
training_end_date
embedding_model_version
threshold_version
```

を固定します。

その後のIntensity期間では、LouvainやHDBSCANの再推定結果を記事分類に使いません。

---

# 7. 分類モデルの実装モード

一つのNotebookで次の3モードを切り替え可能にします。

## Mode A：`rules_prototype`

学習ラベルがほぼない場合の初期実装です。

```text
Narrative description
aliases
keywords
seed articles
```

からscoreを作ります。

長所は説明可能性と導入の容易さ、短所は閾値依存とNarrative間の識別力です。

## Mode B：`ovr_logistic`

推奨baselineです。

各Narrativeについて独立したone-vs-rest logistic regressionを推定します。

入力特徴量は、

```text
document embedding
semantic prototype score
lexical score
entity score
rule match
exclusion match
```

です。

Narrativeごとにbinary classifierを持つため、multi-label classificationになります。

## Mode C：`hybrid_reranker`

大量Narrative向けの拡張です。

1. embeddingで上位候補Narrativeを検索  
2. cross-encoderで記事とNarrative descriptionを再評価  
3. calibrated thresholdで判定  

計算負荷は高いですが、近接Narrativeの識別力が上がります。

最初の実装は、

```python
CLASSIFICATION_BACKEND = "rules_prototype"
```

とし、教師データの蓄積後に、

```python
CLASSIFICATION_BACKEND = "ovr_logistic"
```

へ移行するのが安全です。

---

# 8. 関連記事の抽出

既存の `make_stage2_input` は、動的family IDを使って記事を選択しています。これを固定Narrative版では次に置き換えます。

```python
related_df = get_fixed_narrative_articles(
    result=result,
    narrative_id="N_MACRO_INFLATION",
    start_date="2025-01-01",
    end_date="2025-12-31",
    min_relevance_score=None,   # Registryの閾値を利用
    include_syndicated=True,
    canonical_only=False,
)
```

出力は次のようにします。

```text
article_id
canonical_content_id
date
headline
body
source
url
narrative_id
relevance_score
relevance_flag
semantic_score
lexical_score
entity_score
rule_matches
exclusion_matches
sentiment_label
negative_probability
positive_probability
syndication_group_id
count_for_intensity
```

Narrative Mapに渡す場合は、

```python
map_input_df = make_fixed_narrative_map_input(
    result=result,
    narrative_id="N_MACRO_INFLATION",
    start_date="2025-01-01",
    end_date="2025-03-31",
    canonical_only=True,
)
```

とします。

Narrative Mapsは、あるissueに関するtime-stamped documentsが与えられた状態で、記事間のevent similarityとtopic similarityを用いてgraphを作る手法です。したがって、固定Narrative分類器で抽出した関連記事集合を、そのままNarrative Mapの入力にできます。`2009.04508v2.pdf`

重要なのは、

```text
Intensity計算：
    関連する全media itemsを使用

Narrative Map：
    canonical articleのみを使用
```

と分けることです。

---

# 9. Sentimentの推定

## 9.1 文献準拠baseline

文献ではLoughran-McDonaldに基づくnegative／positive sentiment記事を用いています。`Narrative Momentum(1).pdf`

英語記事については、辞書法をbaselineとして実装可能です。

日本語記事についてはLoughran-McDonald辞書を直接適用できないため、次の優先順位を推奨します。

```text
1. 既存のvendor sentiment
2. 日本語・多言語の金融sentiment model
3. 日本語金融辞書
4. general multilingual sentiment model
```

出力は共通化します。

```text
sentiment_label     # negative / positive / neutral
negative_probability
positive_probability
neutral_probability
sentiment_model_version
```

文献に近いIntensityではhard labelを使います。

## 9.2 Positive／overall系列

文献のbaselineはnegative intensityですが、positive intensityと、

$$
I^{\mathrm{overall}}=I^{-}+I^{+}
$$

でもrobustness testを行っています。`Narrative Momentum.pdf`

したがって、最低限次の4系列を作ります。

```text
intensity_negative
intensity_positive
intensity_overall       # negative + positive
intensity_coverage      # sentimentを問わない関連記事比率、追加診断
```

`intensity_coverage` は文献の主要定義ではなく、本実装独自の診断系列です。

---

# 10. Narrative Intensityの計算

## 10.1 文献準拠のhard intensity

日 $d$ の対象記事集合を $\mathcal A_d$、固定Narrative $n$ への所属を $y_{a,n}$、negative flagを $g_a^-$ とすると、

$$
I^-_{n,d}
=
\frac{
\sum_{a\in\mathcal A_d}
y_{a,n}g_a^-
}{
N_d
}
$$

ここで、

$$
N_d=|\mathcal A_d|
$$

は、その日に収集されたeligible media items総数です。

同様に、

$$
I^+_{n,d}
=
\frac{
\sum_{a\in\mathcal A_d}
y_{a,n}g_a^+
}{
N_d
}
$$

$$
I^{\mathrm{overall}}_{n,d}
=
I^-_{n,d}+I^+_{n,d}
$$

とします。

記事が複数Narrativeに属することは許容します。そのため、Narrative間でIntensityを合計しても1以下になるとは限りません。

## 10.2 月次Intensity

文献に合わせ、

$$
I^-_{n,m}
=
\frac{1}{|D_m|}
\sum_{d\in D_m}I^-_{n,d}
$$

とします。

**月間記事数の比率ではなく、日次Intensityの月平均**である点が重要です。`Narrative Momentum(1).pdf`

一般に、

$$
\frac{\sum_d \mathrm{numerator}_{n,d}}
{\sum_d N_d}
$$

とは一致しません。両方を保存してもよいですが、文献準拠列は日次平均にします。

## 10.3 Soft intensity

分類・sentimentの不確実性を反映する拡張として、

$$
\widetilde I^-_{n,d}
=
\frac{
\sum_{a\in\mathcal A_d}
q_{a,n}p_a^-
}{
N_d
}
$$

も計算可能です。

ただし、これは*Narrative Momentum*のhard countをsoft countへ拡張した独自仕様です。

保存列を明確に分けます。

```text
intensity_negative_hard   # 文献準拠baseline
intensity_negative_soft   # 拡張
```

---

# 11. 欠損値とゼロの区別

日次panelは全ての、

```text
date × narrative_id × reservoir
```

の組合せを作ります。

処理ルールは次です。

| 状態 | Intensity |
|---|---:|
| 当日に記事収集あり、該当Narrative記事なし | `0.0` |
| 当日に記事収集自体がない | `NaN` |
| そのreservoirが未稼働 | `NaN` |
| Narrative classifierの処理失敗 | `NaN` |
| 正常処理されたがnegative記事なし | `0.0` |

データ欠損をゼロにすると、source universeの変化がNarrative intensityの低下として誤認されます。

文献もpoint-in-timeで記事を収集し、実際にその時点で市場参加者が利用できた情報集合を重視しています。`Narrative Momentum(1).pdf`

---

# 12. Media reservoir

sourceまたはcategory情報がある場合は、文献同様にreservoir別系列を作ります。

文献では、

```text
General
Corporate
FX
Country Equity
```

の4reservoirを用いています。`Narrative Momentum.pdf`

ユーザーデータでは、例えば次の対応が考えられます。

```text
General
Corporate
FX
Rates
Commodities
Country Equity
Policy / Macro
```

reservoir $r$ のIntensityは、

$$
I^-_{n,d,r}
=
\frac{
\sum_{a\in\mathcal A_{d,r}}
y_{a,n}g_a^-
}{
N_{d,r}
}
$$

です。

Aggregateについては二つを保存します。

### Pooled aggregate

$$
I_{n,d}^{\mathrm{pooled}}
=
\frac{
\sum_r \mathrm{numerator}_{n,d,r}
}{
\sum_r N_{d,r}
}
$$

### Reservoir mean

$$
I_{n,d}^{\mathrm{reservoir\ mean}}
=
\frac{1}{|\mathcal R_d|}
\sum_{r\in\mathcal R_d}I_{n,d,r}
$$

文献のreservoir分析に合わせる場合は後者を主要系列にし、利用可能なreservoirだけを平均します。

---

# 13. Point-in-timeとlook-ahead防止

固定Narrative時系列には、次の二つのmodeを設けます。

## Descriptive mode

```text
全期間を用いてNarrative定義・classifierを作成
全期間を再分類
目的：歴史的なNarrative分析
```

これは説明・可視化には使えますが、運用backtestにはそのまま使えません。

## Backtest mode

```text
事前学習期間だけでNarrative Registryとclassifierを構築
out-of-sample期間はclassifierをfreeze
モデル更新はversionを切り替え
```

必須metadataは次です。

```text
registry_version
classifier_version
embedding_model_version
training_start
training_end
scoring_asof
created_at
```

Narrativeを全期間のattention spikeを見た後で追加するとselection biasが生じます。文献もこの問題を認識し、evergreen Narrativeや外部で定義されたNarrative setを用いてrobustnessを確認しています。`Narrative Momentum.pdf`

そのため、運用検証では、

```text
固定Narrative一覧：
    backtest開始前にfreeze

閾値：
    training／validation期間だけで推定

記事分類：
    記事公開時点で利用可能な本文のみ

Intensity：
    後日の訂正やbackfillを使わない
```

を守ります。

---

# 14. 新しいNotebookの推奨構成

新規Notebookは次を推奨します。

```text
02_fixed_narrative_intensity.ipynb
```

`.py`ファイルへ分離せず、一つのNotebookで完結させつつ、既存Notebookの関数を同一signatureで再利用します。

## セル構成

| Section | 処理 |
|---|---|
| 0 | 目的、文献定義、再現範囲 |
| 1 | Configuration |
| 2 | 固定Narrative Registry入力 |
| 3 | 記事DataFrame入力 |
| 4 | 既存の入力QA・テキスト正規化 |
| 5 | technical duplicate／syndication分離 |
| 6 | embedding生成・既存cache読込 |
| 7 | Stage 1結果のoptional読込 |
| 8 | positive／negative seed構築 |
| 9 | fixed multi-label classifier |
| 10 | Narrative固有閾値のcalibration |
| 11 | 全記事のNarrative score推定 |
| 12 | sentiment推定 |
| 13 | article-level long table生成 |
| 14 | 日次Intensity |
| 15 | reservoir別Intensity |
| 16 | 週次Intensity |
| 17 | 月次Intensity |
| 18 | Narrative関連記事抽出 |
| 19 | Narrative Map入力生成 |
| 20 | classifier診断 |
| 21 | Intensity診断 |
| 22 | QA・assertion |
| 23 | 保存 |
| 24 | 最終サマリー |

---

# 15. エントリポイント

中心関数は次の形にします。

```python
result = estimate_fixed_narrative_intensities(
    articles_df=articles_df,
    narrative_registry_df=narrative_registry_df,
    config=CONFIG,
    stage1_result=None,  # optional
)
```

結果オブジェクトは次です。

```python
@dataclass
class FixedNarrativeIntensityResult:
    articles: pd.DataFrame
    registry: pd.DataFrame
    assignments: pd.DataFrame
    sentiment: pd.DataFrame
    daily_intensity: pd.DataFrame
    weekly_intensity: pd.DataFrame
    monthly_intensity: pd.DataFrame
    related_articles: pd.DataFrame
    duplicate_mapping: pd.DataFrame
    classifier_diagnostics: pd.DataFrame
    intensity_diagnostics: pd.DataFrame
    qa_summary: pd.DataFrame
    embeddings: np.ndarray
    config: dict
    fingerprint: str
```

---

# 16. 主要出力

## `assignments`

long形式で保存します。

```text
article_id
canonical_content_id
date
narrative_id
semantic_score
lexical_score
entity_score
rule_score
exclusion_score
raw_score
calibrated_score
classification_threshold
relevance_flag
classifier_version
```

## `daily_intensity`

```text
date
narrative_id
reservoir
eligible_article_count
related_article_count
negative_related_count
positive_related_count
negative_soft_count
positive_soft_count
intensity_negative_hard
intensity_positive_hard
intensity_overall_hard
intensity_negative_soft
intensity_positive_soft
intensity_coverage
source_count
source_hhi
syndication_share
```

## `monthly_intensity`

```text
month
narrative_id
reservoir
intensity_negative
intensity_positive
intensity_overall
intensity_coverage
valid_days
mean_daily_article_count
```

## wide形式

Narrative betaなどの後続処理用に、

```python
monthly_intensity_wide = (
    monthly_intensity
    .pivot(
        index="month",
        columns="narrative_id",
        values="intensity_negative",
    )
)
```

も作ります。

---

# 17. 週次系列への接続

Narrative betaを後続で推定する場合は、文献と同様に木曜から翌水曜の日次Intensityを平均します。

$$
I_{n,w}
=
\frac{1}{|D_w|}
\sum_{d=\mathrm{Thu}(w-1)}^{\mathrm{Wed}(w)}
I_{n,d}
$$

$$
\Delta I_{n,w}
=
I_{n,w}-I_{n,w-1}
$$

文献は、この週次Intensity changeと水曜終値間の株式returnを対応させ、直近52週、最低26週でNarrative betaを推定しています。`Narrative Momentum(1).pdf`

そのため、今回のNotebookではポートフォリオ構築は行わなくても、

```text
weekly_intensity
weekly_intensity_change
```

まで生成しておくべきです。

---

# 18. Hierarchical Narrative

文献には347 Narrativeに加えて、Armed Conflict: IranやArmed Conflict: Ukraineのような682 sub-narrativesを使った検証があります。`Narrative Momentum.pdf`

したがって、Registryは階層対応にしておくと有用です。

```text
Armed Conflict
├── Armed Conflict: Ukraine
├── Armed Conflict: Middle East
└── Armed Conflict: Taiwan
```

親Narrativeのarticle flagを子Narrativeから作る場合は、

$$
y_{a,\mathrm{parent}}
=
\max_{c\in\mathrm{children}}
y_{a,c}
$$

とし、同じ記事が複数の子Narrativeに分類されても、親Narrativeでは一回だけ数えます。

ただし、

```text
direct_parent_classifier
child_aggregation
```

のどちらで作ったかをmetadataで区別します。

---

# 19. QA要件

## Article classifier

Narrativeごとに、

```text
precision
recall
F1
PR-AUC
positive article count
threshold
label prevalence
```

を計算します。

random splitだけでなく、必ずtemporal holdoutを使います。

```text
train period
validation period
test period
```

を時間順に分けます。

## Narrative overlap

Narrative間の、

```text
article overlap
Jaccard similarity
daily intensity correlation
monthly intensity correlation
```

を確認します。

極端に重複するNarrativeは、

- 定義の統合
- parent／child化
- exclusion rule追加

を検討します。

## Time-series QA

```text
Intensityが常にゼロでないか
特定sourceだけで構成されていないか
source数の変化とIntensityが連動していないか
syndication記事だけでspikeしていないか
分類score分布が時間とともにdriftしていないか
peak時の記事がNarrative定義と整合するか
```

を確認します。

## Point-in-time QA

```python
assert article_date <= scoring_asof
assert training_end < out_of_sample_start
assert registry_valid_from <= article_date
assert model_version is not null
```

を実装します。

---

# 20. 推奨する最小実装と最終仕様

## 最小実装

最初は以下で十分です。

```text
固定Narrative Registry
既存prepare_articles
既存embedding
prototype＋rule classifier
独立multi-label score
hard sentiment
hard daily intensity
monthly average
関連記事一覧
```

この段階で、

- Narrative定義
- classification threshold
- daily intensity
- monthly intensity
- 関連記事

を目視検証できます。

## 推奨最終仕様

その後、

```text
one-vs-rest classifier
Narrative別probability calibration
hard negative学習
reservoir別Intensity
technical duplicate／syndication分離
soft intensity
point-in-time model vintage
Narrative Map入力
```

を追加します。

---

# 最終的な接続方針

現在のStage 1との関係は、次のように整理するのが最も自然です。

$$
\boxed{
\begin{aligned}
\text{Stage 1 family discovery}
&\rightarrow
\text{固定Narrativeのseed・taxonomy監視}
\\
\text{Fixed Narrative classifier}
&\rightarrow
\text{全記事を独立multi-label分類}
\\
\text{Sentiment classifier}
&\rightarrow
\text{negative／positive flag}
\\
\text{Daily aggregation}
&\rightarrow
\text{文献準拠Narrative Intensity}
\\
\text{Fixed Narrative関連記事}
&\rightarrow
\text{Narrative Mapによる経路抽出}
\end{aligned}
}
$$

したがって、既存Notebookの動的community membershipからIntensityを直接作るのではなく、**既存Notebookを学習データ・関連記事・embedding基盤として再利用し、その上に固定Narrative Registryと独立multi-label classifierを追加する**のが適切です。

実装に必要な未確定入力は固定Narrative一覧です。既存の73テーマ等を利用する場合は、各テーマについて最低限、

```text
narrative_id
名称
定義文
aliases
include phrases
exclude phrases
```

をRegistryとして与え、既存Stage 1の代表記事をpositive seedとして補完する仕様が最も再現性と実務性のバランスが取れます。

## Response:
2026/8/2 23:14:12

> **思い出しています**
>
>
> **整理する要件**
>
> 既存実装の範囲を保ちながら、テーマ表、用語規則、DNFゲート、近接条件、競合処理、監査可能な出力を整理する。公開分類は72テーマを維持し、未確定の73番目は推測しない。
>
> **設計要件を確認中**
>
> 私は既存分類器の正確なテーマ定義と、増分処理の保存構造・状態管理・メモリ節約設計を確認している。特に73番目を捏造せず、監査証跡と日次集計へ安全につなげる方針だ。
>
> **要件を整理し設計要件を確認した**
>
> 
>
> **要件を定義中**
>
> 私は固定73テーマのナラティブ強度推定を、既存ルールと監査証跡を保った単一ノートブックへ整理する。増分更新、永続状態、日次集計、モメンタム指標、メモリ安全性を必須要件として明確化する。
>
> **要件を定義した**
>
> 
>
> **明確化 your 強度推定要件**
>
> 
>
> **最適化要件を整理中**
>
> 私は全件×73の密行列を避け、候補ベース・バッチ処理・日付分割保存を採用する。既定では陽性証拠のみ保存し、固定レジストリには未確定テーマ名を捏造せず、版管理と監査列を付与する。
>
> **最適化要件を整理した**
>
> 
>
> 15m 42s考えました

# 固定73 Narrative Intensity推定  
## メモリセーフ・増分更新版の実装要件定義

## 1. 方針の確定

新たに、以下のNotebookを1本で完結する形で実装します。

```text
02_fixed_narrative_intensity_incremental_memory_safe.ipynb
```

設計方針は次のとおりです。

```text
01_discover_narrative_families_incremental_memory_safe
    ├─ 日付パーティションStore
    ├─ SQLite状態管理
    ├─ 記事正規化
    ├─ technical duplicate管理
    ├─ embedding生成・cache
    ├─ model epoch管理
    └─ 増分更新・メモリ管理
             ↓ 最大限再利用

固定73 Narrative Registry
    ├─ 固定Narrative定義
    ├─ 日英語句ルール
    ├─ DNFゲート
    ├─ proximity条件
    ├─ exclusion／競合ルール
    └─ semantic prototype
             ↓

記事単位の独立multi-label分類
             ↓

negative / positive sentiment
             ↓

日次・週次・月次Narrative Intensity
             ↓

Narrative Momentum形成シグナル
             ↓

固定Narrative別の関連記事抽出
             ↓

Narrative Map入力DataFrame
```

*Narrative Momentum* は、多数の情報源から約350のNarrativeについてcoverage intensityを構築し、その変化とNarrative-mimicking portfolioを接続しています。`Narrative Momentum.pdf`

ただし今回使う73テーマは、同論文の347 Narrativeそのものではなく、同論文が関連研究として言及しているBhargava et al.の73テーマです。`Narrative Momentum.pdf`

---

# 2. 固定Narrative Registry

## 2.1 Registryの作成方針

既存の `narrative_73_multilabel_rule_classifier.ipynb` のテーマ定義、日英語句ルール、DNFゲート、近接条件、競合ルールを固定Narrative Registryとして採用します。

既存Notebookには、次が実装されています。

- 公開テーママスター
- 1,009件のterm rule
- 有効テーマすべてに対するpositive rule
- DNF形式のgate
- proximity rule
- hard／soft exclusion
- テーマ競合ルール
- 証跡付きmulti-label判定
- 日次・月次Intensity
- Narrative Momentum signal

既存Notebookのルール検証は、73行のtheme master、72の有効テーマ、1,009 term rules、8 proximity rulesという構成です。`narrative_73_multilabel_rule_classifier.ipynb`

## 2.2 73番目のテーマの扱い

重要な制約として、既存Notebookが参照した公開資料では「73テーマ」と記載されている一方、機械可読な公開付録から固有名を確認できたのは72件でした。既存実装は名称を推測せず、73番目を無効なplaceholderとして保持しています。`narrative_73_multilabel_rule_classifier.ipynb`

したがってRegistryは次の状態で固定します。

```text
Registry総行数      : 73
有効Narrative       : 72
無効Narrative       : 1
```

無効行は次のとおりです。

```text
sequence          : 73
narrative_id      : unresolved_public_theme_73
name_en           : UNRESOLVED PUBLIC THEME #73
name_ja           : 公開付録から名称を確認できなかった73番目のテーマ
active            : False
classification    : 対象外
intensity         : 出力対象外
```

この行を推測で有効化しません。既存実装も同じ方針で、名称未確認・分類対象外のplaceholderを保持しています。`narrative_73_multilabel_rule_classifier.ipynb`

## 2.3 作成済みRegistry

固定Narrative Registryを以下の2形式で作成しました。

- [固定Narrative Registryマスター](sandbox:/mnt/data/fixed_narrative_registry_73_master_v1.json)
- [固定Narrative Registry＋全ルール定義](sandbox:/mnt/data/fixed_narrative_registry_73_public_v1.json)

後者には次が含まれます。

```text
73 Narrative master records
72 active Narratives
1 inactive unresolved placeholder
1,009 term rules
DNF clause definitions
8 proximity rules
theme conflict rules
classification / review thresholds
scope_in / scope_out
valid_from / valid_to
rule version
```

## 2.4 Narrative IDポリシー

既存プログラムとの互換性を優先し、既存の `theme_id` をそのまま固定 `narrative_id` として使用します。

例：

```text
inflation
interest_rates
federal_reserve
recession
market_crash
international_conflicts
trade_war
commodity
treasury_bonds
```

名称変更が必要になっても `narrative_id` は変更しません。表示名と定義だけをversion管理します。

---

# 3. Notebookの役割と実装範囲

## 3.1 実装対象

本Notebookは以下を一括して実装します。

1. 固定Narrative Registryの読込・生成・検証
2. 既存Stage 1 Storeの再利用
3. 記事の新規・訂正・削除・遅延到着処理
4. technical duplicateとsyndicationの分離
5. 日英ニュースのテキスト正規化
6. 固定Narrativeへのmulti-label分類
7. ルール判定証跡の保存
8. semantic relevance scoreの付与
9. sentimentの統合
10. 日次Narrative Intensity
11. reservoir別・Aggregate Intensity
12. 木曜～水曜の週次Intensity
13. 月次Intensity
14. Narrative Momentum形成シグナル
15. 固定Narrative別の関連記事抽出
16. Narrative Map向けDataFrameのmaterialize
17. point-in-time版とlatest revised版の両方の管理
18. メモリ・品質・増分更新診断
19. Parquet／JSON／SQLiteへの保存

## 3.2 対象外

以下は本Notebookには含めません。

- Narrative beta推定
- 株式のNarrative-mimicking portfolio
- Narrative Momentumのポートフォリオバックテスト
- Narrative MapのLP最適化
- main route／alternative routeの抽出
- LLM APIによる自動ラベル付け
- 73番目の未確認Narrativeの推測

これらは後続Notebookの対象とします。

---

# 4. 既存メモリセーフNotebookからの再利用

## 4.1 再利用するコンポーネント

| 既存コンポーネント | 新Notebookでの用途 |
|---|---|
| `PartitionedNewsStore` | raw、features、embeddingsの日次保存 |
| `StateDB`の基本設計 | SQLiteによるwatermark・run・article管理 |
| `prepare_incoming_records` | 入力列検証、時刻統一、version管理 |
| `build_features` | headline、lead、hash、event text |
| `TextModelManager` | embedding model epoch管理 |
| `ensure_embeddings` | 新規・変更記事のみembedding生成 |
| `exact_hash_registry` | 過去を含むtechnical duplicate検出 |
| 日付partition upsert | 記事訂正・遅延到着の反映 |
| `.npy`＋index | embeddingのmemory map読込 |
| `log_memory` | RSS監視 |
| run history | 実行履歴、fingerprint、watermark |
| Stage 2 export | 固定Narrative別の関連記事exportへ改修 |

既存メモリセーフNotebookは、raw、features、embeddings、assignmentsを日付partitionで分離し、embeddingを日次の連続 `.npy` とindexで保存して必要な範囲だけmemory mapする構造です。SQLiteには記事、membership、run history、hash index等を保持します。`01_discover_narrative_families_incremental_memory_safe.ipynb`

## 4.2 再利用しない処理

固定Narrativeの推定には以下を使用しません。

```text
Louvain community detection
consensus community
dynamic family UID
family split / merge
HDBSCAN family baseline
article-to-family softmax
Narrative family component closure
```

固定Narrative分類は、記事 $a$ と固定Narrative $n$ の関係、

$$
y_{a,n}\in\{0,1\}
$$

を記事単位で独立に推定するためです。

したがって、動的familyのように、新規記事が過去記事間のcommunity構造を変更することはありません。

## 4.3 増分更新範囲の変更

既存Stage 1では、

```text
changed articles
+ temporal neighborhood
+ touched connected-component closure
```

を再計算対象にしています。`01_discover_narrative_families_incremental_memory_safe.ipynb`

固定Narrative版では次へ簡略化します。

```text
changed / inserted / deleted article
        ↓
当該記事の分類だけ再計算
        ↓
影響したpublished_dateだけ日次集計を再計算
        ↓
該当週・該当月・関連formation signalだけ再計算
```

これは固定分類器がarticle-localであることによる設計上の変更です。

---

# 5. 共有Storeと新規Store

## 5.1 Store構造

```text
<store_root>/
├── data/
│   ├── raw/
│   │   └── published_date=YYYY-MM-DD/
│   ├── features/
│   │   └── model_epoch=<epoch>/
│   │       └── published_date=YYYY-MM-DD/
│   └── embeddings/
│       └── model_epoch=<epoch>/
│           └── published_date=YYYY-MM-DD/
│
├── fixed_narratives/
│   ├── registry/
│   │   └── registry_version=public-73-v1.0.0/
│   ├── rules/
│   │   └── rule_version=public-73-v1.0.0/
│   ├── prototypes/
│   │   └── classification_epoch=<epoch>/
│   ├── assignments/
│   │   └── classification_epoch=<epoch>/
│   │       └── published_date=YYYY-MM-DD/
│   ├── evidence/
│   │   └── classification_epoch=<epoch>/
│   │       └── published_date=YYYY-MM-DD/
│   ├── sentiment/
│   │   └── sentiment_epoch=<epoch>/
│   │       └── published_date=YYYY-MM-DD/
│   ├── intensity/
│   │   ├── daily/
│   │   ├── weekly/
│   │   └── monthly/
│   ├── signals/
│   │   ├── J=1/
│   │   ├── J=3/
│   │   ├── J=6/
│   │   ├── J=9/
│   │   └── J=12/
│   ├── related_articles/
│   ├── runs/
│   │   └── <run_id>/
│   └── state/
│       └── fixed_narrative.sqlite
```

`data/raw`、`data/features`、`data/embeddings` はStage 1と共有します。固定NarrativeのassignmentやIntensityは別namespaceに保存し、動的family結果を上書きしません。

---

# 6. Model epochとVersion管理

## 6.1 Classification epoch

次の組合せから `classification_epoch` を生成します。

$$
\text{classification epoch}
=
H(
\text{text preprocessing version},
\text{embedding model epoch},
\text{registry version},
\text{rule version},
\text{prototype version},
\text{classifier version}
)
$$

例：

```text
text_preprocessing_version = text-v1
embedding_model_epoch      = 2026Q3
registry_version           = public-73-v1.0.0
rule_version               = public-73-v1.0.0
prototype_version          = prototype-v1.0
classifier_version         = fixed-multilabel-v1.0
```

## 6.2 epoch内で固定するもの

同一epoch内では以下を変更しません。

- テキスト正規化
- embedding model
- registry定義
- term rule
- gate rule
- proximity rule
- conflict rule
- threshold
- semantic prototype
- sentiment model

既存メモリセーフ実装も、model epoch内ではembedding fallbackモデルとcharacter n-gramモデルを固定し、週次更新では新規記事をtransformするだけの設計です。`01_discover_narrative_families_incremental_memory_safe.ipynb`

いずれかを変更した場合は新epochを発行し、

```text
rebuild_epoch
```

を実行します。

---

# 7. 入力データ要件

## 7.1 必須列

```text
date
headline
body
```

## 7.2 推奨列

```text
article_id
source
url
language
entities
ingested_at
record_version
is_deleted
reservoir
sentiment_label
negative_probability
positive_probability
neutral_probability
```

既存メモリセーフNotebookも、`date`、`headline`、`body`を必須とし、`ingested_at`、`record_version`、`is_deleted`等を任意列として扱います。`date`と`ingested_at`を分離することで、遅延到着記事を正しく扱います。`01_discover_narrative_families_incremental_memory_safe.ipynb`

## 7.3 正規化後の列

```text
article_id
published_at
published_date
ingested_at
record_version
is_deleted

headline
body
source
url
language
entities
reservoir

normalized_headline
normalized_body
title_fold
body_fold
lead_text
event_text

content_hash
event_text_hash
url_hash
exact_content_hash
```

## 7.4 event text

embedding用の標準textは既存仕様を維持します。

```text
event_text = headline + 本文冒頭5文
```

ただしrule classifierは、既存73テーマ分類器と整合させるため、タイトルと本文全体を別々に検索します。

```text
Embedding:
    headline + lead

Rule matching:
    headline + full body
```

全文は日次partitionまたはarticle batch単位でのみ読み込み、全履歴本文を一括保持しません。

Narrative Maps論文もheadlineを基本単位としつつ、full text、summary、headlineとleadの利用を今後の拡張として挙げています。`2009.04508v2.pdf`

---

# 8. Duplicateの定義

Narrative MapとNarrative Intensityではduplicateの意味が異なるため、次の3種類を区別します。

## 8.1 Technical duplicate

例：

- 同じURLを同一sourceから重複取得
- 同一記事versionの二重登録
- 同一`article_id`・`record_version`
- 同一source、同一時刻、同一content hash

処理：

```text
Intensity numerator   : 除外
Intensity denominator : 除外
Narrative Map         : 除外
```

## 8.2 Syndicated copy

例：

- Reuters原稿が複数媒体に転載
- 同じwire storyが異なるsourceから掲載

処理：

```text
Intensity baseline    : 各media itemを数える
Narrative Map         : canonical contentのみ
```

複数媒体への拡散そのものがmedia attentionを表すためです。

## 8.3 Semantic near duplicate

異なる媒体が同じ出来事を独自に報じた記事です。

```text
Intensity             : 各記事を数える
Narrative Map         : canonical化を選択可能
```

## 8.4 必須出力列

```text
is_technical_duplicate
technical_duplicate_reason
canonical_content_id
syndication_group_id
near_duplicate_group_id
count_for_intensity
count_for_narrative_map
```

robustness用に以下のIntensityも保持します。

```text
media_item_weighted
canonical_content_weighted
source_capped
syndication_group_capped
```

---

# 9. 固定Narrative分類

## 9.1 Multi-label条件

同一記事が複数Narrativeへ分類されることを許可します。

例：

```text
Inflation
Interest Rates
Federal Reserve
Treasury Bonds
Risk
```

したがって、

$$
\sum_n y_{a,n}
$$

は1に制約しません。

動的family版のsoftmax membershipは使用しません。

## 9.2 Baseline分類器

Baselineは既存のrule-based classifierをそのまま移植します。

処理は次のとおりです。

1. 日英phrase／regexを事前compile
2. タイトルと本文を別々に検索
3. concept groupごとのscoreを計算
4. DNF gateを評価
5. 同一文・文字距離等のproximity条件を評価
6. hard exclusionを適用
7. soft exclusion penaltyを適用
8. Narrative別thresholdを適用
9. theme conflict ruleを適用
10. 判定証跡を保存

既存実装のscoreは概念groupの最大一致、title／body倍率、repeat bonus、proximity bonus、soft exclusion penaltyからなり、hard exclusion成立時にはscoreに関係なく棄却します。`narrative_73_multilabel_rule_classifier.ipynb`

## 9.3 判定ステータス

```text
matched
manual_review
hard_exclusion
gate_failed
proximity_failed
score_below_threshold
inactive_narrative
```

## 9.4 Semantic relevance

既存のembeddingを再利用し、記事とNarrative prototypeとの関連度を計算します。

Narrative $n$ のprototypeを、

$$
\mu_n
=
\operatorname{normalize}
\left(
\frac{1}{|P_n|}
\sum_{p\in P_n}e_p
\right)
$$

とし、

$$
S^{sem}_{a,n}
=
1-
\frac{
\arccos(
\operatorname{clip}(e_a^\top\mu_n,-1,1)
)
}{\pi}
$$

を計算します。

prototypeは次から生成します。

```text
Narrative英語名
Narrative日本語名
definition
scope_in
直接語句ルールの代表語
人手確認済みpositive seed記事
```

## 9.5 Semantic scoreの用途

初期版ではsemantic scoreだけでhard matchにしません。

```text
rule matched
    → hard match

rule review + semantic high
    → manual review上位

rule no match + semantic very high
    → semantic_candidate

semantic only
    → soft intensityまたは関連記事rankingに限定
```

人手ラベルが蓄積した後、one-vs-rest classifierへ切替可能にします。

## 9.6 証跡

各article×Narrative判定には以下を保存します。

```text
matched_rule_ids
matched_terms
matched_sentences
matched_fields
matched_concept_groups
selected_clause
proximity_evidence
hard_exclusion_evidence
soft_exclusion_evidence
rule_score
semantic_score
final_score
threshold
decision
classifier_version
```

---

# 10. メモリセーフな分類処理

## 10.1 禁止事項

以下を禁止します。

```text
全履歴を一つのDataFrameへ読込
全記事×全ルールのDataFrame cross join
全記事×全Narrativeのlong tableを一括生成
N×N article similarity matrix
全raw rule matchをメモリ保持
全embeddingの通常ndarray一括読込
```

## 10.2 Batch処理

```python
ARTICLE_BATCH_SIZE = 2_000
EMBEDDING_BATCH_SIZE = 64
SEMANTIC_SCORE_BATCH_SIZE = 2_048
RULE_EVIDENCE_FLUSH_ROWS = 100_000
ASSIGNMENT_FLUSH_ROWS = 100_000
```

72 Narrativeのprototype matrixは小さいため、常時メモリ保持できます。

記事batch $B$ とNarrative数 $M=72$ に対して、

```text
article embeddings : B × d
prototype matrix   : 72 × d
score matrix       : B × 72
```

のみを生成します。

`B=2,048` の場合、`float32` のscore matrixは約0.6MBです。

## 10.3 Rule evidenceの保存

標準では次だけを保存します。

```text
matched
manual_review
semantic_candidate
hard_exclusion
```

`gate_failed`や`score_below_threshold`の全件証跡は保存しません。必要な場合だけdebug modeで特定記事・Narrativeを再評価します。

## 10.4 既存メモリ管理の踏襲

既存メモリセーフ版では、

- kNN queryのbatch化
- typed arrayによるedge保存
- lexical similarityのchunk化
- online consensus集計
- NetworkX属性の最小化

が採用されています。`01_discover_narrative_families_incremental_memory_safe.ipynb`

固定Narrative版にはedge／communityはありませんが、同じ原則で、

```text
batch compute
partition flush
mmap read
float32
必要列のみ読込
明示的del + gc.collect()
RSS logging
```

を適用します。

---

# 11. Sentiment要件

## 11.1 対応モード

```python
SENTIMENT_MODE = {
    "provided_label",
    "provided_probability",
    "dictionary",
    "model",
    "none",
}
```

優先順位は次です。

1. vendor等が提供するsentiment probability
2. vendor等が提供するsentiment label
3. 固定versionの日本語・英語金融sentiment model
4. 固定辞書
5. sentimentなし

## 11.2 出力

```text
sentiment_label
negative_probability
positive_probability
neutral_probability
sentiment_is_labeled
sentiment_model_version
```

## 11.3 sentimentがない場合

sentimentが利用できない場合も、

```text
coverage_intensity
```

は計算します。

negative／positive Intensityは `NaN` とし、未分類をneutralとして扱いません。

---

# 12. Narrative Intensity

## 12.1 日次hard Intensity

日 $d$、reservoir $r$、Narrative $n$ に対して、

$$
I^{-}_{n,d,r}
=
\frac{
\sum_{a\in\mathcal A_{d,r}}
\mathbf 1(y_{a,n}=1)
\mathbf 1(s_a=\mathrm{negative})
}{
N_{d,r}
}
$$

$$
I^{+}_{n,d,r}
=
\frac{
\sum_{a\in\mathcal A_{d,r}}
\mathbf 1(y_{a,n}=1)
\mathbf 1(s_a=\mathrm{positive})
}{
N_{d,r}
}
$$

とします。

ここで、

$$
N_{d,r}
$$

は、当該日・reservoirのtechnical duplicateと削除記事を除く全eligible media items数です。

*Narrative Momentum* も、日次negative／positive intensityを、該当Narrativeのnegative／positive記事数の全media discussionに対する比率とし、月次値を日次値の平均として構築しています。`Narrative Momentum.pdf`

## 12.2 その他の系列

```text
coverage_intensity
    = Narrativeに関連する全記事数 / 全記事数

overall_sentiment_intensity
    = negative_intensity + positive_intensity

negative_intensity_hard
positive_intensity_hard
```

`coverage_intensity` と `overall_sentiment_intensity` は別列にします。

## 12.3 Soft Intensity

拡張系列として、

$$
\widetilde I^{-}_{n,d,r}
=
\frac{
\sum_{a\in\mathcal A_{d,r}}
q_{a,n}p^{-}_{a}
}{
N_{d,r}
}
$$

も計算可能にします。

ただし、文献準拠baselineはhard countです。

```text
negative_intensity_hard   ← baseline
negative_intensity_soft   ← robustness
```

---

# 13. Reservoir

## 13.1 基本reservoir

入力に存在すれば、以下を標準とします。

```text
General
Corporate
FX
Country Equity
```

必要に応じて、

```text
Rates
Commodities
Policy / Macro
```

を追加できます。

入力に `reservoir` がない場合は、全件を、

```text
General
```

として扱います。

## 13.2 Aggregate

各日に利用可能なreservoir集合を $\mathcal R_d$ として、

$$
I_{n,d}^{Aggregate}
=
\frac{1}{|\mathcal R_d|}
\sum_{r\in\mathcal R_d}I_{n,d,r}
$$

を文献準拠のAggregateとします。

データがないreservoirはゼロではなくmissingです。

論文はGeneral、Corporate、FX、Country Equityの4reservoirを使用し、利用可能なreservoirのIntensityを平均したAggregateを主要系列としています。データ収集開始前のreservoirはmissingとして扱われます。`Narrative Momentum.pdf`

比較用に次も保存します。

$$
I_{n,d}^{Pooled}
=
\frac{
\sum_r n_{n,d,r}
}{
\sum_r N_{d,r}
}
$$

---

# 14. 週次・月次Intensity

## 14.1 週次

Narrative betaへの接続を見越し、木曜から翌水曜までを一週とします。

$$
I_{n,w}
=
\frac{1}{|D_w|}
\sum_{d\in D_w}I_{n,d}
$$

$$
\Delta I_{n,w}
=
I_{n,w}-I_{n,w-1}
$$

論文も木曜から水曜の日次Intensityを平均し、水曜終値間の株式returnと対応させています。`Narrative Momentum.pdf`

## 14.2 月次

$$
I_{n,m}
=
\frac{1}{|D_m|}
\sum_{d\in D_m}I_{n,d}
$$

とします。

月間の総記事数比率ではなく、日次Intensityの単純平均です。既存73テーマclassifierも同じ定義です。`narrative_73_multilabel_rule_classifier.ipynb`

次の2系列を保存します。

```text
monthly_intensity_full
monthly_intensity_signal_cutoff
```

`signal_cutoff`版は、翌月初に形成するsignalで月末最終日の記事を使わない仕様です。

---

# 15. Narrative Momentum形成シグナル

look-back $J$ に対して、

$$
M^{(J)}_{n,t}
=
\frac{1}{J}
\sum_{j=1}^{J}I_{n,t-j}
-
\frac{1}{J}
\sum_{j=J+1}^{2J}I_{n,t-j}
$$

を計算します。

```python
LOOKBACK_MONTHS = (1, 3, 6, 9, 12)
DEFAULT_INTENSITY_COLUMN = "negative_intensity_hard"
```

出力には次を含めます。

```text
formation_month
narrative_id
lookback_months
recent_average_intensity
previous_average_intensity
narrative_momentum_signal
eligible_flag
available_months
```

---

# 16. Incremental update

## 16.1 実行モード

```text
initial_full
weekly_incremental
rebuild_epoch
reaggregate
classify_only
```

## 16.2 更新順序

```text
1. watermarkより後のingested_atを取得
2. article_id × record_versionで新規・訂正・削除を特定
3. raw partitionへupsert
4. 対象記事だけfeature／embeddingを生成
5. 対象記事だけ固定Narrative分類
6. assignment／evidenceをupsert
7. 影響したpublished_dateを特定
8. 当該日の日次Intensityを全72 Narrativeについて再集計
9. 対応する週次・月次partitionを再集計
10. 影響を受けるNarrative Momentum signalを再計算
11. SQLiteとrun manifestをcommit
12. watermarkを更新
```

## 16.3 過去月の変更がsignalへ及ぼす範囲

月 $m$ のIntensityが変更された場合、look-back $J$ のsignalでは、

$$
t\in[m+1,m+2J]
$$

のformation monthが影響を受けます。

したがって最大 $J=12$ の場合、変更月から最大24か月先のsignal partitionを再計算します。

## 16.4 訂正・削除

記事訂正または削除では、以前のassignmentを残したまま追加してはいけません。

処理は次です。

```text
旧version assignmentを無効化
新versionを分類
affected dateを再集計
```

daily partition全体を再集計するため、複雑なmanual counter reversalは不要です。

---

# 17. Point-in-time管理

## 17.1 2種類の系列

```text
latest_revised
point_in_time
```

を分けます。

### latest_revised

後日到着・訂正・削除を反映した最新の歴史系列です。

### point_in_time

各signal形成日時点で実際に到着済みだった記事だけを使います。

$$
\mathrm{ingested\_at}
\le
\mathrm{signal\_cutoff}
$$

を必須条件とします。

既存73テーマclassifierも、本番signalでは `ingested_at <= signal_cutoff` を要求しています。`narrative_73_multilabel_rule_classifier.ipynb`

また、*Narrative Momentum* はarchiveのbackfillではなく、各時点で取得したpoint-in-time記事集合を重視しています。`Narrative Momentum.pdf`

## 17.2 Version ledger

SQLiteにはcurrent tableとは別にappend-only ledgerを保持します。

```text
article_version_ledger
assignment_version_ledger
signal_vintage
```

これにより、過去時点の情報集合を復元可能にします。

---

# 18. SQLite要件

## 18.1 テーブル

```text
meta

narrative_registry
narrative_registry_history
rule_manifest
prototype_manifest
classifier_epoch

article_classifier_state
article_version_ledger

article_narrative_current
article_narrative_history

technical_duplicate_registry
syndication_registry

sentiment_current
sentiment_history

intensity_partition_state
signal_vintage

manual_review_queue
run_history
```

## 18.2 主なindex

```text
article_id
published_date
ingested_at
narrative_id
reservoir
classification_epoch
registry_version
decision
signal_cutoff
```

## 18.3 WAL

既存メモリセーフNotebookと同様に、

```sql
PRAGMA journal_mode=WAL;
```

を使用します。既存StateDBもWAL、article registry、hash registry、membership、family、run historyを持つ設計です。`01_discover_narrative_families_incremental_memory_safe.ipynb`

---

# 19. 固定Narrative関連記事

## 19.1 取得関数

```python
related_df = get_fixed_narrative_articles(
    store_root=CONFIG["store_root"],
    classification_epoch=CONFIG["classification_epoch"],
    narrative_id="inflation",
    start_date="2025-01-01",
    end_date="2025-12-31",
    decisions=("matched", "manual_review"),
    min_semantic_score=None,
    canonical_only=False,
    include_syndicated=True,
    columns=None,
)
```

## 19.2 出力列

```text
article_id
canonical_content_id
published_at
headline
body
source
url
reservoir
language

narrative_id
decision
rule_score
semantic_score
final_score
threshold

sentiment_label
negative_probability
positive_probability

matched_rule_ids
matched_terms
evidence_json

syndication_group_id
count_for_intensity
count_for_narrative_map
```

## 19.3 Narrative Map用export

```python
map_df = export_fixed_narrative_map_bundle(
    narrative_id="inflation",
    start_date="2025-01-01",
    end_date="2025-03-31",
    canonical_only=True,
    decisions=("matched",),
    max_articles=1_000,
)
```

出力列：

```text
article_id
date
headline
body
event_text
source
url
embedding
narrative_id
relevance_score
sentiment_label
```

Narrative Mapsは、「特定issueに関する時刻付きdocument集合」を入力として、event similarityとtopic similarityに基づくstoryline graphを構築します。固定Narrative classifierで関連記事を抽出すれば、この前提を満たせます。`2009.04508v2.pdf`

---

# 20. 出力オブジェクト

```python
@dataclass
class FixedNarrativeIntensityUpdateResult:
    run_id: str
    run_mode: str
    classification_epoch: str

    registry: pd.DataFrame
    changed_articles: pd.DataFrame
    assignments: pd.DataFrame
    evidence: pd.DataFrame
    sentiment: pd.DataFrame

    daily_intensity: pd.DataFrame
    weekly_intensity: pd.DataFrame
    monthly_intensity: pd.DataFrame
    momentum_signals: pd.DataFrame

    review_queue: pd.DataFrame
    classifier_diagnostics: pd.DataFrame
    intensity_diagnostics: pd.DataFrame
    qa_summary: pd.DataFrame
    update_report: pd.DataFrame

    config: dict
    store_root: Path
    fingerprint: str
```

---

# 21. 出力DataFrame

## 21.1 `assignments`

```text
article_id
record_version
published_at
ingested_at

narrative_id
decision
is_match

rule_score
semantic_score
final_score
classification_threshold
review_threshold

matched_rule_count
matched_groups
selected_clause
hard_exclusion_flag

registry_version
rule_version
prototype_version
classifier_version
classification_epoch
scored_at
```

## 21.2 `daily_intensity`

```text
date
narrative_id
reservoir

n_total
n_theme
n_theme_negative
n_theme_positive

coverage_intensity
negative_intensity_hard
positive_intensity_hard
overall_sentiment_intensity_hard

negative_intensity_soft
positive_intensity_soft

n_sources
source_hhi
syndication_share
sentiment_coverage

as_of_cutoff
series_type
classification_epoch
```

## 21.3 `weekly_intensity`

```text
week_end_wednesday
narrative_id
reservoir
negative_intensity
positive_intensity
coverage_intensity
negative_intensity_change
valid_days
```

## 21.4 `monthly_intensity`

```text
month
narrative_id
reservoir

negative_intensity
positive_intensity
overall_sentiment_intensity
coverage_intensity

valid_days
mean_daily_article_count
sentiment_coverage
series_type
```

## 21.5 `momentum_signals`

```text
formation_month
narrative_id
lookback_months
recent_average_intensity
previous_average_intensity
narrative_momentum_signal
eligible_flag
```

---

# 22. Notebook構成

| Section | 内容 |
|---|---|
| 0 | 目的・文献との関係・再現限界 |
| 1 | Imports・Configuration |
| 2 | 固定Narrative Registryの生成・読込 |
| 3 | Registry・ruleの静的検証 |
| 4 | 共通日付partition Store |
| 5 | Fixed Narrative用SQLite |
| 6 | 入力QA・version・watermark |
| 7 | テキスト正規化・technical duplicate |
| 8 | embeddingのreuse／生成 |
| 9 | rule compiler |
| 10 | prototype生成 |
| 11 | memory-safe multi-label分類 |
| 12 | conflict resolution |
| 13 | sentiment統合 |
| 14 | assignment／evidence保存 |
| 15 | 日次Intensity |
| 16 | reservoir Aggregate |
| 17 | 週次Intensity |
| 18 | 月次Intensity |
| 19 | Momentum signal |
| 20 | 関連記事抽出 |
| 21 | Narrative Map bundle export |
| 22 | classifier診断 |
| 23 | Intensity診断 |
| 24 | point-in-time監査 |
| 25 | QA・assertions |
| 26 | deterministic incremental demo |
| 27 | 実データ運用テンプレート |
| 28 | Definition of Done |

各処理セルの前に以下を記載します。

```text
処理目的
入力
主要パラメータ
出力
メモリ上の注意
point-in-time上の注意
```

---

# 23. 推奨Configuration

```python
CONFIG = {
    # Reproducibility
    "random_seed": 42,

    # Shared Stage 1 store
    "store_root": "outputs/narrative_family_store",
    "reuse_stage1_raw_features_embeddings": True,
    "embedding_model_epoch": "2026Q3",

    # Fixed registry
    "registry_path": "fixed_narrative_registry_73_public_v1.json",
    "registry_version": "public-73-v1.0.0",
    "rule_version": "public-73-v1.0.0",
    "classifier_version": "fixed-multilabel-v1.0.0",

    # Input
    "date_col": "date",
    "headline_col": "headline",
    "body_col": "body",
    "article_id_col": None,
    "source_col": "source",
    "url_col": "url",
    "language_col": "language",
    "entities_col": "entities",
    "reservoir_col": "reservoir",
    "ingested_at_col": "ingested_at",
    "record_version_col": "record_version",
    "is_deleted_col": "is_deleted",

    # Text
    "lead_sentences": 5,
    "max_body_chars_for_embedding": 4_000,
    "use_full_body_for_rules": True,

    # Memory
    "article_batch_size": 2_000,
    "embedding_batch_size": 64,
    "semantic_score_batch_size": 2_048,
    "rule_evidence_flush_rows": 100_000,
    "assignment_flush_rows": 100_000,
    "embedding_dtype": "float32",
    "log_memory_usage": True,
    "rss_warning_gib": None,

    # Classifier
    "classification_mode": "rule_plus_semantic_review",
    "return_all_narrative_scores": False,
    "store_decisions": (
        "matched",
        "manual_review",
        "semantic_candidate",
        "hard_exclusion",
    ),
    "semantic_candidate_top_k": 5,
    "semantic_candidate_threshold": 0.70,

    # Duplicate
    "technical_duplicate_policy": "exclude",
    "syndication_intensity_policy": "count_media_items",
    "narrative_map_duplicate_policy": "canonical_only",

    # Sentiment
    "sentiment_mode": "provided_probability",
    "negative_label_threshold": 0.50,
    "positive_label_threshold": 0.50,

    # Reservoir
    "default_reservoir": "General",
    "aggregate_method": "mean_available_reservoirs",

    # Intensity
    "daily_timezone": "Asia/Tokyo",
    "weekly_alignment": "thu_to_wed",
    "monthly_signal_exclude_last_day": True,
    "momentum_lookbacks": (1, 3, 6, 9, 12),
    "baseline_intensity": "negative_intensity_hard",

    # Point in time
    "series_types": ("latest_revised", "point_in_time"),
    "require_ingested_at_for_pit": True,

    # Outputs
    "save_evidence": True,
    "save_soft_intensity": True,
    "save_related_article_index": True,
    "strict_qa": False,
}
```

---

# 24. QA要件

## 24.1 Registry QA

```text
Registryが73行
active=Trueが72件
inactive placeholderが1件
narrative_idが一意
全active Narrativeにpositive ruleが存在
全active Narrativeにgateが存在
gateが実在するconcept groupを参照
rule_idが一意
proximity ruleの参照先が有効
valid_from <= valid_to
```

既存classifierも同じ73行・72有効という静的検証を行っています。`narrative_73_multilabel_rule_classifier.ipynb`

## 24.2 Classification QA

```text
multi-labelを許容している
Narrative間softmaxを使っていない
hard exclusionがthresholdより優先される
inactive Narrativeにmatchが存在しない
matched判定にはevidenceが存在
同一article×Narrative×versionが一意
```

## 24.3 Intensity QA

Narrativeごとに、

$$
0\le I_{n,d,r}\le1
$$

を確認します。

ただしmulti-labelなので、

$$
\sum_n I_{n,d,r}
$$

は1を超えても問題ありません。

さらに、

```text
n_theme_negative <= n_theme <= n_total
n_theme_positive <= n_theme <= n_total
technical duplicateがdenominatorに含まれない
データ収集あり・matchなしは0
データ収集なしはNaN
利用前reservoirはNaN
```

を検証します。

## 24.4 Incremental QA

同一入力・同一epochについて、

```text
initial_full結果
=
週次incrementalを順番に適用した最終結果
```

となることをdeterministic demoで確認します。

## 24.5 Point-in-time QA

```python
assert published_at <= signal_cutoff or allow_future_publication is False
assert ingested_at <= signal_cutoff
assert registry_valid_from <= signal_cutoff
assert classifier_training_end < out_of_sample_start
```

を確認します。

## 24.6 メモリQA

各主要段階でRSSを記録します。

```text
before ingest
after feature generation
after embedding
after rule classification
after semantic scoring
after daily aggregation
after persistence
```

また、

```text
全履歴DataFrameが作られていない
N×N行列が作られていない
embeddingがmmapである
evidenceがchunk flushされている
```

ことを診断表に出します。

---

# 25. Definition of Done

実装完了条件は以下です。

1. 一つのNotebookで完結する  
2. `.py`ファイルを生成しない  
3. `uv`管理環境で実行できる  
4. 既存Stage 1のraw、features、embeddingsを再利用できる  
5. 固定Registryが73行、72 active、1 inactiveである  
6. 全active Narrativeが独立multi-label分類される  
7. 全履歴を一括読込しない  
8. article batch単位で分類される  
9. embeddingは日付partitionからmmapされる  
10. technical duplicateとsyndicationが分離される  
11. rule evidenceが監査可能である  
12. 日次negative／positive／coverage Intensityが得られる  
13. reservoir別およびAggregate系列が得られる  
14. 木曜～水曜の週次系列が得られる  
15. 月次系列が日次Intensityの平均である  
16. $J=1,3,6,9,12$ のMomentum signalが得られる  
17. point-in-timeとlatest revisedが区別される  
18. 新規・訂正・削除・遅延記事を増分更新できる  
19. incremental結果とfull rebuild結果が一致する  
20. 固定Narrativeごとの関連記事を抽出できる  
21. Narrative Map用DataFrameをfamily全履歴なしでmaterializeできる  
22. 最終セルにPASS／WARN／FAILのQA summaryを表示する  

---

# 26. 実装上の未確定事項

現時点で次を明示的な未確定事項として残します。

| 項目 | 初期仕様 |
|---|---|
| 73番目のNarrative名 | 不明のためinactive |
| sentiment source | 入力列を優先 |
| reservoir mapping | 入力列、なければGeneral |
| MKTの347 taxonomyとの対応 | 対応させない |
| MKT独自分類器との一致 | 保証しない |
| semantic-only hard match | 初期版では使用しない |
| ルール閾値 | 既存値を初期値とし人手評価で更新 |
| soft intensity | robustness系列 |
| Narrative Map対象記事数上限 | 設定可能、標準1,000件 |

既存73テーマclassifierも、公開情報に整合する高precision proxyであり、MKT MediaStatsのproprietary分類結果との完全一致は保証しないと明記しています。`narrative_73_multilabel_rule_classifier.ipynb`

この要件では、**固定73行のRegistry、既存ルール分類器、Stage 1のメモリセーフStoreを統合し、72の有効固定Narrativeについて、監査可能かつpoint-in-time対応の日次・週次・月次Intensityを増分推定する**構成とします。

## Response:
2026/8/2 23:35:40

> **必要ファイルと参照ノートブックを確認**
>
>
> **設計範囲を整理中**
>
> 私は、完全なノートブックを一つにまとめ、規則レジストリを外部JSONまたは埋め込みデータから読み込む構成を検討しています。実行例と増分同値性のテストも含め、扱いやすさとのバランスを取ります。
>
> **設計範囲を整理した**
>
> 
>
> **固定ナラティブ分類システムの実装**
>
>
> **分類処理を設計中**
>
> 私は、バッチ分類で各記事に規則を適用し、候補テーマだけ意味的に評価する構成を考えています。大量処理に備え、文字列による事前絞り込み、競合解決、根拠表、感情分析の簡易フォールバックも組み込みます。
>
> **分類処理を設計した**
>
> 
>
> **再構築を設計中**
>
> 私は、最新改訂版と指定時点版の両方を扱えるよう、取得時刻付きの履歴を保存し、指定時点以前の最新版を選ぶ方式を考えています。分類履歴も保持し、期間別強度を再現可能にします。
>
> **再構築を設計した**
>
> 
>
> **実装方針を固める**
>
> 私は、実行可能なノートブック内に埋め込みレジストリ、デモ用ストア、最新版・指定時点版の処理、診断表示をまとめる方針です。外部ファイルへの依存を減らし、再現性を優先します。
>
> **実装方針を確定した**
>
> 
>
> **実装済み固定ナラティブ強度ノートブック**
>
>
> **実装ノートブックとレジストリを圧縮**
>
> 
>
> 52m 26s考えました

実装しました。Notebookは**単体完結・実行済み**で、固定Narrative RegistryもNotebook内にgzip＋base64形式で内蔵しています。外部JSONがなくても動作し、外部Registryを指定した運用にも切り替えられます。

## 成果物

- [実装済み・実行済みNotebook](sandbox:/mnt/data/02_fixed_narrative_intensity_incremental_memory_safe_implemented.ipynb)
- [固定Narrative Registryマスター](sandbox:/mnt/data/fixed_narrative_registry_73_master_v1.json)
- [固定Narrative Registry＋全ルール](sandbox:/mnt/data/fixed_narrative_registry_73_public_v1.json)
- [Notebook・Registry一式のZIP](sandbox:/mnt/data/fixed_narrative_intensity_implementation_bundle.zip)

## 実装した主要機能

### 固定Narrative Registry

Registryは以下の構成で固定しています。

| 項目 | 実装値 |
|---|---:|
| Registry総行数 | 73 |
| Active Narrative | 72 |
| Inactive placeholder | 1 |
| Term rules | 1,009 |
| Proximity rules | 8 |
| Narrative conflict rules | 1 |
| 分類方式 | 独立multi-label |

73番目は名称を推測せず、`unresolved_public_theme_73`としてinactiveのまま保持しています。既存classifierが採用していた、73行・72 active・1,009 term rulesという定義を維持しました。`narrative_73_multilabel_rule_classifier.ipynb` `narrative_73_multilabel_rule_classifier.ipynb`

### メモリセーフな永続Store

以下を実装しました。

- raw current／raw historyの分離
- feature current／feature historyの分離
- 日付partition
- SQLite WAL
- append-only article state ledger
- embeddingの日付別 `.npy` shard
- embedding indexによる必要行のみのmemory map
- assignment current／history
- sentiment current／history
- daily／weekly／monthly／signal partition
- run manifest、watermark、fingerprint

設計は参照Notebookの、raw・features・embeddings・assignmentsを分離し、embeddingを日付別配列とindexで管理する構造を踏襲しています。`01_discover_narrative_families_incremental_memory_safe.ipynb`

### 増分更新

次のrun modeを実装しています。

```python
initial_full
weekly_incremental
rebuild_epoch
reaggregate
classify_only
```

週次更新では、同一`article_id`について以下のいずれかが変化した記事だけを再処理します。

- content version
- record version
- 削除状態
- 本文またはタイトル
- classification epoch

更新対象は記事単位で完結し、動的communityのように全connected componentを再計算しません。記事訂正や削除によって影響した公開日、週、月だけを再集計します。

### Duplicate／syndication

次を区別しています。

- **Technical duplicate**  
  同一source・同一content・短い時間窓内の重複。Intensityの分子・分母から除外。
- **Syndicated copy**  
  異なるsourceによる同一原稿。媒体ごとにIntensityへ算入。
- **Semantic near duplicate**  
  Narrative Map canonicalization用の任意機能。既定値は無効。

同じ定型文が数か月後に再登場した場合までduplicateにしないよう、exact duplicate判定には時間窓を設けています。

### Multi-label rule engine

各記事について次を順に処理します。

1. タイトル・本文の日英phrase match
2. concept group score
3. DNF gate
4. 同一文・文字距離proximity
5. hard exclusion
6. soft exclusion penalty
7. Narrative別threshold
8. Narrative競合解決
9. semantic review candidate
10. evidence保存

semantic scoreはhard matchの代替には使わず、次の用途に限定しています。

- manual reviewの優先順位付け
- ruleで捕捉できなかった記事のsemantic candidate
- soft Intensity
- 関連記事ranking

### Narrative Intensity

以下を実装しています。

$$
I^-_{n,d,r}
=
\frac{
N(\text{Narrative }n \cap \text{negative})
}{
N(\text{当日・reservoirのeligible media items})
}
$$

出力系列は次のとおりです。

- `coverage_intensity`
- `negative_intensity_hard`
- `positive_intensity_hard`
- `overall_sentiment_intensity_hard`
- `negative_intensity_soft`
- `positive_intensity_soft`
- `coverage_intensity_soft`

月次Intensityは日次Intensityの平均、週次Intensityは木曜から翌水曜の日次値の平均です。これはNarrative Momentum論文のIntensityおよびweekly alignmentに対応しています。`narrative_73_multilabel_rule_classifier.ipynb` `narrative_73_multilabel_rule_classifier.ipynb`

Reservoirは次に対応します。

```text
General
Corporate
FX
Country Equity
```

Aggregateは、当日に利用可能なreservoirの等ウェイト平均です。利用開始前またはデータのないreservoirをゼロとは扱いません。

### Narrative Momentum形成シグナル

次を実装しています。

$$
M^{(J)}_{n,t}
=
\frac{1}{J}\sum_{j=1}^{J}I_{n,t-j}
-
\frac{1}{J}\sum_{j=J+1}^{2J}I_{n,t-j}
$$

対応look-backは、

```python
J = (1, 3, 6, 9, 12)
```

です。月末最終日の記事を除く`signal_cutoff`系列と、通常の`full`系列を別々に保存します。

### Point-in-time

以下の条件で任意cutoff時点の情報集合を復元します。

$$
\text{state\_effective\_at}
\le
\text{as\_of\_cutoff}
$$

記事訂正、削除、duplicate stateの変更はappend-only ledgerに保存されるため、`latest_revised`と`point_in_time`を分離できます。論文が重視する、archiveの事後修正ではなく各時点の実際の情報集合に対応した構造です。`narrative_73_multilabel_rule_classifier.ipynb`

### Narrative Map接続

固定Narrative ID、期間、decisionを指定し、関連記事だけを抽出できます。

```python
related_df = get_fixed_narrative_articles(
    store_root=...,
    classification_epoch=...,
    narrative_id="inflation",
    start_date="2024-01-01",
    end_date="2025-12-31",
    decisions=("matched", "manual_review"),
)
```

また、以下を含むNarrative Map bundleを出力します。

```text
関連記事表
embedding matrix
embedding index
manifest
```

Narrative Mapsの後段が必要とする「特定issueに関するtime-stamped documents」とembeddingをmaterializeする構造です。start event、end eventの選択とst-graph最適化は後段Notebookの責務として分離しています。`narrative_73_multilabel_rule_classifier.ipynb`

## 実行済みテスト結果

Notebook内で30か月のdeterministic demoを実行しています。demoには次を含めました。

- inflation
- interest rates
- Federal Reserve
- buybacks
- trade war
- unrelated news
- 記事訂正
- 記事削除
- technical duplicate
- cross-source syndication
- no-change update
- point-in-time vintage
- Narrative Map bundle export

最終検証結果は以下です。

| 検証 | 結果 |
|---|---:|
| Incremental／full rebuildのcurrent assignments | 一致 |
| Incremental／full rebuildの日次系列構造 | 一致 |
| 日次Intensity最大絶対差 | 0.0 |
| Registry静的検証 | PASS |
| article×Narrative一意性 | PASS |
| inactive Narrativeへのmatch | 0 |
| Intensity範囲 `[0,1]` | PASS |
| count関係 | PASS |
| no-change update | 正常終了 |
| PIT系列materialization | 正常終了 |
| Narrative Map bundle | 出力済み |

Notebook内のdemoでは外部Registryを参照せず、**内蔵Registryだけを使用して最後まで実行**しています。外部ファイル依存なしで再実行可能です。

## Response:
2026/8/3 22:49:33

> **確認しながら進める**
>
> 指定された研究室名は表記が曖昧なので、まず正式名称を確認し、次にノートブックの構成と既存処理を調べます。そのうえで、口コミ分類や表記ゆれ対応を壊さず実装方針を整えます。
>
> **確認しながら進めた**
>
> 
>
> **東大泉研究室 日本語 極性辞書 xlsx 語彙 スコアを検索中**
>
> [トランプ大統領なぜ日本を支援？日米28年ぶり“協調介入”](https://news.tv-asahi.co.jp/news_economy/articles/900196393.html)
> [日本語評価極性辞書（名詞編）](https://www.cl.ecei.tohoku.ac.jp/Open_Resources-Japanese_Sentiment_Polarity_Dictionary.html)
> [投稿者による評価スコアや感情極性辞書を用いない](https://proceedings-of-deim.github.io/DEIM2022/papers/C34-3.pdf)
> [AIによるテキスト情報の解析（極性辞書の作成編）](https://www.nam.co.jp/news/mpdf/181012_tk.pdf)
> [感情分析に用いる極性辞書を自動生成する](https://qiita.com/g-k/items/1b7c765fa6520297ca7c)
> [金融専門極性辞書 - Izumi-Lab](https://sites.google.com/socsim.org/izumi-lab/tools/%E9%87%91%E8%9E%8D%E5%B0%82%E9%96%80%E6%A5%B5%E6%80%A7%E8%BE%9E%E6%9B%B8)
> [osetiによる日本語の感情分析｜npaka](https://note.com/npaka/n/n3c7722d2e4bc)
> [擬似アノテーションにもとづく日本語ツイートの極性判定](https://db-event.jpn.org/deim2019/post/papers/238.pdf)
> [状況別感情極性日本語辞書の作成とその活用](https://www.anlp.jp/proceedings/annual_meeting/2022/pdf_dir/D1-4.pdf)
> [日本](https://ja.wikipedia.org/wiki/%E6%97%A5%E6%9C%AC)
>
> **10件のウェブサイトを検索しました**
>
> 
>
> **東大泉研究室辞書対応の感情分析調査**
>
>
> **DEFAULT_CONFIG感情設定の続き抽出**
>
> 
>
> **ウェブを検索中**
>
> [Izumi-Lab - 金融専門極性辞書](https://sites.google.com/socsim.org/izumi-lab/tools/%E9%87%91%E8%9E%8D%E5%B0%82%E9%96%80%E6%A5%B5%E6%80%A7%E8%BE%9E%E6%9B%B8)
> [Open Resources/Japanese Sentiment Polarity Dictionary - 東北大学 乾研究室 / Inui Lab, Tohoku University](https://www.cl.ecei.tohoku.ac.jp/Open_Resources-Japanese_Sentiment_Polarity_Dictionary.html)
>
> **実装方針を整理**
>
> XLSX辞書を設定で指定できるようにし、語彙列とスコア列を読み込みます。既存分類処理は維持しつつ、辞書スコアの合計からラベル判定する形で、差分を小さく実装します。
>
> **実装方針を整理した**
>
> 
>
> **マッチング方式を検討**
>
> 辞書は一度だけ読み込み、表記を正規化します。重複語彙は平均スコアで統合し、追加依存を避けるため、まずは日本語フレーズを直接検索する実装にします。
>
> **マッチング方式を検討した**
>
> 
>
> **2件のウェブサイトを検索しました**
>
> 
>
> **推論呼び出し箇所の調査**
>
>
> **設計を具体化**
>
> 設定に辞書パス、シート、語彙列、スコア列を追加し、辞書変更時には再分類できるよう識別情報も更新します。追加依存を避け、トライ木で日本語語彙を効率的に照合します。
>
> **設計を具体化した**
>
> 
>
> **推論呼び出し箇所を調査した**
>
> 
>
> **思い出しています**
>
>
> **確認し、ノートブック制約や実装と依存関係を整理しました**
>
> 
>
> 31m 20s考えました

実装しました。既存Notebookの**固定Narrative分類、増分更新、SQLite、日次・週次・月次Intensity、PIT処理は変更せず**、センチメント付与部分だけをXLSX形式の日本語極性辞書へ置き換えています。

## 成果物

- [日本語極性辞書対応・実行済みNotebook](sandbox:/mnt/data/02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb)
- [入力形式確認用の極性辞書サンプルXLSX](sandbox:/mnt/data/demo_japanese_polarity_dictionary.xlsx)
- [Notebook・Registry・サンプル辞書一式](sandbox:/mnt/data/fixed_narrative_intensity_japanese_polarity_xlsx_bundle.zip)

サンプルXLSXは動作確認用であり、東大・和泉研究室の実辞書そのものではありません。和泉研究室の公式ページでは「金融専門極性辞書」として配布案内されています。([Google](https://sites.google.com/socsim.org/izumi-lab/tools/%E9%87%91%E8%9E%8D%E5%B0%82%E9%96%80%E6%A5%B5%E6%80%A7%E8%BE%9E%E6%9B%B8?utm_source=chatgpt.com))

# 変更内容

## 1. センチメントモード

既定値を次へ変更しました。

```python
"sentiment_mode": "japanese_polarity_xlsx"
```

実データでは、以下だけ設定すれば利用できます。

```python
PRODUCTION_CONFIG = finalize_config({
    # 既存設定
    "store_root": "/path/to/persistent/narrative_store",
    "registry_path": "fixed_narrative_registry_73_public_v1.json",

    # 日本語極性辞書
    "sentiment_mode": "japanese_polarity_xlsx",
    "sentiment_dictionary_path": "/path/to/japanese_polarity_dictionary.xlsx",
    "sentiment_dictionary_sheet_name": 0,
    "sentiment_dictionary_word_col": "語彙",
    "sentiment_dictionary_score_col": "スコア",

    # 以下は通常そのままでよい
    "sentiment_dictionary_neutral_threshold": 0.0,
    "sentiment_dictionary_probability_scale": "auto",
    "sentiment_dictionary_text_field": "event_text",
})
```

実際の列名が異なる場合は、例えば次のように変更できます。

```python
"sentiment_dictionary_word_col": "単語",
"sentiment_dictionary_score_col": "極性値",
```

列名を`None`にすると、次のような候補から自動判定します。

```text
語彙側：
語彙、単語、表現、見出し語、用語、word、term、vocabulary

スコア側：
スコア、極性値、極性、評価極性値、感情極性値、
score、polarity、value
```

## 2. XLSXの読込

XLSXは次により読み込みます。

```python
pd.read_excel(
    dictionary_path,
    sheet_name=...,
    header=...,
    engine="openpyxl",
)
```

そのため、環境に`openpyxl`がない場合だけNotebook外で追加します。

```bash
uv add openpyxl
```

Notebook内で`pip install`は行いません。

## 3. 辞書の前処理

辞書には次の処理を行います。

1. 語彙をUnicode NFKC正規化
2. 英字をcasefold
3. スコアを数値変換
4. 欠損語彙・非数値スコアを除外
5. スコア0の語彙を除外
6. 同一語彙の重複を統合
7. 最短語彙長を下回るものを除外
8. trieへ変換して一度だけキャッシュ

同一語彙の重複は、既定では平均します。

```python
"sentiment_dictionary_duplicate_aggregation": "mean"
```

以下にも切り替えられます。

```text
mean     ：平均
sum      ：合計
max_abs  ：絶対値が最大のスコア
```

## 4. 記事センチメントスコア

記事 $a$ の極性スコアは、記事中で一致した語彙のスコア合計です。

$$
S_a
=
\sum_{w\in\mathcal M(a)}
p_w
$$

ここで、

- $\mathcal M(a)$：記事中で一致した辞書語彙
- $p_w$：XLSXに記載された符号付き極性スコア

です。

判定は次のとおりです。

$$
\mathrm{label}_a
=
\begin{cases}
\mathrm{positive}, & S_a>\theta\\
\mathrm{negative}, & S_a<-\theta\\
\mathrm{neutral}, & |S_a|\le\theta
\end{cases}
$$

既定値は、

```python
"sentiment_dictionary_neutral_threshold": 0.0
```

です。したがって、正味スコアが正ならpositive、負ならnegative、ゼロならneutralです。

## 5. 語彙一致方法

既定では、

```python
"sentiment_dictionary_match_mode": "longest_nonoverlap"
```

を使います。

例えば辞書に、

```text
悪
悪化
```

の両方がある場合、「悪化」には長い方の「悪化」だけを適用します。また、一つの文字範囲を複数回重複計上しません。

全文×全語彙のcross joinや巨大な文書語彙行列は作らず、辞書をtrie化して記事を逐次走査するため、既存のメモリセーフ設計を維持しています。

なお、最小変更版であるため、現段階では以下は追加していません。

```text
形態素解析による原形化
否定表現による極性反転
係り受け解析
文脈別の語義判定
```

辞書に原形だけが収録されている場合、活用形まで網羅したいときは、後続拡張としてSudachi等による原形化を追加する余地があります。

# 既存処理との互換性

下流処理が使用する列は変更していません。

```text
sentiment_label
negative_probability
positive_probability
neutral_probability
sentiment_is_labeled
sentiment_model_version
scored_at
```

したがって、以下は既存実装のままです。

- `sentiment_current`
- `sentiment_history`
- daily Narrative Intensity
- reservoir別Intensity
- Aggregate Intensity
- 木曜～水曜の週次Intensity
- 月次Intensity
- Narrative Momentum形成シグナル
- point-in-time series
- related article抽出
- Narrative Map bundle

*Narrative Momentum*が定義する、日次negative／positive intensityを「当該Narrativeに関連するnegative／positive記事の全記事に対する比率」とし、月次値を日次値の月平均とする集計方法も変更していません。`narrative_73_multilabel_rule_classifier.ipynb`

# Probabilityへの変換

既存のsoft intensityを維持するため、符号付きscoreからnegative／positive／neutral probabilityも生成します。

辞書内のpositive scoreの絶対量を $P_a$、negative scoreの絶対量を $N_a$ とすると、

$$
T_a=P_a+N_a
$$

$$
p_a^{neutral}
=
\exp\left(-\frac{T_a}{\lambda}\right)
$$

$$
p_a^{positive}
=
(1-p_a^{neutral})
\frac{P_a}{T_a}
$$

$$
p_a^{negative}
=
(1-p_a^{neutral})
\frac{N_a}{T_a}
$$

です。

$\lambda$は既定で辞書スコア絶対値の中央値から自動設定されます。

```python
"sentiment_dictionary_probability_scale": "auto"
```

hard intensityでは`sentiment_label`を、soft intensityではこの確率を使用します。

# Dictionary version管理

辞書ファイルのSHA-256を計算し、

```text
sentiment_model_version
classification_epoch
```

へ組み込みました。

例えば、

```text
japanese-polarity-xlsx-5f3a33ee7977
```

のようなversionになります。

したがって、XLSXの内容を変更すると、

```text
辞書fingerprint変更
→ classification_epoch変更
→ 旧辞書と新辞書の結果を混在させない
```

という動作になります。

同じ辞書を別ディレクトリへコピーしただけで内容が同一なら、同じfingerprintになります。

# 動作確認

内蔵した小規模XLSX辞書を使ってNotebook全体を実行し、以下を確認しました。

| 検証 | 結果 |
|---|---:|
| Notebook実行エラー | 0 |
| Registry行数 | 73 |
| Active Narrative | 72 |
| Dictionary有効語彙 | 15（demo） |
| Incremental／full rebuild assignment | 一致 |
| Incremental／full rebuild daily structure | 一致 |
| Daily Intensity最大絶対差 | 0.0 |
| Inactive Narrativeへのmatch | 0 |
| Intensity範囲 | PASS |
| Point-in-time処理 | PASS |
| Narrative Map bundle | PASS |
| 最大RSS | 約0.28 GiB |

demoでは、例えば次の判定を確認しています。

```text
「景気の悪化が懸念され、市場リスクが高まる」
    → negative

「業績が改善し、株主還元を強化する」
    → positive

「地域イベントが開催された」
    → neutral
```

---

**Sources:**

- [金融専門極性辞書 - Izumi-Lab](https://sites.google.com/socsim.org/izumi-lab/tools/%E9%87%91%E8%9E%8D%E5%B0%82%E9%96%80%E6%A5%B5%E6%80%A7%E8%BE%9E%E6%9B%B8?utm_source=chatgpt.com)

## Response:
2026/8/4 8:13:51

> **確認している実装**
>
> 実装では、記事ごとにナラティブ分類を直接付与しています。記事同士をクラスタリングして家族単位でラベルを決める処理は使っておらず、グループ列は主に集計用です。
>
> **実装を確認した**
>
> 
>
> **ノートブックを検索し記事を分類、関連フレーズを抽出**
>
> 
>
> 4m 42s考えました

現在の実装は、**記事を一件ずつ固定Narrativeへ直接紐づける方式**です。  
`Narrative family`のように記事群を先にクラスタリングし、そのクラスタを固定Narrativeへ対応付ける処理は、固定Narrative Intensityの本番分類経路には入っていません。

## 現在の処理フロー

記事 $a$ ごとに、72個の有効な固定Narrative $n$ に対して独立に、

$$
y_{a,n}\in\{0,1\}
$$

を判定します。

```text
記事1件
  ↓
headline・本文の正規化
  ↓
固定Narrativeの語句ルールを検索
  ↓
DNF gate・近接条件・除外条件を評価
  ↓
Narrative別thresholdを評価
  ↓
必要に応じてNarrative間の競合解決
  ↓
article × Narrativeのmulti-label assignment
```

Notebookにも、固定Narrativeへの「rule-based hard match＋semantic review candidate」を行い、全記事×全Narrativeの巨大なlong tableは作らず、rule hitがあるNarrativeとsemantic上位候補だけを保存する仕様が記載されています。semantic similarityは `batch_size × 72` の行列として計算されます。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb` `02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

したがって、一つの記事が例えば、

```text
inflation
interest_rates
federal_reserve
treasury_bonds
```

の4つに同時に紐づくことがあります。これは排他的クラスタリングではなく、**独立multi-label分類**です。Notebookでは `article × Narrative` のassignment一意性をQA対象としています。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# Narrative familyはどこで使われているか

`01_discover_narrative_families_incremental_memory_safe` から再利用しているのは主に基盤部分です。

```text
日付partition
SQLite状態管理
記事正規化
embedding
technical duplicate管理
増分更新
メモリ管理
```

一方、以下は固定Narrative分類には使用していません。

```text
Louvain community detection
HDBSCANによるfamily抽出
family UID
family split / merge
family単位のsoft membership
```

つまり、

```text
記事
→ Narrative family
→ 固定Narrative
```

ではなく、

```text
記事
→ 固定Narrative
```

です。

`01`のfamily discovery結果と固定Narrativeのassignment結果は、別の系統として保存されます。

---

# Semantic similarityの位置づけ

semantic similarityも、記事をクラスタリングするためには使っていません。

各固定Narrativeには、名称、定義、代表語、seed等から作ったprototype embeddingがあり、記事embeddingとの類似度を計算します。

$$
S^{\mathrm{sem}}_{a,n}
=
1-
\frac{
\arccos
\left(
e_a^\top \mu_n
\right)
}{\pi}
$$

ただし現行のbaselineでは、

```text
rule条件を満たす
    → hard match

ruleでは確定しないがsemantic similarityが高い
    → manual_review / semantic_candidate

semantic similarityだけが高い
    → 自動的にはhard matchにしない
```

という仕様です。Notebookにも、hard matchはruleで決定し、semantic-only candidateを自動hard matchへ昇格させないと明記されています。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# Duplicate groupingとの違い

実装にはduplicate groupやsyndication groupがありますが、これはNarrative familyではありません。

| grouping | 目的 |
|---|---|
| technical duplicate | 二重取得を分母・分子から除外 |
| syndication group | 同一wire記事の媒体間転載を識別 |
| near-duplicate group | Narrative Map用に類似記事をcanonical化 |
| Narrative family | 関連記事群から動的episodeを発見 |

`near-duplicate`も既定では無効で、固定Narrativeへの分類単位は各media itemです。Notebookではsemantic near-duplicateはNarrative Map canonicalization用の任意機能とされています。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# Intensityへの集計

記事単位で得たassignmentを日次に集約します。

Narrative $n$ のnegative intensityは、

$$
I^{-}_{n,d}
=
\frac{
\sum_{a\in\mathcal A_d}
\mathbf 1(y_{a,n}=1)
\mathbf 1(s_a=\mathrm{negative})
}{
N_d
}
$$

です。

したがって、クラスタ数やfamily規模ではなく、**その日に固定Narrativeへ分類された記事数**が分子になります。

これはNarrative Momentum論文の、各Narrativeの日次attentionを「そのNarrativeに関する記事の全記事に対する割合」とする設計に対応しています。論文では347 Narrativeへ記事を分類し、日次negative／positive intensityを、Narrativeに関連するnegative／positive記事の全media discussionに占める割合として計算しています。`narrative_73_multilabel_rule_classifier.ipynb` `narrative_73_multilabel_rule_classifier.ipynb`

---

# 両方式の関係

現在の設計は次のように役割を分けています。

```text
固定Narrative分類
    記事単位
    長期的に同じNarrative ID
    Intensity時系列の計算に使用

Narrative family discovery
    記事群単位
    期間内のepisodeを動的に発見
    新しい話題・sub-narrative・関連記事群の探索に使用

Narrative Map
    固定Narrativeに分類済みの記事集合を入力
    その中で経路・storylineを抽出
```

Narrative Maps論文でも、入力は「ある特定issueに関するtime-stamped documents」であり、その記事集合の内部でtopic clusteringとstoryline graphを構築します。したがって、クラスタリングは固定Narrativeへの一次分類ではなく、すでにスコープされた記事集合の内部構造を抽出する段階です。`narrative_73_multilabel_rule_classifier.ipynb`

## 現行実装の要約

$$
\boxed{
\text{記事単位で固定Narrativeへ直接multi-label分類}
}
$$

であり、

$$
\boxed{
\text{Narrative familyを介した二段階分類ではない}
}
$$

というのが回答です。Family discoveryは、固定Narrative分類の代替ではなく、未捕捉episodeの発見やNarrative Map構築の補助として別系統で利用する設計です。

## Response:
2026/8/4 20:47:00

> **原因を確認中**
>
> SQLite のディスク I/O エラーは、保存先の権限・容量・一時ファイル領域・同時接続が主な候補です。まずノートブック内の DB 初期化と更新処理を確認し、最小限の修正箇所を特定します。
>
> **原因を確認した**
>
> 
>
> **FixedNarrativeStateDBのSQLite I/Oエラー診断**
>
>
> **原因を絞り込み中**
>
> 今回の失敗箇所は、履歴レコードを書き込む段階に集中している可能性があります。WAL 非対応の保存先、容量不足、権限、同時アクセス、巨大な JSON、既存ファイル破損を順に確認します。
>
> **原因を絞り込みました**
>
> 
>
> **Diagnosing and fixing SQLite database locking error**
>
>
> **修正方針を整理中**
>
> まず保存先をローカルディスクへ移し、SQLite の WAL を無効化して再試行します。併せて空き容量、権限、既存の WAL・SHM ファイル、DB 整合性を確認し、必要なら再構築します。
>
> **修正方針を整理した**
>
> 
>
> **優先順位を決定中**
>
> 最も疑わしいのは、同期フォルダや共有ドライブ上で WAL を使っていることです。次に容量・クォータと大きな履歴書き込みを確認し、ローカル保存と DELETE ジャーナルで切り分けます。
>
> **優先順位を決定した**
>
> 
>
> **FixedNarrativeStateDB使用箇所の確認**
>
>
> **FixedNarrativeStateDBのSQLiteロックエラーと使用箇所を診断・修正**
>
> 
>
> **SQLite書き込み処理を安定化した**
>
> 
>
> **ノートブックのインポート部分を確認**
>
>
> **進め方を整理中**
>
> エラー原因は、履歴書き込み時の SQLite 本体より、保存先や WAL 周辺にありそうです。まず診断情報を確認し、必要ならローカル保存・WAL 無効化・小分け更新・ロールバック対応へ切り替えます。
>
> **進め方を整理した**
>
> 
>
> **SQLite接続エラーの原因特定と修正**
>
>
> **ノートブックのSQLite設定・スキーマ・実行処理を検証修正**
>
> 
>
> **SQLiteパッチ検証ノートブック実行**
>
>
> **SQLiteパッチ検証ノートブックを実行**
>
> 
>
> **コンテナとPython環境のファイル確認**
>
>
> **コンテナとデータを確認しSQLite修正版と状態管理を検証**
>
> 
>
> 23m 52s考えました

このエラーは、`history_rows`の型やSQL列数の不一致よりも、**SQLiteのファイルシステム書込み層の問題**である可能性が高いです。

実装ではSQLite DBを

```text
<store_root>/fixed_narratives/state/fixed_narrative.sqlite
```

へ置き、従来版は起動時に `PRAGMA journal_mode=WAL` を強制していました。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb` `02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

## 原因として最も疑わしいもの

### 1. `store_root`がクラウド・ネットワーク・同期フォルダ上にある

特に以下では、SQLite WALの `-wal`・`-shm` sidecarとロック機構が不安定になる場合があります。

```text
Google Drive
OneDrive
Dropbox
NFS
SMB
NAS
FUSE mount
共有ドライブ
```

`assignment_current`への書込みまでは通り、より大きい`assignment_history`を書いた時点でjournalの拡張・flushが発生し、そこで `disk I/O error` が顕在化したと考えられます。

`assignment_history`には、`evidence_json`、一致ルール、matched textsなど比較的大きいTEXT列も含まれています。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

### 2. 旧実装のtransaction境界に問題がある

旧実装は、

```text
assignment_currentをDELETE
→ commit
→ currentをINSERT
→ historyをINSERT
→ commit
```

という順序でした。つまり、history書込みに失敗すると、古いcurrent assignmentの削除だけが確定してしまう可能性があります。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

そのため、今回失敗した既存Storeは、単純に同じ処理を再開するより、状態を検査する必要があります。

なお、ローカルコードで変数名が本当に `histoy_sql`、`histry_rows` になっている場合は、それは別途修正が必要です。ただし、その場合の例外は通常 `NameError` であり、`disk I/O error` にはなりません。

# 修正版Notebook

[SQLite disk I/O安全化版Notebook](sandbox:/mnt/data/02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx_sqlite_io_safe.ipynb)

## 修正内容

以下を最小限の変更で反映しています。

- 既定のjournal modeを`WAL`から`DELETE`へ変更
- `busy_timeout=60秒`
- 記事250件単位のbounded transaction
- 各batch内で  
  `DELETE current → INSERT current → INSERT history`  
  を同一transactionとして実行
- history書込み失敗時にcurrent削除もrollback
- `replace_sentiment`も同様にatomic化
- 失敗時に必ずrollback
- failed-run metadataの保存失敗が、元の例外を上書きしないよう修正
- 例外messageへ以下を追加
  - DBの絶対パス
  - 実際のjournal mode
  - 親directoryの書込み権限
  - 空き容量
  - DB、`-wal`、`-shm`のサイズ
- `PRAGMA quick_check`用の診断関数を追加

設定には以下が追加されています。

```python
"sqlite_journal_mode": "DELETE",
"sqlite_synchronous": "NORMAL",
"sqlite_busy_timeout_ms": 60_000,
"sqlite_write_batch_articles": 250,
```

ローカルSSD上でWALを使いたい場合だけ、

```python
"sqlite_journal_mode": "WAL",
```

へ戻せます。ただし、クラウド・共有・同期mountでは`DELETE`を推奨します。

# 現在のDBに対して行う確認

まず、**同じDBを使用しているNotebook kernel、Python processをすべて停止**してください。その後、DBとsidecarを含むstate directoryをバックアップします。

```python
from pathlib import Path
import os
import shutil
import sqlite3

store_root = Path(PRODUCTION_CONFIG["store_root"]).expanduser().resolve()
db_path = (
    store_root
    / "fixed_narratives"
    / "state"
    / "fixed_narrative.sqlite"
)

print("DB path:", db_path)
print("DB exists:", db_path.exists())
print("Parent writable:", os.access(db_path.parent, os.W_OK))

usage = shutil.disk_usage(db_path.parent)
print("Free GiB:", usage.free / 1024**3)

for suffix in ("", "-wal", "-shm", "-journal"):
    path = Path(str(db_path) + suffix)
    print(
        path.name,
        "exists=", path.exists(),
        "size=", path.stat().st_size if path.exists() else 0,
    )

conn = sqlite3.connect(str(db_path), timeout=60)
try:
    print("journal mode:", conn.execute(
        "PRAGMA journal_mode"
    ).fetchone())

    print("quick check:", conn.execute(
        "PRAGMA quick_check"
    ).fetchone())
finally:
    conn.close()
```

期待される結果は、

```text
quick check: ('ok',)
```

です。

## `quick_check == ok`の場合

修正版Notebookで、次の設定を使って再実行してください。

```python
PRODUCTION_CONFIG = finalize_config({
    # 既存設定
    "store_root": "/path/to/local/fixed_narrative_store",
    "registry_path": (
        "/path/to/fixed_narrative_registry_73_public_v1.json"
    ),
    "sentiment_dictionary_path": (
        "/path/to/japanese_polarity_dictionary.xlsx"
    ),

    # SQLite
    "sqlite_journal_mode": "DELETE",
    "sqlite_synchronous": "NORMAL",
    "sqlite_busy_timeout_ms": 60_000,
    "sqlite_write_batch_articles": 250,
})
```

`store_root`がGoogle Drive等にある場合は、可能な限りローカルSSD上のpathへ変更してください。

```python
"store_root": str(
    Path.home() / "narrative_store_local"
)
```

ローカルで処理を完了してDB connectionを閉じた後に、Storeをバックアップ先へコピーする方が安全です。

## `quick_check`が`ok`でない場合

既存DBをそのまま更新しないでください。

1. `fixed_narratives/state` directory全体を退避
2. 新しいローカル`store_root`を指定
3. 完全な記事snapshotを入力
4. `initial_full`または`rebuild_epoch`を実行

```python
rebuilt_result = run_fixed_narrative_update(
    historical_articles_df,
    PRODUCTION_CONFIG,
    run_mode="initial_full",
)
```

今回の失敗箇所より前にcurrent assignmentの削除がcommitされていた可能性があるため、**失敗したStoreのincremental再開よりfull rebuildの方が安全**です。

# WAL sidecarについて

以下を、DB connectionやNotebook kernelが開いている状態で手動削除しないでください。

```text
fixed_narrative.sqlite-wal
fixed_narrative.sqlite-shm
```

正常なconnectionが開ける場合は、全writer停止後にcheckpointできます。

```python
conn = sqlite3.connect(str(db_path), timeout=60)
try:
    print(conn.execute(
        "PRAGMA wal_checkpoint(TRUNCATE)"
    ).fetchall())
finally:
    conn.close()
```

これ自体が`disk I/O error`になる場合は、sidecarを直接操作せず、state directoryを退避して新規DBを構築する方が安全です。

# 修正版の検証状況

修正版については、

- 全code cellの構文compile
- `journal_mode=DELETE`
- `assignment_current`書込み
- `assignment_history`書込み
- current replacement
- current削除
- history保持
- `PRAGMA quick_check == ok`

のSQLite特化smoke testを実施し、正常終了しています。Notebook全体のNarrative分類demoは再実行していないため、実データ投入前に小規模batchでの確認を推奨します。

このエラーはNarrative MomentumやNarrative Mapsの数理手法ではなく、実装上の永続化層に関するものです。Narrative Momentum論文はリアルタイム収集記事からNarrative coverage intensityを構築する枠組みを扱い、`Narrative Momentum.pdf` Narrative Maps論文はcoherence・coverage制約によるstoryline graph抽出を扱っています。`2009.04508v2.pdf`

## Response:
2026/8/4 21:22:51

> **比較準備を進めています**
>
> 添付された元ノートブックと変更後ノートブックを直接比較し、セル単位で追加・削除・置換された処理を確認しています。特にSQLite入出力、安全化処理、判定ロジック、出力形式の差分を整理します。
>
> **比較準備を進めました**
>
> 
>
> **思い出しています**
>
>
> **比較結果を整理中です**
>
> 元版と変更版をセル単位で照合し、変更箇所を追加・削除・置換に分けて確認しています。SQLite保存、安全化処理、極性判定、診断出力、完了条件の移動を重点的に整理します。
>
> **比較結果を整理しました**
>
> 
>
> **変更箇所を整理中**
>
> 元版と変更版をセル単位で照合し、設定、SQLite接続、トランザクション、保存処理、診断セル、実行結果削除を分けて、具体的な差分として列挙します。
>
> **変更箇所を整理した**
>
> 
>
> **差分を確定中**
>
> 元版に対する主な変更は、設定追加、SQLiteの初期化・診断、保存処理の原子化、エラー情報強化、診断セル追加、実行結果削除です。次に各変更を、該当セクションと処理単位で具体化します。
>
> **差分を確定した**
>
> 
>
> **ノートブック変更とSQLite入出力の差分を比較しました**
>
> 
>
> 11m 18s考えました

比較対象は以下の2ファイルです。

- [変更前：日本語極性辞書版](sandbox:/mnt/data/02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb)
- [変更後：SQLite I/O安全化版](sandbox:/mnt/data/02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx_sqlite_io_safe.ipynb)

ソースコード上の変更は、**既存コードセル3か所の修正、診断用2セルの追加、見出し番号の変更**です。固定Narrative分類、極性辞書、Intensity計算などには変更を加えていません。

# 1. ConfigurationにSQLite設定を4項目追加

## 変更前

SQLite設定はConfigurationに存在せず、`FixedNarrativeStateDB`内部でWALが固定されていました。元の実装では、DB初期化時に次が直接実行されていました。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

```python
PRAGMA journal_mode=WAL;
PRAGMA synchronous=NORMAL;
```

## 変更後

`DEFAULT_CONFIG`に次を追加しました。

```python
"sqlite_journal_mode": "DELETE",
"sqlite_synchronous": "NORMAL",
"sqlite_busy_timeout_ms": 60_000,
"sqlite_write_batch_articles": 250,
```

各設定の意味は以下です。

| 設定 | 変更後の既定値 | 目的 |
|---|---:|---|
| `sqlite_journal_mode` | `DELETE` | WAL sidecar依存を避ける |
| `sqlite_synchronous` | `NORMAL` | 書込み安全性と速度のバランス |
| `sqlite_busy_timeout_ms` | 60,000 ms | 一時的なロックを即時エラーにしない |
| `sqlite_write_batch_articles` | 250記事 | 巨大transactionを避ける |

`DELETE`はSQLiteのrollback journalを使用します。`WAL`も引き続き指定できます。

```python
"sqlite_journal_mode": "WAL"
```

ただし、Google Drive、OneDrive、NFS、SMB、FUSE等では`DELETE`を既定としています。

---

# 2. `FixedNarrativeStateDB.__init__`を変更

## 変更前

```python
def __init__(self, path: str | Path) -> None:
    self.path = Path(path)
    self.path.parent.mkdir(parents=True, exist_ok=True)
    self.conn = sqlite3.connect(self.path)
    self.conn.row_factory = sqlite3.Row
    self._init_schema()
```

SQLite接続について設定できるのはDB pathだけでした。

## 変更後

```python
def __init__(
    self,
    path: str | Path,
    *,
    journal_mode: str = "DELETE",
    synchronous: str = "NORMAL",
    busy_timeout_ms: int = 60_000,
    write_batch_articles: int = 250,
) -> None:
```

内部では次を行います。

```python
self.path = Path(path).expanduser().resolve()

self.journal_mode = str(journal_mode).upper()
self.synchronous = str(synchronous).upper()
self.busy_timeout_ms = max(1_000, int(busy_timeout_ms))
self.write_batch_articles = max(1, int(write_batch_articles))

self.conn = sqlite3.connect(
    str(self.path),
    timeout=self.busy_timeout_ms / 1_000.0,
)
```

具体的な変更は次の5点です。

1. DB pathを絶対pathへ変換
2. journal modeを引数化
3. synchronous modeを引数化
4. connection timeoutを60秒に設定
5. 書込みbatch記事数を保持

許容する値も明示的に検証します。

```python
_ALLOWED_JOURNAL_MODES = {
    "DELETE",
    "TRUNCATE",
    "PERSIST",
    "WAL",
}

_ALLOWED_SYNCHRONOUS = {
    "OFF",
    "NORMAL",
    "FULL",
    "EXTRA",
}
```

不正な値の場合は、SQLite接続後ではなく、初期化時に`ValueError`で停止します。

---

# 3. SQLite PRAGMAの設定方法を変更

## 変更前

table作成SQLと同じ`executescript`内に、以下が固定で含まれていました。

```sql
PRAGMA journal_mode=WAL;
PRAGMA synchronous=NORMAL;
```

## 変更後

table作成前に、Python側から個別に実行します。

```python
row = self.conn.execute(
    f"PRAGMA journal_mode={self.journal_mode}"
).fetchone()

self.conn.execute(
    f"PRAGMA synchronous={self.synchronous}"
)

self.conn.execute(
    f"PRAGMA busy_timeout={self.busy_timeout_ms}"
)

self.conn.execute("PRAGMA temp_store=MEMORY")
self.conn.execute("PRAGMA foreign_keys=ON")
```

WALが明示指定された場合だけ、次も実行します。

```python
if self.actual_journal_mode == "WAL":
    self.conn.execute(
        "PRAGMA wal_autocheckpoint=1000"
    )
```

また、SQLiteが実際に採用したjournal modeを保存します。

```python
self.actual_journal_mode
```

これは、要求したmodeと実際のmodeが一致しないファイルシステムを診断するためです。

---

# 4. SQLite診断関数を3つ追加

`FixedNarrativeStateDB`に以下を追加しました。

## 4.1 `storage_diagnostics`

```python
def storage_diagnostics(self) -> dict[str, Any]:
```

返す項目は次です。

```text
db_path
parent_exists
parent_writable
free_bytes
total_bytes

journal_mode_requested
journal_mode_actual
busy_timeout_ms

db_bytes
wal_bytes
shm_bytes
journal_bytes
```

したがって、エラー発生時に、

- DBがどこにあるか
- 親directoryへ書込み可能か
- 空き容量があるか
- `-wal`や`-shm`が巨大化していないか
- 実際のjournal modeが何か

を確認できます。

## 4.2 `quick_check`

```python
def quick_check(self) -> str:
    row = self.conn.execute(
        "PRAGMA quick_check"
    ).fetchone()
    return str(row[0])
```

正常なDBでは、

```text
ok
```

を返します。

## 4.3 `_sqlite_operational_error`

```python
def _sqlite_operational_error(
    self,
    operation: str,
    exc: BaseException,
) -> sqlite3.OperationalError:
```

従来は、

```text
sqlite3.OperationalError: disk I/O error
```

だけでした。

変更後は、例えば次の情報を含む例外になります。

```text
replace_assignments batch of 250 articles failed:
disk I/O error.

db_path=/...
journal_mode=DELETE
parent_writable=True
free_gib=...
db_bytes=...
wal_bytes=...
shm_bytes=...
```

さらに、DBがクラウド・ネットワークmount上にある可能性もmessageへ含めています。

---

# 5. commitしない内部DELETE関数を追加

次の2関数を追加しました。

```python
_delete_current_assignments_no_commit(...)
_delete_current_sentiment_no_commit(...)
```

例：

```python
def _delete_current_assignments_no_commit(
    self,
    article_ids: Sequence[str],
) -> None:
    for batch in self._chunks(list(article_ids)):
        if batch:
            marks = ",".join("?" for _ in batch)
            self.conn.execute(
                f"""
                DELETE FROM assignment_current
                WHERE article_id IN ({marks})
                """,
                batch,
            )
```

これらは**内部でcommitしません**。

後述する、

```text
DELETE current
INSERT current
INSERT history
```

を一つのtransactionへ入れるための変更です。

---

# 6. `close()`にrollback処理を追加

## 変更前

```python
def close(self) -> None:
    self.conn.close()
```

## 変更後

```python
def close(self) -> None:
    try:
        if self.conn.in_transaction:
            self.conn.rollback()
    finally:
        self.conn.close()
```

例外発生時に未完了transactionを残したままconnectionを閉じないようにしました。

---

# 7. `delete_current_assignments`をtransaction化

## 変更前

```python
def delete_current_assignments(
    self,
    article_ids,
):
    ...
    self.conn.commit()
```

DELETE処理単独で即時commitしていました。

## 変更後

```python
def delete_current_assignments(
    self,
    article_ids: Sequence[str],
) -> None:
    try:
        with self.conn:
            self._delete_current_assignments_no_commit(
                article_ids
            )
    except sqlite3.OperationalError as exc:
        self.conn.rollback()
        raise self._sqlite_operational_error(
            "delete_current_assignments",
            exc,
        ) from exc
```

変更点は次の3つです。

1. `with self.conn:`でtransaction管理
2. `OperationalError`時に明示rollback
3. 診断情報付き例外へ変換

---

# 8. `replace_assignments`を全面的にtransaction-safe化

ここが今回の主要変更です。

## 8.1 変更前の処理順序

旧コードは次の順序でした。

```python
self.delete_current_assignments(article_ids)
```

この関数内でDELETEがcommitされます。その後に、

```python
self.conn.executemany(
    current_sql,
    rows,
)

self.conn.executemany(
    history_sql,
    history_rows,
)

self.conn.commit()
```

を実行していました。元コードではcurrent insert、history insert、最後のcommitが一括で行われています。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

処理構造は以下でした。

```text
DELETE old current
    ↓
COMMIT                      ← ここで削除が確定
    ↓
INSERT new current
    ↓
INSERT history              ← ここでdisk I/O error
    ↓
COMMIT
```

したがって、history insertで失敗すると、先にcommitされたDELETEだけが残る可能性があります。

## 8.2 変更後の処理順序

変更後は記事IDを250件ずつ処理します。

```python
for id_batch in self._chunks(
    ordered_ids,
    size=self.write_batch_articles,
):
    with self.conn:
        self._delete_current_assignments_no_commit(
            id_batch
        )

        if current_batch:
            self.conn.executemany(
                current_sql,
                current_batch,
            )

        if history_batch:
            self.conn.executemany(
                history_sql,
                history_batch,
            )
```

一つのbatch内では、

```text
BEGIN
    DELETE old current
    INSERT new current
    INSERT history
COMMIT
```

となります。

途中で失敗した場合は、

```text
ROLLBACK
```

となり、そのbatchのDELETEも取り消されます。

## 8.3 全記事一括の`history_rows`を廃止

変更前：

```python
history_rows = []

for row in assignments.to_dict("records"):
    ...
    history_rows.append(...)

self.conn.executemany(
    history_sql,
    history_rows,
)
```

変更後：

```python
rows_by_article = defaultdict(list)
history_by_article = defaultdict(list)
```

記事IDごとに整理し、各batchについて、

```python
current_batch
history_batch
```

を作ります。

したがって、エラー行周辺は、

```python
self.conn.executemany(
    history_sql,
    history_batch,
)
```

へ変わっています。

## 8.4 `article_ids`の補完

変更後は、引数の`article_ids`だけでなく、`assignments`に実際に含まれる記事IDも処理対象へ追加します。

```python
ordered_ids = list(
    dict.fromkeys(
        [str(x) for x in article_ids]
        + [
            str(x)
            for x in assignments.get(
                "article_id",
                pd.Series(dtype=str),
            )
        ]
    )
)
```

呼出側の`article_ids`に漏れがあっても、assignment行が保存されない問題を防ぎます。

## 8.5 エラーmessageへbatch情報を追加

```python
raise self._sqlite_operational_error(
    f"""
    replace_assignments batch of {len(id_batch)}
    articles
    ({len(current_batch)} current rows,
     {len(history_batch)} history rows)
    """,
    exc,
)
```

どの規模の書込みで失敗したかを確認できます。

## 8.6 transactionの粒度

重要な点として、変更後は**run全体で一つのtransactionではなく、250記事単位のtransaction**です。

したがって、例えば3番目のbatchで失敗した場合、

```text
batch 1：commit済み
batch 2：commit済み
batch 3：rollback
```

となります。

ただし、`INSERT OR REPLACE`と主キー制約により再実行可能な設計です。

---

# 9. `replace_sentiment`も同じ方式へ変更

エラーが出たのはassignment側ですが、sentiment側も同じ書込み構造だったため、同様に変更しました。

## 変更前

```text
DELETE sentiment_current
COMMIT

INSERT sentiment_current
INSERT sentiment_history
COMMIT
```

## 変更後

250記事単位で、

```text
BEGIN
    DELETE sentiment_current
    INSERT sentiment_current
    INSERT sentiment_history
COMMIT
```

とします。

追加された内部関数は、

```python
_delete_current_sentiment_no_commit(...)
```

です。

エラー時は、

```python
self.conn.rollback()
```

を行い、診断情報付き例外を返します。

---

# 10. `run_fixed_narrative_update`のDB生成部分を変更

## 変更前

```python
state = FixedNarrativeStateDB(
    store.db_path
)
```

元のPipelineではDB pathだけを渡していました。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

## 変更後

```python
state = FixedNarrativeStateDB(
    store.db_path,
    journal_mode=str(
        cfg.get(
            "sqlite_journal_mode",
            "DELETE",
        )
    ),
    synchronous=str(
        cfg.get(
            "sqlite_synchronous",
            "NORMAL",
        )
    ),
    busy_timeout_ms=int(
        cfg.get(
            "sqlite_busy_timeout_ms",
            60_000,
        )
    ),
    write_batch_articles=int(
        cfg.get(
            "sqlite_write_batch_articles",
            250,
        )
    ),
)
```

これにより、ConfigurationのSQLite設定が実際のDB connectionへ渡されます。

なお、その他の場所で、

```python
FixedNarrativeStateDB(store.db_path)
```

としている箇所は残っていますが、constructorの新しい既定値が、

```text
DELETE
NORMAL
60秒
250記事
```

なので、同じ安全設定になります。

---

# 11. Pipeline全体の例外処理を変更

## 変更前

```python
except Exception as exc:
    state.finish_run(
        run_id,
        "failed",
        {
            "error": (
                f"{type(exc).__name__}: {exc}"
            )
        },
    )
    raise
finally:
    state.close()
```

元のエラー処理は、失敗後に`finish_run`をDBへ書き込んでいました。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

SQLite自体が書込み不能の場合、`finish_run`も失敗し、最初の`disk I/O error`を上書きする可能性があります。

## 変更後

最初にrollbackします。

```python
try:
    if state.conn.in_transaction:
        state.conn.rollback()
except Exception:
    pass
```

次にfailed-run情報を保存します。

```python
try:
    state.finish_run(
        run_id,
        "failed",
        {
            "error": (
                f"{type(exc).__name__}: {exc}"
            ),
            "sqlite_diagnostics": (
                state.storage_diagnostics()
            ),
        },
    )
except Exception as finish_exc:
    print(
        "[WARN] Could not persist "
        "failed-run metadata: ..."
    )
```

これにより、

1. 未完了transactionをrollback
2. 診断情報をrun historyへ保存
3. run-history保存も失敗した場合はwarningだけ表示
4. 元の例外をそのまま再送出

となります。

---

# 12. SQLite診断用セクションを2セル追加

旧Notebookの最後に、以下の2セルを追加しました。

## Markdownセル

```text
## 11. SQLite `disk I/O error` 診断・復旧
```

内容は、

- WALからDELETEへの変更理由
- cloud／network filesystemへの注意
- `-wal`、`-shm`を接続中に削除しないこと
- ローカルSSD推奨

です。

## Codeセル

コメントを外して任意実行できる診断です。

```python
cfg = finalize_config(
    PRODUCTION_CONFIG
)

store = FixedNarrativeStore(
    cfg["store_root"],
    cfg["classification_epoch"],
)

state = FixedNarrativeStateDB(
    store.db_path,
    journal_mode=cfg.get(
        "sqlite_journal_mode",
        "DELETE",
    ),
    synchronous=cfg.get(
        "sqlite_synchronous",
        "NORMAL",
    ),
    busy_timeout_ms=cfg.get(
        "sqlite_busy_timeout_ms",
        60_000,
    ),
    write_batch_articles=cfg.get(
        "sqlite_write_batch_articles",
        250,
    ),
)

try:
    display(
        pd.Series(
            state.storage_diagnostics(),
            name="value",
        ).to_frame()
    )

    print(
        "PRAGMA quick_check:",
        state.quick_check(),
    )
finally:
    state.close()
```

---

# 13. `Definition of Done`の番号だけ変更

元の見出し：

```text
## 11. Definition of Done
```

変更後：

```text
## 12. Definition of Done
```

本文は同じです。

---

# 14. Notebookの実行出力をクリア

コード以外の差分として、変更後Notebookでは、

```text
execution_count = null
outputs = []
```

へ統一しています。

具体的には、変更前に含まれていた以下のdemo出力を除去しました。

- Registry検証表
- incremental demoのstdout
- comparison表
- monthly intensity表示
- production config表示

したがって変更後Notebookは、**ソースは修正済みですが、Notebookとしては未実行状態**です。

ファイルサイズが変更前より小さくなっている主因は、この出力削除です。

---

# 15. 変更していない箇所

以下には変更していません。

- 固定73 Narrative Registry
- 72 active＋1 inactive placeholder
- 1,009 term rules
- DNF gate
- proximity rule
- semantic candidate
- article-to-Narrative multi-label分類
- 日本語極性辞書XLSXの読込
- 極性語彙のtrie matching
- sentiment scoreとlabel
- hard／soft Narrative Intensity
- technical duplicate判定
- syndication処理
- reservoir Aggregate
- 木曜〜水曜の週次集計
- 月次Intensity
- Narrative Momentum signal
- point-in-time vintage
- 関連記事抽出
- Narrative Map bundle
- SQLite table schemaと主キー

今回の変更は、**Narrative MomentumのNarrative Intensityやmimicking portfolioの考え方ではなく、SQLite永続化層だけ**です。Narrative Momentum論文は、リアルタイム収集記事からNarrative coverage intensityとNarrative-mimicking portfolioを構築する方法を扱っています。`Narrative Momentum.pdf`

同様に、Narrative Maps側のcoherence、coverage、st-graph、main route等にも変更はありません。Narrative Mapsはcoherence最大化と構造・coverage制約によるgraph抽出を扱う別の後段処理です。`2009.04508v2.pdf`

最も重要なコード差分を一行で表すと、次です。

```text
変更前：
DELETEを先にcommit
→ current全件INSERT
→ history全件INSERT
→ commit

変更後：
250記事ごとに
BEGIN
→ DELETE
→ current INSERT
→ history INSERT
→ COMMIT
失敗時はbatch全体をROLLBACK
```

## Response:
2026/8/5 19:02:59

> **Inspecting narrative intensity storage and loading**
>
>
> **構成を整理する**
>
> 永続的な正規データは週次結果の一時的な表ではなく、保存先のパーティションに保持されます。差分実行では影響を受けた日・週・月だけが再計算されるため、完全な履歴は保存領域から読み戻します。
>
> **構成を整理した**
>
> 
>
> **確認点を補足する**
>
> 後続週の負の変化量は、過去日を修正した場合に古いまま残る可能性があります。月次は対象月を再構築し、シグナルは月次全体から再生成します。軽量化設定は返却結果だけに影響し、永続化データは別に保存されます。
>
> **確認点を補足した**
>
> 
>
> **読み込み方法を整理する**
>
> 全履歴を一括結合せず、保存済みパーティションを順次処理するのが安全です。返却結果は差分のみの場合があるため、完全な日次・週次・月次系列は永続領域から読み込みます。設定と分類エポックは実行間で一致させます。
>
> **読み込み方法を整理した**
>
> 
>
> **確認したナラティブ強度の保存・再計算・出力仕様**
>
> 
>
> **全履歴の保存先**
>
> 初回実行では全期間が各パーティションへ保存され、差分実行後は該当日・週・月だけが更新されます。完全系列は実行結果ではなく、日次・週次・月次の正規パーティションを読み込みます。実行別スナップショットは監査用で、結合して履歴を復元しません。
>
> **まとめて読み込む**
>
> 私は、初回・差分実行が正規パーティションを自動更新し、返却結果は差分中心だと整理します。完全履歴は同じ設定と分類エポックで各パーティションを順次読み取り、必要なら期間指定やポイントインタイム系列を使います。
>
> **全履歴の保存先をまとめて読み込んだ**
>
> 
>
> **再計算と集計を行い保持ロジックとノートブック操作を比較説明**
>
> 
>
> 8m 7s考えました

# 結論

`initial_full`と、その後の`weekly_incremental`は、**同じ`store_root`と同じ`classification_epoch`を使って実行**します。

```python
initial_result = run_fixed_narrative_update(
    historical_articles_df,
    PRODUCTION_CONFIG,
    run_mode="initial_full",
)

weekly_result = run_fixed_narrative_update(
    new_or_changed_articles_df,
    PRODUCTION_CONFIG,
    run_mode="weekly_incremental",
)
```

このとき、全期間のNarrative Intensityは`weekly_result.daily_aggregate`などの返り値に全て格納されるのではなく、**`store_root`配下の日次・週次・月次パーティションへ累積的に保存**されます。

したがって、全期間系列を利用する際は、

```text
run_fixed_narrative_updateの返り値
```

ではなく、

```text
FixedNarrativeStoreに保存された全partition
```

を読み込みます。

*Narrative Momentum*論文も、約350 Narrativeのcoverage intensityを継続的に計測し、その時系列をNarrative-mimicking portfolioへ接続する構造です。`Narrative Momentum.pdf`

---

# 1. 保存されるデータの2種類

## 1.1 Canonical Store：全期間の最新状態

こちらが分析で利用すべき正式な保存先です。

```text
<store_root>/
└── fixed_narratives/
    ├── intensity/
    │   ├── daily_by_reservoir/
    │   │   └── classification_epoch=<epoch>/
    │   │       ├── date=2025-01-01/
    │   │       ├── date=2025-01-02/
    │   │       └── ...
    │   │
    │   ├── daily_aggregate/
    │   │   └── classification_epoch=<epoch>/
    │   │       ├── date=2025-01-01/
    │   │       ├── date=2025-01-02/
    │   │       └── ...
    │   │
    │   ├── weekly/
    │   │   └── classification_epoch=<epoch>/
    │   │       ├── week_end_wednesday=2025-01-08/
    │   │       └── ...
    │   │
    │   └── monthly/
    │       └── classification_epoch=<epoch>/
    │           ├── month=2025-01/
    │           └── ...
    │
    ├── signals/
    │   └── classification_epoch=<epoch>/
    │       ├── formation_month=2025-01/
    │       └── ...
    │
    ├── runs/
    │   └── <run_id>/
    │
    └── state/
        └── fixed_narrative.sqlite
```

これらのdirectoryは`FixedNarrativeStore`の生成時に設定されます。ファイル形式は`pyarrow`があればParquet、なければPickleです。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

## 1.2 Run Snapshot：その実行回の処理結果

各実行の返り値は、次にも保存されます。

```text
<store_root>/
└── fixed_narratives/
    └── runs/
        └── <run_id>/
            ├── changed_articles.parquet
            ├── assignments.parquet
            ├── sentiment.parquet
            ├── daily_intensity.parquet
            ├── daily_aggregate.parquet
            ├── weekly_intensity.parquet
            ├── monthly_intensity.parquet
            ├── momentum_signals.parquet
            ├── qa_summary.parquet
            └── run_metadata.json
```

ただし、`weekly_incremental`のrun snapshotに入るのは、原則として**その実行で影響を受けた日・週・月だけ**です。`runs/<run_id>`は監査・デバッグ用であり、全期間masterではありません。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

したがって、次は行わないでください。

```python
# 非推奨
full_history = pd.concat([
    initial_result.daily_aggregate,
    weekly_result.daily_aggregate,
])
```

訂正記事や再計算されたpartitionがあると、古い値と新しい値が重複するためです。

---

# 2. `initial_full`実行時の保存

例えば2020年1月から2025年12月までを`initial_full`で処理した場合、全日付について以下が作成されます。

```text
daily_by_reservoir/date=YYYY-MM-DD
daily_aggregate/date=YYYY-MM-DD
weekly/week_end_wednesday=YYYY-MM-DD
monthly/month=YYYY-MM
signals/formation_month=YYYY-MM
```

月次Intensityは、論文の定義と同じく日次Intensityの月内平均です。論文では日次negative／positive intensityを、各Narrativeに関連するnegative／positive記事数の全media discussionに対する割合とし、月次値をその日次値の平均としています。`Narrative Momentum.pdf`

---

# 3. `weekly_incremental`実行時の保存

追加記事、訂正記事、削除記事を`weekly_incremental`で渡すと、次のように更新されます。

```text
新規・変更記事
    ↓
その記事が属する日付をaffected_datesとして取得
    ↓
affected_datesの日次partitionを再計算・置換
    ↓
affected_datesを含む週次partitionを再計算・置換
    ↓
affected_datesを含む月次partitionを再計算・置換
    ↓
全月次partitionからMomentum signalを再構築
```

具体的には、日次partitionは既存のNarrative行を削除してから、同じ日付について再集計した値をupsertします。週次partitionと月次partitionは、対象partition directoryを一旦削除したうえで再計算されます。Momentum signalは、保存済みの全月次partitionを読み込み、既存signal partitionを全て削除してから再構築されます。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb` `02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

例えば、2026年1月15日の記事を追加した場合、

```text
2020-01 ～ 2026-01の既存partition
```

のうち、

```text
daily/date=2026-01-15
weekly/week_end_wednesday=2026-01-21
monthly/month=2026-01
```

が更新され、それ以前のpartitionはそのまま残ります。

---

# 4. 返り値に入る期間

ここは特に重要です。

## `initial_result`

`initial_full`では、全入力記事がchanged扱いになるため、概ね入力期間全体が返ります。

```python
initial_result.daily_intensity
initial_result.daily_aggregate
initial_result.weekly_intensity
initial_result.monthly_intensity
```

## `weekly_result`

`weekly_incremental`では、次の返り値は**更新対象だけ**です。

```python
weekly_result.daily_intensity
# affected_datesのみ、reservoir別

weekly_result.daily_aggregate
# affected_datesのみ、Aggregate

weekly_result.weekly_intensity
# affected weeksのみ

weekly_result.monthly_intensity
# affected monthsのみ
```

一方、`weekly_result.momentum_signals`は、全月次partitionを読み直して再計算されるため、変更があるrunでは基本的に全期間分が返ります。コード上も、全monthly partitionを読み込み、signal directoryを全削除して再構築しています。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

整理すると次です。

| オブジェクト | `weekly_incremental`の返り値 |
|---|---|
| `daily_intensity` | 更新日だけ |
| `daily_aggregate` | 更新日だけ |
| `weekly_intensity` | 更新週だけ |
| `monthly_intensity` | 更新月だけ |
| `momentum_signals` | 原則、全期間再構築 |
| Canonical Store | 全期間の最新状態 |

---

# 5. 全期間をロードする標準コード

以下をNotebookの後段に追加すると、保存済みの全期間系列を取得できます。

```python
from __future__ import annotations

from typing import Sequence

import pandas as pd

def load_all_partitioned_results(
    store: FixedNarrativeStore,
    base,
    partition_col: str,
    *,
    key_cols: Sequence[str],
    sort_cols: Sequence[str],
) -> pd.DataFrame:
    """
    Partition Storeから全期間を読み込み、
    business keyで最新行を一意化する。
    """
    frames: list[pd.DataFrame] = []

    for _, frame in store.iter_partitions(
        base,
        partition_col,
    ):
        if not frame.empty:
            frames.append(frame)

    if not frames:
        return pd.DataFrame()

    result = pd.concat(
        frames,
        ignore_index=True,
        sort=False,
    )

    valid_sort_cols = [
        col for col in sort_cols
        if col in result.columns
    ]
    if valid_sort_cols:
        result = result.sort_values(
            valid_sort_cols,
            kind="stable",
        )

    valid_key_cols = [
        col for col in key_cols
        if col in result.columns
    ]
    if valid_key_cols:
        result = result.drop_duplicates(
            valid_key_cols,
            keep="last",
        )

    return result.reset_index(drop=True)

def load_full_fixed_narrative_store(
    *,
    store_root,
    classification_epoch: str,
) -> dict[str, pd.DataFrame]:
    """
    最新改訂済みの全期間Narrative Intensity、
    月次系列、Momentum signalをロードする。
    """
    store = FixedNarrativeStore(
        store_root,
        classification_epoch,
    )

    daily_by_reservoir = load_all_partitioned_results(
        store,
        store.daily_reservoir_dir,
        "date",
        key_cols=[
            "date",
            "reservoir",
            "narrative_id",
            "series_type",
        ],
        sort_cols=[
            "date",
            "reservoir",
            "narrative_id",
            "series_type",
        ],
    )

    daily_aggregate = load_all_partitioned_results(
        store,
        store.daily_aggregate_dir,
        "date",
        key_cols=[
            "date",
            "narrative_id",
            "series_type",
        ],
        sort_cols=[
            "date",
            "narrative_id",
            "series_type",
        ],
    )

    weekly = load_all_partitioned_results(
        store,
        store.weekly_dir,
        "week_end_wednesday",
        key_cols=[
            "week_end_wednesday",
            "narrative_id",
            "series_type",
        ],
        sort_cols=[
            "week_end_wednesday",
            "narrative_id",
            "series_type",
        ],
    )

    monthly = load_all_partitioned_results(
        store,
        store.monthly_dir,
        "month",
        key_cols=[
            "month",
            "narrative_id",
            "series_type",
            "monthly_variant",
        ],
        sort_cols=[
            "month",
            "monthly_variant",
            "narrative_id",
            "series_type",
        ],
    )

    signals = load_all_partitioned_results(
        store,
        store.signal_dir,
        "formation_month",
        key_cols=[
            "formation_month",
            "narrative_id",
            "lookback_months",
            "series_type",
        ],
        sort_cols=[
            "formation_month",
            "lookback_months",
            "narrative_id",
            "series_type",
        ],
    )

    return {
        "daily_by_reservoir": daily_by_reservoir,
        "daily_aggregate": daily_aggregate,
        "weekly": weekly,
        "monthly": monthly,
        "signals": signals,
    }
```

`iter_partitions`と`read_partitions`はStore内に実装されており、ファイルがParquetかPickleかを呼出側で意識する必要はありません。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# 6. 実行後にロードする例

`weekly_result`の情報を使うのが最も安全です。

```python
full_result = load_full_fixed_narrative_store(
    store_root=weekly_result.store_root,
    classification_epoch=(
        weekly_result.classification_epoch
    ),
)

daily_by_reservoir_df = (
    full_result["daily_by_reservoir"]
)
daily_aggregate_df = (
    full_result["daily_aggregate"]
)
weekly_intensity_df = full_result["weekly"]
monthly_intensity_df = full_result["monthly"]
momentum_signals_df = full_result["signals"]
```

確認します。

```python
print({
    "daily_by_reservoir": len(
        daily_by_reservoir_df
    ),
    "daily_aggregate": len(
        daily_aggregate_df
    ),
    "weekly": len(
        weekly_intensity_df
    ),
    "monthly": len(
        monthly_intensity_df
    ),
    "signals": len(
        momentum_signals_df
    ),
})

print(
    daily_aggregate_df["date"].min(),
    daily_aggregate_df["date"].max(),
)

print(
    monthly_intensity_df["month"].min(),
    monthly_intensity_df["month"].max(),
)
```

---

# 7. 最新改訂済み系列の選択

通常のCanonical Storeには、

```text
series_type = latest_revised
```

が保存されます。

したがって、日次Aggregateは次のように絞ります。

```python
daily_latest = (
    daily_aggregate_df.loc[
        daily_aggregate_df[
            "series_type"
        ].eq("latest_revised")
    ]
    .sort_values(
        ["date", "narrative_id"]
    )
    .reset_index(drop=True)
)
```

Reservoir別の場合：

```python
daily_reservoir_latest = (
    daily_by_reservoir_df.loc[
        daily_by_reservoir_df[
            "series_type"
        ].eq("latest_revised")
    ]
    .sort_values(
        [
            "date",
            "reservoir",
            "narrative_id",
        ]
    )
    .reset_index(drop=True)
)
```

---

# 8. 月次系列は`full`と`signal_cutoff`を区別する

月次データには、2種類あります。

## 8.1 `monthly_variant="full"`

月内の全日次Intensityを平均した記述・分析用系列です。

```python
monthly_full = (
    monthly_intensity_df.loc[
        monthly_intensity_df[
            "series_type"
        ].eq("latest_revised")
        &
        monthly_intensity_df[
            "monthly_variant"
        ].eq("full")
    ]
    .sort_values(
        ["month", "narrative_id"]
    )
    .reset_index(drop=True)
)
```

## 8.2 `monthly_variant="signal_cutoff"`

Narrative Momentum形成用の系列です。設定により月末最終日を除外します。

```python
monthly_signal = (
    monthly_intensity_df.loc[
        monthly_intensity_df[
            "series_type"
        ].eq("latest_revised")
        &
        monthly_intensity_df[
            "monthly_variant"
        ].eq("signal_cutoff")
    ]
    .sort_values(
        ["month", "narrative_id"]
    )
    .reset_index(drop=True)
)
```

Momentum signalはこの`signal_cutoff`系列から作られます。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# 9. 後続分析用のwide形式

## 9.1 月次negative Narrative Intensity

```python
monthly_negative_wide = (
    monthly_signal
    .pivot(
        index="month",
        columns="narrative_id",
        values="negative_intensity_hard",
    )
    .sort_index()
)
```

## 9.2 月次positive Narrative Intensity

```python
monthly_positive_wide = (
    monthly_signal
    .pivot(
        index="month",
        columns="narrative_id",
        values="positive_intensity_hard",
    )
    .sort_index()
)
```

## 9.3 Coverage Intensity

```python
monthly_coverage_wide = (
    monthly_signal
    .pivot(
        index="month",
        columns="narrative_id",
        values="coverage_intensity",
    )
    .sort_index()
)
```

## 9.4 週次Narrative beta推定用

```python
weekly_latest = (
    weekly_intensity_df.loc[
        weekly_intensity_df[
            "series_type"
        ].eq("latest_revised")
    ]
    .copy()
)

weekly_negative_wide = (
    weekly_latest
    .pivot(
        index="week_end_wednesday",
        columns="narrative_id",
        values="negative_intensity_hard",
    )
    .sort_index()
)

weekly_change_wide = (
    weekly_latest
    .pivot(
        index="week_end_wednesday",
        columns="narrative_id",
        values="negative_intensity_change",
    )
    .sort_index()
)
```

論文では、木曜から翌水曜の日次Intensityを平均して週次Intensityとし、その前週差を株式のNarrative beta推定に使用しています。`Narrative Momentum.pdf`

---

# 10. メモリを抑えて期間指定でロードする

全期間を一括で`pd.concat`すると、データ量によってはメモリを消費します。期間指定の場合は`read_partitions`を使います。

## 日次Aggregateの期間指定

```python
store = FixedNarrativeStore(
    weekly_result.store_root,
    weekly_result.classification_epoch,
)

date_values = (
    pd.date_range(
        "2024-01-01",
        "2026-07-31",
        freq="D",
    )
    .strftime("%Y-%m-%d")
    .tolist()
)

daily_2024_2026 = store.read_partitions(
    store.daily_aggregate_dir,
    partition_col="date",
    partition_values=date_values,
    columns=[
        "date",
        "narrative_id",
        "series_type",
        "negative_intensity_hard",
        "positive_intensity_hard",
        "coverage_intensity",
        "n_total",
        "n_theme",
    ],
)
```

## 月次の期間指定

```python
month_values = (
    pd.period_range(
        "2024-01",
        "2026-07",
        freq="M",
    )
    .astype(str)
    .tolist()
)

monthly_2024_2026 = store.read_partitions(
    store.monthly_dir,
    partition_col="month",
    partition_values=month_values,
)
```

---

# 11. さらにメモリセーフに逐次処理する

全期間をDataFrameへ結合せず、一日ずつ処理できます。

```python
store = FixedNarrativeStore(
    weekly_result.store_root,
    weekly_result.classification_epoch,
)

for date_value, daily_frame in (
    store.iter_partitions(
        store.daily_aggregate_dir,
        "date",
    )
):
    daily_frame = daily_frame.loc[
        daily_frame["series_type"].eq(
            "latest_revised"
        )
    ]

    # ここで日単位に処理する
    # 例：別DBへの書込み、統計量更新など
    print(date_value, len(daily_frame))

    del daily_frame
```

`iter_partitions`は各partitionを順番に読み込むため、全期間を同時にメモリへ置きません。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# 12. Consolidated fileとして別途保存する場合

Canonical Storeはpartition形式のまま保持するのが推奨ですが、後続Notebookへ渡すために一つのファイルへまとめることもできます。

```python
from pathlib import Path

export_dir = (
    Path(weekly_result.store_root)
    / "fixed_narratives"
    / "exports"
    / f"classification_epoch={weekly_result.classification_epoch}"
)

export_dir.mkdir(
    parents=True,
    exist_ok=True,
)

store = FixedNarrativeStore(
    weekly_result.store_root,
    weekly_result.classification_epoch,
)

store._write_table(
    daily_latest,
    export_dir
    / f"daily_aggregate_latest{store.table_suffix}",
)

store._write_table(
    monthly_signal,
    export_dir
    / f"monthly_signal_cutoff{store.table_suffix}",
)

store._write_table(
    momentum_signals_df,
    export_dir
    / f"momentum_signals{store.table_suffix}",
)
```

ただし、これは派生exportです。正式なsource of truthは引き続きpartition Storeです。

---

# 13. 記事単位のassignmentとsentimentのロード

Narrative Intensityだけでなく、記事単位の紐付けも取得できます。

```python
cfg = weekly_result.config

store = FixedNarrativeStore(
    weekly_result.store_root,
    weekly_result.classification_epoch,
)

state = FixedNarrativeStateDB(
    store.db_path,
    journal_mode=cfg.get(
        "sqlite_journal_mode",
        "DELETE",
    ),
    synchronous=cfg.get(
        "sqlite_synchronous",
        "NORMAL",
    ),
    busy_timeout_ms=cfg.get(
        "sqlite_busy_timeout_ms",
        60_000,
    ),
    write_batch_articles=cfg.get(
        "sqlite_write_batch_articles",
        250,
    ),
)

try:
    articles = state.query_current_date_range(
        "2024-01-01",
        "2026-07-31",
    )

    article_ids = (
        articles["article_id"]
        .astype(str)
        .tolist()
    )

    assignments = (
        state.query_assignments_current(
            article_ids
        )
    )

    sentiment = (
        state.query_sentiment_current(
            article_ids
        )
    )
finally:
    state.close()
```

SQLiteは記事・assignment・sentimentのcurrent／historyを管理します。一方、Narrative Intensityそのものは主にpartition fileとして保存されます。

---

# 14. Point-in-time系列の保存とロード

Canonical Storeは、後日到着した記事や訂正を反映した、

```text
latest_revised
```

です。

ある過去時点で利用可能だった情報だけを使う場合は、`materialize_point_in_time_series`を使います。

```python
cfg = weekly_result.config

store = FixedNarrativeStore(
    weekly_result.store_root,
    weekly_result.classification_epoch,
)

state = FixedNarrativeStateDB(
    store.db_path,
    journal_mode=cfg.get(
        "sqlite_journal_mode",
        "DELETE",
    ),
    synchronous=cfg.get(
        "sqlite_synchronous",
        "NORMAL",
    ),
    busy_timeout_ms=cfg.get(
        "sqlite_busy_timeout_ms",
        60_000,
    ),
    write_batch_articles=cfg.get(
        "sqlite_write_batch_articles",
        250,
    ),
)

try:
    pit_result = materialize_point_in_time_series(
        as_of_cutoff=(
            "2026-07-31 23:59:59+00:00"
        ),
        start_date="2020-01-01",
        end_date="2026-07-31",
        store=store,
        state=state,
        narrative_master=weekly_result.registry,
        config=cfg,
        save=True,
    )
finally:
    state.close()
```

保存先は次です。

```text
<store_root>/
└── fixed_narratives/
    └── point_in_time/
        └── classification_epoch=<epoch>/
            └── as_of=20260731T235959Z/
                ├── articles.parquet
                ├── daily_by_reservoir.parquet
                ├── daily_aggregate.parquet
                ├── weekly.parquet
                ├── monthly.parquet
                ├── signals.parquet
                └── manifest.json
```

関数は、記事state、assignment history、sentiment historyを指定cutoffで再構成し、日次・週次・月次・signalを保存します。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

その場でDataFrameを利用する場合は、

```python
pit_daily = pit_result[
    "daily_aggregate"
]
pit_weekly = pit_result["weekly"]
pit_monthly = pit_result["monthly"]
pit_signals = pit_result["signals"]
```

です。

---

# 15. 同じ`store_root`と`classification_epoch`を使う

初回と増分更新では、必ず同じ設定を使います。

```python
PRODUCTION_CONFIG = {
    "store_root": (
        "/local/path/fixed_narrative_store"
    ),
    "registry_path": (
        "/path/fixed_narrative_registry_73_public_v1.json"
    ),
    "sentiment_dictionary_path": (
        "/path/japanese_polarity_dictionary.xlsx"
    ),

    # 同じ設定を維持
    "registry_version": "public-73-v1.0.0",
    "classifier_version": (
        "fixed-multilabel-v1.0.0"
    ),
    "sqlite_journal_mode": "DELETE",
}
```

```python
initial_result = run_fixed_narrative_update(
    historical_articles_df,
    PRODUCTION_CONFIG,
    run_mode="initial_full",
)

weekly_result = run_fixed_narrative_update(
    new_or_changed_articles_df,
    PRODUCTION_CONFIG,
    run_mode="weekly_incremental",
)

assert (
    initial_result.classification_epoch
    ==
    weekly_result.classification_epoch
)

assert (
    initial_result.store_root
    ==
    weekly_result.store_root
)
```

以下を変更すると`classification_epoch`が変わる可能性があります。

```text
固定Narrative Registry
Narrative rule
分類器version
embedding model
テキスト前処理
日本語極性辞書の内容
sentiment model version
```

既存Storeに異なるepochを混在させない設計で、異なるepochが検出されると停止します。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# 16. 過去記事の訂正・バックフィル時の注意

現行コードでは、`affected_dates`を含む週だけを`affected_weeks`として再計算します。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

週次Intensity changeが、

$$
\Delta I_w=I_w-I_{w-1}
$$

であるため、古い週 $w$ を訂正すると、

- 週 $w$ のchange
- 週 $w+1$ のchange

の両方が影響を受けます。

通常の「最新週への記事追加」では問題になりませんが、過去週への訂正・バックフィルがある場合は、集計全体を再構築するのが安全です。

```python
reaggregate_result = run_fixed_narrative_update(
    pd.DataFrame(),
    weekly_result.config,
    run_mode="reaggregate",
)
```

`reaggregate`はSQLiteに保存されている全current dateを`affected_dates`として取得し、Intensityを再集計します。`02_fixed_narrative_intensity_incremental_memory_safe_implemented_japanese_polarity_xlsx.ipynb`

---

# 17. 推奨する実務フロー

```python
# 1. 初回
initial_result = run_fixed_narrative_update(
    historical_articles_df,
    PRODUCTION_CONFIG,
    run_mode="initial_full",
)

# 2. 毎週
weekly_result = run_fixed_narrative_update(
    new_or_changed_articles_df,
    PRODUCTION_CONFIG,
    run_mode="weekly_incremental",
)

# 3. 全期間の最新系列をStoreからロード
full_result = load_full_fixed_narrative_store(
    store_root=weekly_result.store_root,
    classification_epoch=(
        weekly_result.classification_epoch
    ),
)

# 4. 運用signal用
monthly_signal = (
    full_result["monthly"]
    .query(
        "series_type == 'latest_revised' "
        "and monthly_variant == 'signal_cutoff'"
    )
)

# 5. Narrative beta用
weekly_intensity = (
    full_result["weekly"]
    .query(
        "series_type == 'latest_revised'"
    )
)

# 6. 既に計算済みのMomentum signal
momentum_signals = full_result["signals"]
```

Narrative Mapへ渡す関連記事はIntensity partitionとは別に、固定Narrative IDと期間を指定して抽出します。Narrative Mapsの入力は、特定issueに関する時刻付き文書集合と開始・終了イベントであるため、全期間Intensityを直接入力するのではなく、該当Narrativeの記事集合をmaterializeします。`2009.04508v2.pdf`

最も重要な整理は次のとおりです。

```text
weekly_result
    = 今回の増分更新結果

runs/<run_id>
    = 今回の監査用snapshot

intensity/* partition
    = 全期間の最新Canonical系列

point_in_time/as_of=*
    = 特定時点で利用可能だった歴史系列
```



---
Powered by [ChatGPT Exporter](https://www.chatgptexporter.com)